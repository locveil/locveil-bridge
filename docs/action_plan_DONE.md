# Action Plan — Completed tasks (frozen archive, by workstream)

**Status:** Frozen archive of completed `action_plan.md` tasks, organized **by workstream**. Re-IDed
to the `PREFIX-N` scheme 2026-06-30 (§5.2 #5 / DOC-9). One ledger, **every ID in exactly one file**; a
task **moves** here from `action_plan.md` on completion (same change as its journal entry). Old
positional IDs resolve via [`action_plan_aliases.md`](action_plan_aliases.md). This file is move-only.

---

## DRV — Device drivers

- [x] **DRV-1** `[P0]` — **DONE 2026-07-07** (closed by the user at the rack sitting). **#7 — Per-driver HW verification pass, pre-P3.6 scenarios.** All seven driver classes hardware-verified (per-row evidence in the table below, updated through the 2026-07-07 sitting). **Residuals, explicitly re-homed at close:** children-room LG smoke **waived by the user** ("always worked — it was my development environment"); the LG reconnect-cycle test + the mf_amplifier **mute** re-check (ROM20 stored-code suspect, user re-learns from the OEM remote) fold into **REL-3**'s converged rack pass; the eMotiva scenario route + the Auralic playback-with-content walk ride **SCN-3**; the A77 transport walk moved to **DRV-15** (post-release, user decision). Methodology gate added 2026-05-27 after the user's instinct (matches [[mock-tests-miss-driver-bugs]]): scenarios are composites; verifying scenarios first masks driver bugs inside composite flows and makes diagnosis confusing. So verify each of the seven driver classes on hardware **before** the P3.6 scenario pass. **Subsumes §5.1 #3 (A77 re-verify) — A77 is just one row of the pass.** Same shape per driver (and per config instance for the multi-instance ones):
  - **Setup.** Bridge starts cleanly; driver registers with no errors; the WB virtual device appears with `meta/available=1`; the device card shows in the WB UI.
  - **Action set.** Walk the concrete action list (below). State persists across a bridge restart (assumed-state DB does its job).
  - **State read-back.** Any value topic the driver publishes (`/devices/<id>/controls/<x>`) updates within ~1 s of the change.
  - **Error recovery.** Disconnect the device briefly (pull power, drop off Wi-Fi, etc.) → driver re-establishes cleanly on next attempt, no leaked tasks/sockets in the bridge log.

  | Driver | Instance(s) | Action set to walk |
  |---|---|---|
  | `LgTv` (asyncwebostv) | **LG living: DONE 2026-05-27** · LG children: deferred | DONE on the living-room OLED77G1RLA: power on (WoL → connect → 3 subscriptions register cleanly post-`tvpower` URI flip) · volume± + mute (subscription delivers physical-remote deltas via the `volumeStatus` unwrap) · foreground-app transitions (home / launch_app / browser) · `power_state` subscription delivers physical-remote off in ~0.5 s with `reason=remoteKey`, ~45 s before the WebSocket finally closes (audit canary closed) · `current_app` + `input_source` coalesced via `app_id_to_input_id` helper · pointer move + tap-click on the pointer socket. DEFERRED (non-blocking): `children_room_tv` smoke pass (config-only, identical driver — expected to mirror); reconnect-cycle test (TV unplug → reconnect, exercises asyncwebostv 0.3.4 close-callback registry + the discard-old-controls reconnect contract). See §6 entry 2026-05-27. |
  | `EMotivaXMC2` (pymotivaxmc2 0.6.8) | **DONE except scenario route 2026-07-07** (input redesign HW-verified 2026-05-29; power/zones/volume verified 2026-07-07) | Input switched from physical HDMI connectors (`hdmiN`) to **logical sources** (`sourceN` via `select_source` + `get_input_names`); topology `processor:hdmiN`→`sourceN`, reconciler unchanged (data-driven). **2026-07-07 rack pass:** zone-1 power-on clean (3.6 s, 9-property refresh, ARC recognized `HDMI ARC`→`arc`); source1/ZAPPITI + source2 re-verified (the **ack quirk observed live** — device answers `emotivaUpdate` where `emotivaAck` expected; pymotivaxmc2 warns + retries, notify stream confirms the switch → handled, benign); **zone2 power toggles + independence verified** (main zone untouched throughout); **zone2 volume verified** (−10/−5 dB; exposed the DRV-11 space-padded-negative parse bug, fixed same sitting). **Main-zone (zone1) volume: N/A by topology** — the processor's only audio output is `processor:zone2 → mf_amplifier:aux2` (main-zone outputs unwired; TV audio returns via ARC); nothing to test. Mute read-back stays protocol-impossible (optimistic-only, documented). **Remaining: scenario route only** (rides the SCN-3/SCN-9 composite pass). See §6 2026-05-29 + 2026-07-07. |
  | `AppleTVDevice` (pyatv) | **living + children: DONE 2026-05-28** (tvOS 26.5) | DONE on both units: power on/off + clean connect (pyatv git-pinned to master SHA `9177803` for the `TVRCSessionStart` fix — tvOS 26.5 silently drops Companion *query* commands without it; see §6) · app list works · nav (up/down/left/right/select/menu/home) + playback (play/pause/stop/next/prev) via Companion HID · pointer pad: drag→directional gesture + tap→select (dx/dy param fix + capability `click`→`select`) · **volume±: absolute `set_volume` removed** (no `_mcF` Volume flag on tvOS 26.5 — Companion volume is dead), routed through the **WB IR blaster** (living `wb-msw-v3_207` ROM5/6; children `wb-msw-v3_220` ROM1/2); volume UI = up/down buttons, mf_amplifier-style (no slider/mute). app-launch-by-name works; dynamic-launch UX deferred to §5.1 #2. See §6 2026-05-28. **Re-verified living unit 2026-07-07** (rack sitting): fully green — dropdown app launch fixed same day (DRV-2 DONE) and the pointer pad verified right after. |
  | `AuralicDevice` (openhomedevice, all-network) | Auralic Altair G1 (**wired LAN**, DHCP-reserved `.142` since 2026-07-07) | **HW walk LARGELY DONE 2026-07-07** (the DRV-12/13/14 sitting — see those DONE rows + journal). **Bench-probe answered:** Volume service PRESENT (volume readable even in standby); sources list = 11 (Playlist/Receiver/AirPlay/Radio/Bluetooth/AES/COAX/TOS/USB/QobuzConnect/LightningCast); power tri-state on/standby/**halted** mapped and fully network-controllable (DRV-14 — IR retired). **Verified:** discovery (rebuilt on raw M-SEARCH, DRV-13 — the async listener received nothing on real HW), wake-probe cadence (DRV-12), full power ladder incl. halt wake on fresh handles (22 s), inputs list + true-index source selection wiring (DRV-14), rediscovery across many port-changing transitions, clean start + state restore across bridge restarts (exercised ~6× this sitting). **Remaining:** playback walk with content playing (play/pause/stop, next/previous, volume±, mute, now-playing read-back) — natural fit for the `music_auralic` SCN-3 pass; the Previous button still unexposed in `streamer.json` (add if wanted); optionally tune `op_timeout`/`reconnect_interval` to observed timings. |
  | `WirenboardIRDevice` (aiomqtt → WB IR) | DVDO, Pioneer LD, Panasonic VHS, **MF amplifier: FIXED 2026-05-29**, Zappiti, Dodocus | per-instance: 2–3 representative actions from the device's configured action set; full coverage isn't needed if a sampled action proves the IR path. **mf_amplifier (Musical Fidelity M6si): root-caused + fixed** — IR was dead because `wb-msw-v3_207` bank 65 was stuck in edit mode (coil `5199+65`=1), which made the blaster return "Slave Device Busy" for *every* Play. Caused by an `ir_restore.py` bug (no edit-exit on a busy commit), **not** firmware. Cleared the lock live (amp responds) and hardened `ir_restore.py` (guaranteed edit-exit + busy-retry + preflight unstick). See §6 2026-05-29. |
  | `RevoxA77ReelToReel` (aiomqtt → WB IR) | A77 | **Moved out of release scope 2026-07-07 (user decision) → DRV-15** — the transport walk is post-release; DRV-1 completes without it. |
  | `BroadlinkKitchenHood` (RF, broadlink) | **kitchen_hood: DONE 2026-05-29 (tested working)** | hood power, fan speed, light on/off (orphan — no live scenario uses it, but verify the driver still works after the hexagonal pass) |

  **Also covered as a side-effect:** the aiomqtt 2.0.1 downgrade HW verify (every IR-via-WB driver row exercises the aiomqtt stack). Pass = ready to go into P3.6 scenario verification with isolated-driver confidence.

- [x] **DRV-2** `[P0]` — **DONE 2026-07-07** (root-caused live at the rack sitting, fixed same day, HW-verified from the dropdown — «works!!!»). **Apple TV app launching** (`Запуск приложений на AppleTV`). The launch-by-name path had worked since 2026-05-28; what failed was the **UI dropdown**, which sends the **app id** (`{'app': 'de.swr.avp.ard.tablet'}`) while `handle_launch_app` resolved the param only as a display NAME through the name→id table (entry: "ARD Mediathek") → `App not found`. Fix: dual matching (the LG driver's `_find_app_by_name_or_id` precedent) — an exact id from the installed-apps table launches directly, anything else goes through the case-insensitive name lookup, unknown values still error. 2 regression tests (exact-id launch incl. the ARD evidence shape; unknown-id errors without dispatch). Suite 614, pyright 0, contracts 6/6. HW-verified: ARD Mediathek launches from the dropdown on `appletv_living`. The pointer-pad re-test stays noted on the DRV-1 Apple TV row.

- [x] **DRV-5** `[P1]` — **DONE 2026-07-08** (desk session; UX pinned interactively same day — **reactive re-tap** replaced the old arm-checkbox sketch, and the scenario-level variant was filed as **SCN-11**). **Reserved `force` param bypasses idempotence guards + the UI re-tap escape hatch for optimistic-state desync.** The trap: optimistic state says `power=on`, the device is actually off (someone used the physical remote) → the IR driver's "already on, skip" guard swallows the correcting command on a one-way channel — unfixable from the UI. **Shipped:** (1) **`BaseDevice.idempotence_skip(...)`** — the single guard chokepoint: skips only when at-target AND not forced, marks every skip with `data={no_op: true, skipped_reason: "idempotence"}`; availability guards ("unreachable / no client") never use it and are never force-bypassed. All **9 guard sites** converted: WirenboardIR `power_on`/`power_off` (the HIGH-value pair), eMotiva `power_on` / `power_off` ×2 / `set_input` / `set_volume` / the ARC power-cycle guard (post-inventory addition — believed `arc` goes stale, force re-runs the cycle), Auralic `power_on` (live-query, LOW), LgTv `power_on` (the DRV-10 guard — skip surfaced in the handler, `force` threaded into `power_on()` as a belt). (2) **`force` is reserved cross-cutting:** `_resolve_and_validate_params` preserves it alongside declared params (undeclared keys were silently dropped for declared-params commands); the canonical expansion already passes unmapped names through. (3) **Response threading:** `CanonicalActionResponse.skipped_reason` (new field, OpenAPI + UI types regenerated); the `wait:false` branch threads `no_op`+`skipped_reason` (the guard runs synchronously inside `perform_action`, so the marker needs no echo wait). **Bonus fix found en route:** a `wait:true` canonical call on an already-at-target guarded device previously **503-ed** (idempotence skips never fire `update_state`, so no echo lands) — the guards now set `data.no_op`, so the existing short-circuit returns a clean no-op success; voice saying «включи» to an already-on IR device is fixed. (4) **UI (reactive re-tap, zero standing chrome, no manifest change — the skip response IS the signal):** on `skipped_reason: "idempotence"`, `RuntimeDevicePage` arms an 8 s offer — amber banner in the remote + pulse on the matching power button; re-tapping the SAME control re-sends with `params.force=true`; any other control or the timeout disarms. **Tests:** `test_force_idempotence.py` (one regression per guarded handler: skip carries marker + nothing actuated / force actuates) + 2 canonical-route tests (marker rides `wait:false` with force pass-through; skip short-circuits the `wait:true` echo wait). Suite 636, pyright 0, contracts 6/6, UI check+build clean, eval `make cli` 4/4. **Docs:** `ui_backend_contract.md` force re-tap section; `architecture/devices-and-scenarios.md` + `ui.md` (the long-promised "manual resync button" now describes the shipped affordance). **Still true (unchanged non-goals):** toggle-code IR power has no guard to bypass (the state-claim issue is SCN-11's `assume_state` correction); force is a user-mediated escape hatch, not feedback. Successor: **SCN-11** (per-device force-reconcile dialog on the active scenario page, builds on this foundation).

- [x] **DRV-7** `[P2]` — **RETIRED 2026-07-12 (exported, closed by DRV-35 — never completed here).** The ESP32 deck-bridge firmware scaffold moved to **`locveil-satellite`** (council HK-4 / PROD-15): imported verbatim at bridge `a80322f` (satellite DES-6, their commit `0d950a9`), then harmonized by their DES-1 — the bench-truth docs absorbed into `locveil-satellite/docs/devices/` per-device dossiers + their `docs/review/` DES-1 truth-pass record, the code judged reference-only (FW-phase material), the single-image FR-1 shape retired for per-device apps (the GPIO14 triple-booking). The bridge-side `ESP32/` tree is DELETED; every file stays resolvable in git history at `a80322f` and earlier. Deferred bench fill-ins + first-light verification now belong to the satellite's FW phase. Journal 2026-07-12. _Original entry (frozen as parked):_ **PARKED: ESP32 firmware scaffold for the 4 transport-source bridges** (Revox A77 + Revox B215 + Pioneer CLD-D925 + Panasonic NV-FS90). Lived at `ESP32/` (PIO layout: `include/` + `src/` + `docs/`) — single image, identity selected at runtime via NVS + MQTT `/provision`. ~95% shared core (Wi-Fi auto-light-sleep + Wirenboard MQTT + MQTT-triggered `esp_https_ota` + record-arming + reel-motion interlock); 3 drivers cover 4 decks (Pioneer + Panasonic share `driver_ir.cpp` as baseband IR). 2026-05-26: rewritten from the original Arduino scaffold to pure ESP-IDF (C++17, framework=espidf, no Arduino libs); custom dual-OTA partition table (1.5 MB app slots); built clean end-to-end from `pio run -t fullclean` (RAM 11.2%, Flash 59.6% of 1.5 MB). Authoritative spec was `ESP32/REQUIREMENTS.md`; conventions `ESP32/CLAUDE.md`; per-device hardware handoffs `ESP32/docs/`.

- [x] **DRV-9** `[P1]` — **DONE 2026-07-04** (filed + executed same day, interactive session; pulled forward out of DRV-1 at the user's direction — "don't want to wait for the entire DRV-1"). **Capability map for `kitchen_hood`** (`config/capabilities/classes/BroadlinkKitchenHood.json`) — the last unmapped device instance; closes the coverage gap CORE-2 surfaced. **Shape (user-decided):** two domains, `light` + `fan` — deliberately **no `power` domain**: the appliance's power is neither the light nor the fan, so a generic power-off can never half-kill it by accident. `light` = stateful, `state_field: light`, `on_value: "on"`, actions `on`/`off` → `set_light` with fixed `{state}` params, enum field triplet (`on`/`off` wire=canonical + ru/en labels) for the catalog. `fan` = stateful, `state_field: speed`, parametric `set` → `set_speed` (`param_map level→level`, 0–4) + `off` shortcut (`{level: 0}`) — the dimmable-light `brightness.set` precedent, not the enum-select one. Both `reconcile: false` (upscaler precedent): the hood is appliance-only, lives in no topology path; exposed on page/WB/HTTP/voice but the reconciler never drives it. Both `feedback: false`, `gate.delay_ms: 500` (RF blaster, no return channel). **WB output byte-identical** — explicit `wb_controls` take meta precedence; verified by the existing `test_wb_rekey` oracle (kitchen_hood row, unchanged snapshot). New `test_kitchen_hood_class_map_light_and_fan` (shape + referenced-commands-exist + no-power-domain guard); suite 488 passing, contracts 3/3. **Coverage milestone:** every shipped device instance now resolves a non-empty capability map (5 AV classes + 5 IR device maps + 57 profiled WB-passthroughs + the hood) — acceptance-gate item 1 annotated as satisfied for the current fleet (final confirmation stays with the gate pass). Stale "capability-less kitchen_hood" comments corrected (`wb_device/service.py`, `devices/base.py`).

- [x] **DRV-10** `[P1]` — **DONE 2026-07-07** (filed + fixed same rack sitting; found live during DRV-1). **LG TV driver: false-negative response classifiers fixed + already-on power churn killed.** Rack evidence: UI `set_volume` 20/40 → TV applied both (OSD moved, internal speakers), state stayed correct via the volumeStatus subscription, driver reported `success: False` (`{'volume': 20, 'soundOutput': ''}`). **Root cause (verified in `../asyncwebostv`):** the library's `standard_validation` pops `returnValue` — raising `IOError` when falsy — and returns the **stripped** payload; a returned dict therefore already means success. Three driver sites re-checked the stripped payload: **(1)** `_execute_media_command` — classifier replaced with the contract (returned-without-raising ⇒ success; failure channel = the library's `IOError`); **(2)** `launch_app` — same replacement (worked today only because this firmware's ack strips to `{}`; a `sessionId`-echoing response would have false-negatived); **(3)** `_execute_with_monitoring` — **correct as-is** (the `*_with_monitoring` library methods bypass `exec_command`/validation and return raw status dicts; its `returnValue`/`status` checks are appropriate). **Startup-init question answered along the way:** the TV initialized correctly at backend start (persisted state restored → WS connected → volume/power_state/foreground_app subscriptions → initial power_state delivery synced `power='on'` within 2 s); the observed weirdness was purely the power-on-while-already-on path — `ssap://system/turnOn` answers an **empty payload** on an already-on TV → old code WoL'd pointlessly, slept the 20 s boot wait, and did a full reconnect (the 10:51:29→10:51:49 churn). **Fix:** idempotence guard in `power_on()` — connected + `state.power == 'on'` ⇒ no-op success (`method: already_on`); trustworthy because power_state feedback delivers physical-remote transitions <1 s. **DRV-5 inventory updated** (LgTv zero → one guard, LOW force value, guard site carries a "must honor `force`" note). 4 regression tests (echo-payload set_volume, IOError failure channel, sessionId launch echo, already-on no-op asserting no WoL/reconnect/monitoring calls). Suite 591, pyright 0, contracts 6/6. **Re-verify `set_volume` + power-on-while-on at the TV after backend restart** (rides the DRV-1 LG row). **Re-verified on hardware same sitting (post-restart):** cold WoL power-on 11:19:36 (packet → 20 s boot wait → reconnect → subscription-confirmed `Active`), `set_volume` 30 with the exact echo payload `{'volume': 30, 'soundOutput': ''}` → success + OSD moved, already-on no-op guard 11:22:34 → success in **1 ms**, no WoL/reconnect, `method: already_on`.

- [x] **DRV-11** `[P1]` — **DONE 2026-07-07** (filed + fixed same rack sitting; found during the DRV-1 eMotiva walk). **eMotiva XMC-2: space-padded negative volumes parsed as 0.0.** Rack evidence: at connect and again on the zone-1 power-on refresh, the device notified `zone2_volume = '- 3.0'` — the XMC-2 right-pads short negative values with a space after the sign — and the driver's `_process_property_value` did plain `float(value)`, which raises on that form; the silent `except → 0.0` fallback reported **0 dB while the hardware sat at −3.0 dB**. Any zone's volume in the −9.9…−0.1 range hits the same shape (main volume −61.0 parsed fine only because two digits fill the field). **Fix:** strip spaces from string values before `float()` in the converter (+ a warning log replacing the silent fallback), and route the one raw write site — the zone2 `client.status(Property.ZONE2_VOLUME)` sync path, which stored the device string verbatim — through the converter. 3 regression tests (space-padded main + zone2, unparseable fallback). Suite 594, pyright 0, contracts 6/6. Library (`pymotivaxmc2`) confirmed not at fault — it hands notification values through as raw XML strings by design. **Re-verify zone2 volume display after next backend restart** (rides the DRV-1 eMotiva row's zone2 pass).

- [x] **DRV-12** `[P1]` — **DONE 2026-07-07** (filed + fixed same rack sitting; found during the DRV-1 Auralic walk). **Auralic: a device asleep at backend startup was never rediscovered.** Rack evidence: streamer physically woken → status stayed disconnected; live log showed **zero discovery attempts in ~40 min** after the boot-time failure. Root cause: `setup()` sets `_deep_sleep_mode = True` when boot discovery fails (a *guess* — "may be in deep sleep"), and the periodic loop's deep-sleep branch never probed ("stay quiet; power-on is the IR handler's job") — which also made the "never connected → retry on cadence" branch **unreachable** (the guess shadows it forever). The UI/IR `power_on` handler schedules a delayed discovery, but an **out-of-band wake** (front panel, Auralic app) had no hook at all. **Fix:** loop body extracted to `_periodic_tick(now)` (testable); the deep-sleep branch keeps state honest **and probes on the `reconnect_interval` cadence** like the other disconnected branches; `_last_reconnect_probe`/`_was_connected` promoted to instance attrs. 4 regression tests (deep-sleep probes on cadence, cadence window respected, offline-at-boot probes, healthy connection polls without discovery). Suite 598, pyright 0, contracts 6/6. **Workaround used to unblock the sitting:** UI power_on (IR pulse harmless on the already-on unit; its delayed-discovery hook connects). **The full Auralic action walk + reconnect-cycle validation continue on the DRV-1 row** after the next backend restart.

- [x] **DRV-13** `[P1]` — **DONE 2026-07-07** (filed + fixed same rack sitting; found immediately after DRV-12 — the cadenced probes fired correctly but discovery itself never found the unit). **Auralic SSDP discovery replaced: `SsdpSearchListener` receives nothing on real hardware.** Diagnosis chain at the rack: unit alive at `.11` (ping + raw M-SEARCH both confirm; config updated from the DHCP-drifted `.16`), yet the driver's discovery failed every round. Reproduced standalone against `async_upnp_client` **0.44.0**: `SsdpSearchListener` returned **zero responses** under every variant (sync/async callback, explicit `source=(lan_ip, 0)`, `ssdp:all` and `upnp:rootdevice` targets) while a **raw-socket M-SEARCH from the same box got 16 answers** including both Auralic endpoints (`lightningRender` + `lightningServer`). The May robustness pass's "async SSDP discovery" was mock-tested only — this was its first hardware contact. **Fix:** `_discover_device_url_async` now does a plain M-SEARCH (`ST: upnp:rootdevice`, MX 2, ~4 s) via `_msearch_sync` in an executor + pure-function `_extract_ssdp_locations` (sender-IP filter, LOCATION-host check, dedup); the downstream candidate classification (XML fetch → AURALIC manufacturer / name match → renderer prioritization) unchanged; `async_upnp_client` import dropped from the driver. Live-verified standalone: 2 candidates at the target, renderer found. 3 parsing tests (filter+dedup, host-mismatch reject, garbage tolerance). Suite 601, pyright 0, contracts 6/6. **Address question open (user-owned):** router lease table maps the unit's MAC `bc:34:00:a0:67:ba` to a `.142` "LivingRoom" entry while the woken unit still holds `.11` without re-DHCPing — decide the canonical address (fix the reservation to `.11`, or keep `.142` and let the unit renew onto it, then flip `streamer.json` once).

- [x] **DRV-14** `[P1]` — **DONE 2026-07-07** (research → implementation → HW-verified ladder, all in one rack sitting). **Auralic: all-network power control; IR power path retired.** The May-era "deep sleep needs IR" assumption was disproven live: the unit's power ladder is fully network-controllable — on ⇄ standby via `Product.SetStandby`, standby ⇄ **halted** via `HardwareConfig.SetHaltStatus` (halted = network UP, description + `HardwareConfig`/`Volume` served, `Product` deregistered; no network-dead state short of the rear rocker). **Library (fork `droman42/openhomedevice`, branch `hardware-config-halt`, PR'd upstream as bazwilliams/openhomedevice#26):** `hardware_config_service` + `is_halted()`/`set_halt()` (graceful None/no-op when absent) **+ the `Visible` dialect fix** (Linn `true` vs AURALiC `1` — the unit's 11 sources were all filtered out); fork suite 81 OK; bridge pins advanced twice (pin-guard test updated per its own instruction). **Driver:** `_adopt_openhome_device` classifies discovered devices (Product present ⇒ connected; absent ⇒ **halted, detected never guessed** — the boot-time deep-sleep guess is gone, unreachable is an honest error); `_wake_from_halt` rediscovers **first** on every attempt and sends the wake to the freshest handle (rack finding: the halt transition moves the unit's ports, so the stored handle got connection-refused and the swallowed error masked a undelivered wake — first ladder attempt failed exactly there); `power_off` = stop + `SetStandby(true)` + `SetHaltStatus(true)`, honest messaging, self-correcting via the cadence probe; IR machinery deleted (`_send_ir_command`, ROM62 toggle topics out of `streamer.json` — the toggle hazard is gone). **Also fixed en route:** options now speak the UI dialect (`input_id`/`input_name` — the empty-dropdown finding) **and carry TRUE device indices** (invisible sources occupy slots: AES = visible-position 5 but device-index 9 — filtered-position selection would have switched the wrong source); `set_input` resolves both numeric and by-name against the cached options; state `ip_address` refreshed on adopt (stale persisted `.16` shown long after the move to `.142`). **HW-verified ladder (13:18):** power_off → standby+halt (panel dark) → power_on → rediscover → `SetHaltStatus(false)` on the fresh handle → services re-register → standby exit → **on in 22 s**; inputs list shows all 11 sources. 10 new driver tests across the sitting (halted adoption/wake/power_off/no-op/dialect/true-index). Suite 612, pyright 0, contracts 6/6, OpenAPI byte-identical.

- [x] **DRV-18** `[P2]` — **DONE 2026-07-07** (filed at completion; the doc arrived from a browser design session per the cross-repo intake rule — claims verified against the `zappiti_updater` repo + live bridge code, then reviewed and reworked in-session). **Zappiti Neo network driver + catalog — design.** Deliverable: [`docs/design/zappiti-driver-spec.md`](design/zappiti-driver-spec.md). **Part I (device control, live-verified on the box):** the Dune HD IP Control contract — HTTP port 80, no auth, no runtime ADB; **discrete power** (`standby` is soft/reachable, `main_screen` wakes — the ROM26 toggle era ends); **fire-then-poll launch verification** (`command_status=ok` ≠ playing — proven by the `storage_label://` silent failure); `media_url` schemes (NFS/`storage_name://` tested); the `status` read model + 4-state machine; §6 two-authorities metadata (stored ffprobe-semantics content vs live state, joined by lang+codec, never by index). **Part II (catalog & indexing, decisions resolved same day):** **browser-native indexer** — mediainfo.js/WASM header probe via `showDirectoryPicker` over laptop mounts (zero-install; the productization direction for the orphaned-Zappiti user base), TMDb-first matching with the contributor loop (no-hit → fix TMDb upstream → re-match; OMDb demoted to a post-release optional module), bridge = **sole SQLite writer** behind an ingest API (separate `catalog.sqlite`, plain SQL — the draft's Peewee claim was wrong and is gone), server-side `w500`/`w780` artwork (~50–100 MB/1000 titles on `/mnt/data`), series/season/episode modeling, fingerprint idempotence + scan-session mark-missing pruning, catalog panel on the Zappiti device + movie scenario pages. **Open gates recorded in-doc:** §8 device items (power-cycle persistence, cold-launch wake, mount-order stability) + §13 design items (mediainfo↔ffprobe parity bench, HI-flag coverage, ingest auth). Per `design-then-implement`, implementation filed as **DRV-19** (Part I driver) + **DRV-20** (Part II catalog), both `[deferred]`.
- [x] **DRV-21** `[P1]` — **DONE 2026-07-09** (REL-5 #3 remediation, same session as the review). **Canonical select-form `set` now forwards the reserved `force`/`assume_state` params — the UI re-tap-to-force escape hatch works for AV inputs again.** The actions-form dispatch path forwards all incoming params through `CapabilityAction.expand`, but `CapabilitySelect.expand` takes only `value`, so `force`/`assume_state` were silently dropped for select-form capabilities (Emotiva/LG/Auralic `input`) — an input desync could never be recovered through the UI (the scenario reconciler had already worked around the same drop by manually re-injecting `force`). Fix: a new domain constant `RESERVED_PARAMS = {"force","assume_state"}` in `domain/capabilities/models.py`; the canonical dispatcher (`presentation/api/routers/devices.py`) overlays those params from the payload onto each expanded select-form `NativeStep` (step params win any conflict), mirroring the actions-form behaviour. Presentation→domain import only (hexagon clean — presentation can't reach infrastructure's `base.py` where the same reserved set is preserved). 3 new tests in `test_select_canonical.py` (force forwarded on parametric + by_value forms, assume_state forwarded). Suite +3, pyright 0, import-linter 6/6.
- [x] **DRV-22** `[P2]` — **DONE 2026-07-19** (REL-5 #10 remediation; low-hanging-fruit sweep). **IR driver's dead `last_command` enrichment removed.** The claim reconciled exactly: `record_last_command` (setting `command_topic`/`command_payload` into params) ran only inside the two IR handlers, and `BaseDevice._execute_single_action` unconditionally re-records `last_command` with plain params the moment the handler returns — the enriched record never survived a single call. The driver's version also hardcoded `source="mqtt"`, wrong for API-originated actions; the base record carries the true source. Fix = delete the method + both call sites (`_execute_ir_command`, `_create_generic_handler`) + the now-unused `datetime`/`LastCommand` imports. Zero behavior change (the base chokepoint recorded unconditionally before and after). Suite 738, pyright 0, import-linter 6/6. docs: none — internal dead-code cleanup, no user-visible behavior. contracts: none — no versioned surface touched.
- [x] **DRV-23** `[P1]` — **DONE 2026-07-09** (filed by the voice repo per `cross-repo-source-of-truth`; verified against live code + the running WB7, reframed, and fixed same session). **WB-passthrough devices now project their mirrored feedback onto the top-level `state.<field>` that voice reads.** **The filing's two claims, verified live:** (1) the **read path is genuinely broken** — WbPassthrough writes state-topic feedback only into `state.mirrored`, never the top-level field the catalog advertises; voice reads top-level `state.<field>` per the ARCH-8 contract (`mqtt_integration.md` §5c) and got `None`. Confirmed end-to-end: `GET /devices/cabinet_floor/state` had `room_temperature` (24.125 °C) **only in `mirrored`**, and «какая температура в кабинете» failed with `state read failed for cabinet_floor.room_temperature`. (2) The filing's **write-path claim was DISPROVEN** by a live on→off→off repro: `power.off` after `power.on` fired (`no_op:false`, light physically off) — WbPassthrough idempotence already compares `state.mirrored` correctly, and this driver never emits `skipped_reason:"idempotence"` (that marker is DRV-5's AV-driver helper). **Scope (live sweep of all 79 devices):** the bug is **WbPassthroughDevice-only — 39 of 65** WB-passthrough devices (every one advertising a readable field: heating_loop setpoint/room_temperature, dimmable level, cover position, hvac, sensor_room). **All other classes are clean** (`state.mirrored` exists only on `WbPassthroughState`; `BroadlinkKitchenHood` proves the point — it advertises a readable field and already exposes it top-level). **Fix:** a `model_dump` override on `WbPassthroughState` lifts each mirrored value to a top-level key of the same name — serialized-view only, so `mirrored` stays the single source of truth (idempotence reads `self.state.mirrored` directly, untouched; the reconstructed state gains no phantom attributes) and a declared field like `power` is never shadowed. 2 new tests in `test_wb_passthrough.py` (projection end-to-end + no-shadow guard). Suite 662, pyright 0, import-linter 6/6, OpenAPI/golden byte-identical (additive serialization; no contract/UI change). **Live re-verification owed to the redeploy** — «какая температура» should return 24.125 once the rebuilt backend image is on the WB7. **Voice re-tested live 2026-07-09: read path confirmed (24.125 spoken back); write-path filing retracted (the on→off→off repro settled it).**
- [x] **DRV-24** `[P2]` — **DONE 2026-07-09 (design task, `design-then-implement`).** **Make WB-passthrough switch `power` an authoritative, readable state — design.** Follow-on to DRV-23, from the voice side's live re-test: on the **39** momentary-power WB-passthrough switch devices (`light_switch`×24, `dimmable_light`×13, `power_switch`×2), the relay's real state sits raw in `state.mirrored['power']` while top-level `state.power` stays the vestigial default and the catalog advertises power as non-readable — so it reports the opposite of reality, and voice (which reads the catalog-advertised top-level `state.<field>`) can't query it (needed by voice QUAL-68 + ARCH-39). Deliverable: [`docs/design/wb_passthrough_readable_power.md`](design/wb_passthrough_readable_power.md) — three decisions: **(D1)** the three profiles' `power` capability goes `momentary → stateful readable` (`state_field` + `fields[power]` with an `'1'↔'on'` value table so the catalog advertises it); **(D2)** a load-time enrichment lets a device's bare `state_topics[field]` inherit the profile field's `type`/`values` (declare once per profile, device-explicit wins — also fixes the DRV-23 sibling where `heating_loop` `mode` projects as raw `'0'`); **(D3)** `_on_value_message` sets the declared top-level `power` from the coerced canonical value (DRV-23's `model_dump` `setdefault` stays for non-declared keys). **Scope pinned:** WB-passthrough `power` only — NOT kitchen_hood (Broadlink one-way), NOT HVAC (its on/off is `mode`, already DRV-23-projected), NOT AV/IR. **Contract:** power becomes a readable catalog field ⇒ **golden re-pin (cross-repo)**. Implementation filed as **DRV-25**. No code touched. **Design REVISED 2026-07-09 after maintainer review** (the D1/D3 summary above is the first cut): the review found the `mirrored` bucket is a workaround (the driver never holds a logical value apart from the mirror — for passthrough the mirror *is* the state; two consumers read two places, voice top-level vs the UI's `mirrored.power` raw). **D3 converged to retiring `mirrored` entirely** — passthrough state moves to **top-level dynamic fields** (`extra="allow"`, the AV-driver shape), which **subsumes DRV-23's projection** and drops the `power` duplication. Now an openapi **and** golden change (state shape changes) + a UI `HvacPanel` migration. The doc reflects the revised shape; DRV-25 carries the converged scope.
- [x] **DRV-25** `[P1]` — **DONE 2026-07-09** (implementation of the DRV-24 converged design, `docs/design/wb_passthrough_readable_power.md`; **pulled forward from `[deferred]` — the maintainer judged it a nasty bug that must not ship in release 1**). **WB-passthrough state converged to top-level fields; switch `power` is now readable/authoritative.** **D3 — `mirrored` retired:** `WbPassthroughState` is `extra="allow"`; the driver's `_on_value_message` sets each coerced value as a **top-level** field (declared `power`, or a dynamic `room_temperature`/`level`/`position`/`mode`…), `BaseDeviceState.model_dump` merges `__pydantic_extra__`, the idempotence guard reads `getattr(state, field)`, DRV-23's `model_dump` projection is gone, and a config-load collision guard rejects a `state_topics` key shadowing a reserved base field (`power` is the intended overlap). **D1 — profiles:** `light_switch`/`dimmable_light`/`power_switch` `power` → `stateful` + `state_field:"power"` + `fields[power]` value table `'1'↔'on'` + **`reconcile:false` explicit** (else the momentary→stateful flip would start reconciling all 39). **D2 — loader enrichment:** `attach_capability_maps` enriches a bare device `state_topics[field]` from the profile field's `type`/`values` (device-explicit wins) so `_coerce_mirror` yields canonical — verified live in-process: a light_switch echo `'1'` → `state.power = 'on'`, `power_off` while ON publishes, while OFF no-ops. **UI:** `HvacPanel.tsx` migrated from `state.mirrored.*` (raw `'1'`) to top-level canonical fields. **Contract (cross-repo re-pin):** golden `8159b4b0068d1c63` → **`16eee0f2f7832995`** (`power` now a readable catalog field). **openapi is byte-identical** — the `/state`/`persisted_state` responses are typed as an empty schema, so `WbPassthroughState` isn't in openapi and the shape change is invisible to it (the design's openapi-change prediction was wrong); `ui/src/types/*` regen was a no-op. Only the golden + stamp moved — **the voice side re-pins to `16eee0f2f7832995`.** Tests: `test_wb_passthrough.py` rewritten off `mirrored` (57 pass), DRV-23's two tests → direct top-level-field asserts, + collision-guard test. Suite **662**, pyright 0, import-linter 6/6, UI check+build green. Docs: `architecture/devices-and-scenarios.md` (state surfaces top-level). Supersedes DRV-23's projection (external read behaviour preserved). **Live re-verify owed to the redeploy** — rebuild the backend image + `update.sh`, then a spoken "is the light on?" reads authoritative power.
- [x] **DRV-26** `[P1]` — **DONE 2026-07-10.** **HVAC value tables: wire side is now the firmware's NUMERIC publish index — mode/fan/vane/widevane control works again.** The VWB-14 tables carried label strings as `wire` (`"COOL"`, `"<<"`), but `mitsubishi2wb` publishes numeric indices and its command callback silently ignores non-numeric payloads (`mitsubishi2wb.ino` `hpSettingsChanged()`/`mqttCallback()` — the 2026-07-09 investigation) — so inbound echoes failed enum-parse (raw `"2"` in `/state`) AND outbound `set_mode/set_fan/set_vane/set_widevane` were silently dropped by the firmware: HVAC control was dead in both directions. **Fix (config-only + tests):** in every table the declaration order already equals the firmware index, so `wire = str(position)` — applied to the `hvac` profile + the 3 device configs (canonical identifiers + trilingual labels unchanged): **mode** 0=auto…4=fan_only; **fan** 0=auto 1=quiet 2..5=speed_1..4; **vane** 0=auto 1=swing 2..6=pos_1..5; **widevane** 0=swing 1..5=far_left..far_right 6=split. **Tests:** the two VWB-14 drift-guard tests updated to the firmware truth (one generalized: every enum table asserts `wire == str(index)`); 2 new both-direction tests against the REAL children config (`_on_value_message('mode','2') → state.mode='cool'`; `set_mode(cool)` publishes wire `"2"`); the profile↔config drift-guard passed unchanged (both sides moved together). **Contract:** golden `16eee0f2f7832995` → **`a4a2b1aed5f86447`** (openapi byte-identical) — **the voice side re-pins to `a4a2b1aed5f86447`** (supersedes yesterday's un-pinned hash; one re-pin covers DRV-25 + DRV-26). Suite **664**, pyright 0, import-linter 6/6; no UI change (canonical vocab unchanged — HvacPanel unaffected). **Owed to the rack (REL-3):** the live check — canonical `set_mode(cool)` → wire `2` → echo → `state.mode='cool'`, and the first-ever functional mode/fan/vane taps from the HvacPanel.
- [x] **DRV-27** `[P2]` — **DONE 2026-07-10 (design task, `design-then-implement`; pulled forward from `[deferred]`; first cut REVIEWED-AND-REVISED same day — the reopen is the record of `read-at-start`/review discipline working).** **`MitsubishiHvac` dedicated driver — design, rev. 2** → [`docs/design/mitsubishi_hvac_driver.md`](design/mitsubishi_hvac_driver.md). Rev-1 decisions that STAND: **D1** name `MitsubishiHvac` (not "ESP32ManagedDevice" — ESP8266 modules; the contract is the mitsubishi2wb firmware dialect); **D3** wire↔canonical tables in the class map ONLY (driver translates via its attached capability map); **D5** typed state → VWB-18 restore-at-boot + heartbeat reachability (the firmware's unconditional 45 s room_temperature publish; no LWT exists); **D7** NO bridge-owned WB card; **D8** icons via the AV IconResolver mechanism → **UI-16**. Rev-2 REVISIONS (user review rejected the first cut's shape): **D2** the single `climate` capability replaced by **SIX per-domain capabilities** (power/mode/fan/vane/widevane/temperature — the LgTv/kitchen-hood/eMotiva action-group shape; `{value}` param convention mirroring VWB-19; `reconcile:false` explicit; `profiles/hvac.json` dies, `heating_loop` untouched); **D4** topic derivation from one `mqtt_device` field rejected — the 3 device configs keep the **explicit per-action `commands` + bare `state_topics` schema** and **move out of `wb-devices/` to `config/devices/` root** (bespoke home), the driver validating completeness at load. Owned consequences: canonical HVAC vocabulary changes (golden bump, rides the single voice re-pin) and `HvacPanel.tsx` requires rework (reverses rev-1's "panel unchanged"). REST-firmware horizon in §8 (bench-twin-gated, unfiled). Implementation = **DRV-28** `[P1][release]` (amended to rev. 2). No code touched.
- [x] **DRV-28** `[P1]` — **DONE 2026-07-10** (implementation of the DRV-27 rev. 2 design, `docs/design/mitsubishi_hvac_driver.md`; one sitting). **The `MitsubishiHvac` driver is live in the codebase — the three air conditioners are bespoke devices now.** **Driver** (`infrastructure/devices/mitsubishi_hvac/`): typed `MitsubishiHvacState` (canonical mode/fan/vane/widevane + setpoint/room_temperature floats + reachable; base `power` carries on/off) riding VWB-18 restore-at-boot; **heartbeat reachability** (the firmware's unconditional 45 s `room_temperature` publish; watchdog flips `reachable` after 3 silent intervals — first honest offline detection, no LWT exists); commands translate canonical → the firmware's NUMERIC wire via the ATTACHED class map and ride the standard `idempotence_skip` chokepoint (force/assume_state; cold-start unknown state never skips); **NEVER creates a WB virtual device** (user hard requirement — the firmware owns its card; enforced twice: inherited `enable_wb_emulation=False` + no emulation call, test-pinned). **Shared translation module** `infrastructure/devices/value_translation.py` extracted from the passthrough driver (which now delegates via staticmethod aliases, public/test surface unchanged) — the import-linter `independence` contract forbids driver cross-imports; `mitsubishi_hvac` added to the independence list + entry point registered. **Config**: `MitsubishiHvacConfig` subclasses the passthrough schema + a firmware-contract completeness validator (missing command/state field = loud load error); the 3 device configs MOVED `wb-devices/<room>/` → `config/devices/` root, reshaped (explicit per-action `commands`, BARE `state_topics`, `device_category: appliance`, state field `temperature` → `setpoint`); `classes/MitsubishiHvac.json` = six per-domain capabilities with the DRV-26 tables + `reconcile:false` (param_maps target the configs' historical native names speed/angle/direction); `profiles/hvac.json` DELETED. **device-state-mapping.json** entry + `MitsubishiHvacState` in `OPENAPI_EXTRA_MODELS` (user catch — openapi 90→91 schemas). **Catalog derivation extended**: a `set {value}` param inherits the capability's STATE-FIELD table (preserves the VWB-24 zero-round-trip property under the new `{value}` convention; the legacy by-name rule stays). **UI**: `HvacPanel.tsx` reworked to the six-capability vocabulary (`power.on/off`, `{mode,fan,vane,widevane,temperature}.set {value}`; state reads top-level canonical incl. the `setpoint` rename). **Docs**: `devices-and-scenarios.md` (nine drivers, MitsubishiHvac in the native-protocol table, HVAC graduation note), `README.md`, `howto-new-device.md` (hvac profile removed from the pick list; stale value-label workaround note replaced). **Tests**: new `test_mitsubishi_hvac.py` (15 — contract validation, no-WB-emulation pin, subscriptions, both translation directions, idempotence/force/cold-start, heartbeat flip+recover, restore round-trip, catalog projection); `test_capabilities.py` hvac block rewritten against the class map (+ bare-config enrichment drift-guard); contract tests updated to the new shape. **Contract:** golden → **`5622ba7a1a78102a`**, openapi +`MitsubishiHvacState`; UI types regenerated — **the voice side re-pins ONCE to `5622ba7a1a78102a`** (covers DRV-25+26+28; the canonical HVAC vocabulary changed: six capabilities, `{value}` params — their intent mapping must follow, not just the hash). Suite **678**, pyright 0, import-linter 6/6, UI check+build green, eval cli 4/4. **Owed:** backend+UI image rebuild + WB7 redeploy; HW check rides REL-3.
- [x] **DRV-29** `[P1]` — **DONE 2026-07-10** (filed by the voice repo per `cross-repo-source-of-truth` off the first DRV-28 live smoke; verified against code + accepted same day — the filing was consumed on acceptance, NOT erased, same as DRV-23). **The canonical echo window now honors the capability's `gate.poll_timeout_ms` — AC commands no longer report failure while succeeding.** The filing was exactly right on the mechanism: `CANONICAL_ECHO_TIMEOUT_S` was one flat 500 ms constant (`devices.py:30/:570`) with no override, while the mitsubishi2wb firmware confirms on its packet rotation — observed live: command 08:36:23.9 → 503 at +500 ms → the echo landed ~7 s later and state flipped correctly. Every AC command 503'd `device_unreachable` while working (the 2026-07-09 living-room 503 was the same signature). **The fix is cleaner than the filed suggestions** (per-class constant / new accepted-confirmed shape): the per-capability timing already existed — `CapabilityGate.poll_timeout_ms`, honored by the reconciler since day one (LgTv power: 8000 ms) but IGNORED by the canonical endpoint. Now the endpoint waits `cap.gate.poll_timeout_ms` when set, else the 500 ms relay default (also silently improves LG power over voice). **The window is DERIVED, not guessed** (user requirement — read from the firmware + HeatPump lib source): confirm latency = `PACKET_SENT_INTERVAL_MS` 1 s + settings read-back on the 6-slot info rotation × `PACKET_INFO_INTERVAL_MS` 2 s = ~13 s worst case, ~5–7 s typical (matches the observed 7 s) → `gate: {poll_timeout_ms: 15000}` on all six `MitsubishiHvac` capabilities. **No contract change** — gates aren't catalog surface; golden/openapi byte-identical, NO re-pin. The "accepted/confirmed" alternative already exists as `wait:false` — voice's choice: truthful-but-late speech (their HTTP timeout must exceed 15 s) or instant-optimistic. 2 new endpoint tests (slow echo + gate → 200 with confirmed state; ungated → 503 at 500 ms byte-identical). Suite **682**, pyright 0, import-linter 6/6. Backend-only image rebuild + redeploy; then the still-owed DRV-26 mode-change smoke returns a clean 200.

- [x] **DRV-30** `[P0]` — **DONE 2026-07-10** (REL-3 rack sitting #1 findings F1+F4, `docs/review/rel3_rack_findings_2026-07-10.md`; owner mandate: scenario startup safe *as if ARC/CEC worked* — must-have pre-release, shipped same day with SCN-14). **eMotiva driver hardening: post-power-on readiness gate + keepAlive watchdog + re-subscribe on recovery — all notification-driven, no blind delays (owner requirement), all timings measured on hardware.** **(a) Readiness gate:** `handle_set_input` holds via `_await_input_ready()` inside the post-power-on window — normal path proceeds after **2 s of notification quiescence** (clean power-up burst measured < 1 s ×3 samples); the **ARC-exit case holds the full 15 s window** (design sharpened during implementation: the incident wedged **3.3 s of silence AFTER the `'arc'` claim** — quiescence alone provably insufficient; the CEC handshake is invisible beyond notifications; tune at the DRV-32 bench); the window anchors on our `power_on` send AND any observed Off→On (front panel / CEC power-ups covered); no anchor or long-settled → zero hold. **(b) keepAlive watchdog:** `Property.KEEPALIVE` joined `PROPERTIES_TO_MONITOR`; interval read from the transponder discovery info (**7500 ms** device-advertised; `getattr(client, "_info")` — the library has no public accessor yet, candidate lib PR), fallback 7.5 s; **3 missed intervals** → `connected=False` + `error` (NO new state field — reuses the existing surfaces, so openapi/contracts stay byte-identical, no re-pin) + **fail-fast guard on all six command handlers** (speakable error instead of the incident's 2×9 s blind retry burns; reserved `force` bypasses per the DRV-5 convention). **(c) Re-subscribe on recovery:** the watchdog's recovery probe IS a re-subscribe (device-side subscriptions die with the device — F4; subscribing is idempotent per spec, its ack proves liveness, and the subscribe response re-seeds state through the callbacks); a stray notification while lost also recovers in place (subscriptions survived). Watchdog task starts after setup's subscribe, cancelled in shutdown; every notification timestamps `_last_notification_monotonic` (monotonic — wall-clock jumps can't fake a beat); `keepalive` property is stateless by test. **9 new tests** (heartbeat bookkeeping, Off→On anchor + no re-anchor, notification-recovers-lost, watchdog tick lost→probe-recovers + stays-lost-on-failed-probe, fail-fast + force bypass, quiescence hold, ARC-exit full-window hold, no-hold-when-settled). Suite **595**, pyright 0, import-linter 6/6, contracts untouched.

- [x] **DRV-33** `[P1]` — **DONE 2026-07-10** (REL-3 rack **sitting #2** flag — the only one; filed + fixed same session, SCN-12 precedent). **LG TV optimistic input write used the user-assigned LABEL — believed state poisoned whenever the TV was already on target.** `handle_set_input_source` wrote `input_to_set.get("label")` (`"Emotiva XMC"`, the owner's rename of HDMI2) into `state.input_source`, while the foreground-app subscription writes the **ID** (`app_id_to_input_id` → `'HDMI_2'`). When the TV physically changes input the event corrects the label ~100 ms later; when the TV is **already on the target there is no event** and the label sticks → `'Emotiva XMC'` ≠ `'hdmi2'` under any normalization → **every scenario switch re-dispatched the TV input as a phantom diff and its gate reported a false failed step** (log evidence: gate timeouts at 12:41/12:44/12:45/12:47 on every switch; in the 12:48 SCN-11 force the optimistic label write even clobbered the real `'HDMI_2'` event 80 ms after it landed). SCN-14's honest gates surfaced the pre-existing lie — the actuation itself always worked (`com.webos.app.hdmi2` confirmed). Fix: one line — the optimistic write uses `input_id`, matching the event vocabulary. Test updated (it had PINNED the label behavior) + a dedicated already-on-target regression (no event → optimistic write is the only writer → must stay id-form). Full tree **696**, pyright 0, import-linter 6/6, contracts untouched.

- [x] **DRV-34** `[P2]` — **DONE 2026-07-12** (filed + executed same session; PROD-15 bridge delegation items 1a+5, council HK-4). **HK-4 supersession doc pass — the ESP32ManagedDevice narrative annotated, DRV-7's destiny recorded, `productization_bridge.md` §3 amended.** **(a)** The VWB context narrative's "Decision locked 2026-06-08" paragraph now carries a dated SUPERSEDED block (annotate-don't-rewrite): the HVACs never became the class — they run **ESP8266** (Wemos D1 Mini), so the chip discrepancy parked at intake resolves AGAINST the plan narrative and FOR the board's HK-4 wording (evidence: DRV-27 decision D1, `docs/design/mitsubishi_hvac_driver.md`, `docs/wb_device_authoring_log.md`) — and they graduated 2026-07-10 to the bespoke `MitsubishiHvac` driver (DRV-27/28); the ESP-managed class arrives renamed + reshaped as descriptor-native **`EspManagedDevice`** (DRV-36) consuming the VWB-38 convention descriptors, with the grow-into-UI-surfaces intent (provisioning state, OTA progress, firmware version) carried over. **(b)** The narrative's stale positional back-pointer fixed to **DRV-7**; the remaining `§5.1 #N`-style refs verified as legitimate alias-map history and left alone. **(c)** DRV-7's entry annotated with its changed destiny: the `ESP32/` tree is a frozen import source (single-image FR-1 retired satellite-side — the GPIO14 double-booking), retirement rides DRV-35, do not reactivate in this repo. **(d)** `productization_bridge.md` §3's "no bridge work filed" bullet amended (dated, HK-4, the doc's established annotation style): the two-layer generator shape (convention down, descriptors up — regime 1 vindicated) + the filed IDs. Frozen journal/revision-log/DONE mentions untouched; `docs/architecture/`, `docs/guides/`, `README*`, `ui/`, `backend/` grep-verified clean of the name. Docs-only — no code, no contract.

- [x] **DRV-35** `[P2]` — **DONE 2026-07-12** (PROD-15 bridge delegation item 1b, council HK-4; filed at intake `caa72a5`, unblocked same day by the satellite's repo-to-repo confirmation `c592733`). **The top-level `ESP32/` tree DELETED (35 tracked files, one change) + DRV-7 retired with a journal pointer — the deck-bridge scaffold's home is `locveil-satellite` now.** Gate evidence: satellite **DES-6** (their `0d950a9`) copied the tree verbatim @ bridge `a80322f` into `imports/bridge-esp32/`; their **DES-1** harmonization then absorbed the docs into `locveil-satellite/docs/devices/` dossiers + their DES-1 truth-pass review record and deleted the staging copy (import commit keeps it resolvable; owner verdict recorded there: code reference-only, docs were the value). Pre-delete sweep: zero references to the tree outside itself (README/architecture/guides/CI/build configs/code all clean; the plan's historical `§5.1`/alias refs stay per convention). The entry's VWB-38 NB is discharged — the wb-mqtt-v1 promotion source was re-pointed BEFORE the delete (`be774a8`, satellite repo-to-repo filing) to their truth pass + dossiers, so nothing depends on the deleted copy. The 2026-07-08 verdict executed as reconfirmed by HK-4. Tree removal + ledger only — no code, no contract. *(Execution note: the delete took two commits — `git add -A` in the first also swept in two previously-untracked local leftovers (`include/config.h`, `sdkconfig.serial`) because deleting `ESP32/.gitignore` in the same change unmasked them; both verified secret-free (config.h was byte-identical to the committed template — placeholders only; sdkconfig.serial is ESP-IDF-generated) and removed in the follow-up commit. No history rewrite needed.)*

- [x] **DRV-36** `[P2]` — **DONE 2026-07-12** (PROD-15 bridge delegation item 3, council HK-4; design task, `design-then-implement`; **design-only by owner decision — implementation waits for the first real satellite device**). **`EspManagedDevice` driver designed** → [`docs/design/esp_managed_device.md`](design/esp_managed_device.md), consuming the VWB-38 convention (`device-integration-v1`). The shape: **one driver class for every Locveil-built satellite device, descriptor-native** — a driven adapter whose entire per-device knowledge arrives from the satellite-authored descriptor; a new device = pin its descriptor + thin device config, zero bridge authoring. Key decisions: **(a)** thin `config/devices/<id>.json` (`device_class` + room + `descriptor` ref) + **byte-identical descriptor pin** at `config/descriptors/<device_id>.json` (the mirrored-pins discipline, inverse direction of the convention pin), validated at load against the VWB-39 convention pin — schema nonconformance / unknown profile / convention-major mismatch **refuses to load** (VWB-35 fail-fast precedent); **(b)** the descriptor's capability block is **mechanically translated into the class-map dialect** as the loader's third per-instance source (`control`→`command`, static `confirm_latency_ms`→`gate.poll_timeout_ms` on feedback capabilities, triplet tables pass through) — catalog projection, param derivation, and the DRV-29 gate honoring are UNCHANGED code; **(c)** `EspManagedDeviceState` = dynamic canonical fields + `reachable` from the retained `meta/online` **LWT** (honest last will at last — DRV-27's heartbeat workaround explicitly not copied) + the `meta/locveil` stamp block with derived `stale_pin` (monitor-only, never branched on); one-time `OPENAPI_EXTRA_MODELS` + `device-state-mapping.json` addition; **(d)** commands publish `<control>/on` (pushbutton fires `"1"`), confirmation = the convention's echo within `confirm_latency_ms`; unreachable → fail-fast + `force` bypass (DRV-30 convention), stateful actions ride `idempotence_skip`, momentary never skip; **(e)** broker-wipe self-heals by design (firmware re-announces on ITS reconnect — no VWB-32-style bridge action needed); **(f)** `requires_arm` stays firmware-enforced single-point — no bridge-side gate; UI arm-flow + interlock catalog projection deliberately deferred to the first device's UX pass; **(g)** the deck transport vocabulary is a DEFERRED deliberate contract cut batched with the implementation (satellite's repo-to-repo vocabulary request + golden bump + single voice re-pin) — golden untouched by this design. Follow-up filed: **DRV-37** (implementation, BLOCKED on the satellite's first conforming descriptor — the descriptor is the fixture; VWB-39 activates alongside). Design-only — no code, no contract.

- [x] **DRV-39** `[P1]` — **DONE 2026-07-15.** **eMotiva silence-while-busy: zero new control traffic while a power/input transition is live — the terminal fix for the wedge whack-a-mole.** Evidence: [`emotiva_wedge_20260714.md`](review/emotiva_wedge_20260714.md) (Findings 1/4/4c) + [`emotiva_arc_community_research_2026-07-15.md`](review/emotiva_arc_community_research_2026-07-15.md). **`busy` is now a first-class driver state** (`_busy_since`, renamed from `_power_on_monotonic`): armed by ANY commanded transition (`power_on`/`power_off`/`set_input` — armed in `execute_action` after a successful dispatch, plus the power-on handler) AND by the **uncommanded `arc` grab** (`_handle_property_change`: source→arc arms independently — the wedge-#2 trigger condition, previously only power-on anchored). **The gate (`_await_device_ready`) now returns a verdict and FAILS CLOSED:** settled (2 s notification quiescence) → proceed + clear the window; cap reached without settling (still transitioning, or a fresh-`arc` handshake whose CEC traffic is invisible to us) → **refuse** — `execute_action` returns `success=False`/"still settling" instead of firing into a live window (reverses DRV-38a's release-on-cap; wedge cost ≫ failed-step cost). `set_input` TO arc rides the quiescence path (engagement choreography); `force` is the operator override (checked in `execute_action`, bypasses the whole gate). **The power-on handler tail is GONE** — the defensive `subscribe(9)` + `sleep(1) → _refresh_device_state()` batch that fired a dozen-plus packets straight into the CEC window (the 2026-07-14 wedge itself) removed; we're already subscribed and the device pushes its power notification + burst unbidden. **Subscription-breadth audit** (the openHAB "limited processing power" lever): each of the 9 monitored properties maps to a surfaced state field (UI reads `zone2_volume`; scenarios match `video_input`/`audio_input`; `audio_bitstream` is now wanted for DRV-32), so the set stays — the real load win was dropping the redundant re-subscribe. **DRV-40 folded in: recovery-probe backoff** — the watchdog still ticks every keepAlive interval (tight detection) but the re-subscribe probe backs off interval→×2→cap (`KEEPALIVE_PROBE_BACKOFF_MAX_S=90 s`), reset on recovery; the 2026-07-14 outage's 2 455 probe cycles / 12.5 h become a handful. Tests: 6 new + 5 reshaped (arc-arm, transition-arm, fail-closed through real dispatch, force bypass, the 2026-07-12 wedge replay now asserts *refused* not just held, probe backoff + reset); suite 728→734, pyright 0, import-linter 6/6, no contract drift (fail-closed is a standard `CommandResponse{success:False}` the executor already handles as a failed step). **HW verification rides the DRV-38 rack replay** (with the DRV-32 CEC-state pin first). docs: none — internal driver readiness semantics; no manifest node describes the gate (arch/ui power-button §① describes the UI escape hatch, still true).

- [x] **DRV-40** `[P2]` — **DONE 2026-07-15 — folded into DRV-39** (same `_watchdog_tick`; doing it separately would have meant two passes over one function). Recovery-probe exponential backoff (interval → ×2 → `KEEPALIVE_PROBE_BACKOFF_MAX_S`), reset on recovery, first probe still immediate on going lost. Detection latency unchanged (the tick cadence is the keepAlive interval); only the re-subscribe attempts against a confirmed-dead unit are throttled. Tests + gates counted under DRV-39. docs: none — internal watchdog timing; no manifest node describes the recovery probe.

## SCN — Scenarios / topology / reconciler

- [x] **SCN-1** — **DONE** — scenario layer rebuilt (Harmony/reconciler model) + **hardware-verified** 2026-05-22, re-verified at the Layer-3 cutover 2026-05-24 ("no degradation"). Tail: one deferred full-reconciler HW re-test (resync the amp's drifted optimistic state). See §6 (2026-05-21/22/24) + `docs/archive/scenarios/scenario_redesign_progress.md`. _Original task:_ **Investigate and fix the scenario layer — currently broken.** Per the project vision, the #1 success criterion is "it actually works": every device action + every scenario, end-to-end on real hardware. Device actions mostly work; **scenarios do not** (confirmed by the user 2026-05-20). This is the headline gap between today and "done = my house works", and is distinct from the architecture/docs/GSD track. Scope: reproduce the failure(s), determine whether it's startup/shutdown sequencing, condition evaluation, role-action dispatch, WB-adapter, or state — then fix and verify on hardware.
- [x] **SCN-2** — **DONE 2026-05-26 — Transition-aware manual notes (load-bearing).** Backend `79c3588`: `ScenarioState.manual_steps` (single source of truth) populated by `ScenarioManager` on activation, cleared on deactivate/shutdown; dropped the redundant copies from `ScenarioResponse` + SSE event payloads + the `_switch_via_reconciler` return. UI `bd80cc5`: `RemoteControlLayout` new `manualSteps` prop renders a "For this activation" subsection (amber) above the static startup/shutdown notes; the `<details>` auto-opens when transition steps exist; `RuntimeScenarioPage` threads `ScenarioState.manual_steps`, guarded on `lifecycleActive`. New transition-case test (start appletv → switch to ld → Dodocus note appears; deactivate clears; next start is fresh). Phase-2 refinement (only emit notes for newly-activated links — diff-based, not every-activation) intentionally NOT done: over-prompting on every activation is correct for load-bearing notes. **Hardware verification still gated on the user.**
- [x] **SCN-3** `[P0]` — **DONE 2026-07-07 — all four music scenarios hardware-verified** (the second half of the SCN-9 rack sitting; user closed after confirming amp/A77/Auralic behavior). **Per-scenario evidence:** `music_turntable` — amp power→4 s gate→`input_cd`, volume ± and **mute (fresh ROM20) verified**, reworded manual notes surfaced; `music_auralic` — the cleanest switch of the day (ONE action: amp `input_balanced`), playback from Lightning DS on the Playlist source with **full now-playing read-back** (title/artist/source) after three live driver fixes (list-artist flattening, SourceIndex-based current-source, the lib's skip() Playlist fallback) + **Previous exposed end-to-end** (catalog golden → `8159b4b0068d1c63`); `music_reel`/`music_tape` — back-and-forth switches at 13–21 ms (post gate-fix), amp on `cd`, A77 power toggle behaving, hub notes per position. **Collateral hardened en route:** the reconciler power-off gate bool-complement fix (the 25 s teardown stall), lifecycle SSE moved to the domain chokepoint (stale-page fix), the quiet halted probe, and the manual-notes two-source rework (user wording in topology positions). **DRV-1's delegated residuals are all discharged** (eMotiva scenario route rode SCN-9's movie switches; Auralic playback rode this). Original build record below (2026-05-25, mock-validated then):

**BUILT 2026-05-25 (mock-validated; pending hardware verification).** Wiring interview done; the four
round-2 **music** scenarios are authored + reconciler-driven (`f1455c6`, `368fbcb`, `59fb661`):

| Scenario | Source | Amp routing | Notes |
|---|---|---|---|
| `music_auralic` | `streamer` (Auralic) | direct → `mf_amplifier:balanced` | controllable; playback on the streamer |
| `music_reel` | `reel_to_reel` (Revox A77) | Dodocus **Reel** → `mf_amplifier:cd` | controllable (IR); Dodocus note auto-surfaces |
| `music_tape` | `b215` (Revox B215) | Dodocus **Tape** → `mf_amplifier:cd` | **passive** manual source; amp volume + "press Play" note |
| `music_turntable` | `kuzma` (Kuzma Stabi S) | → Sugden PA4 → Dodocus **Phono** → `mf_amplifier:cd` | **passive**; amp volume + manual notes (power on Sugden, set hub, cue the record) |

The Dodocus RCA hub is now the central analog selector (5 positions: ld/vhs/reel/tape/phono, all →
amp `cd`). The two passive sources (no driver) are modelled as **manual topology nodes** + a one-line
reconciler change (a manual-node `source` anchors the topology path so the amp input + the hub note
resolve, but isn't itself controlled) — see §6 (2026-05-25). `kitchen_hood` stays appliance-only.

**Remaining:** **hardware verification** of the four (amp powers + selects the right input; Dodocus
manual notes show; Auralic/A77 playback; passive ones show the right manual steps). The **children's
room** (children_room_tv + appletv_children) was **deferred by the user** (skipped this round) — a
possible round-3.

- [x] **SCN-4** `[P0]` — **DONE 2026-07-04 (design task — deliverable is the design document, per `design-then-implement`).** **Scenario ↔ Wirenboard integration, decided in an interactive session** → `docs/design/canonical_first.md`. The discussion outgrew the original WB-representation question into the system's **target actuation architecture**; decisions: **(1)** option **(b)** — one **Scenario Manager** WB device («Сценарии») with a `{wire,canonical,labels}` enum select over scenario ids (activate = write id, deactivate = `none`; the entity's only published state) — no per-scenario devices (the deleted, disliked approach), no WB scenes (source-of-truth inversion), not option (a) (would forfeit the future WB-native Alisa path for scenarios); **(2)** scenario-inherited commands (volume/playback/…) fire at the same entity as canonical commands with **bridge-side fire-time role resolution** (static-union catalog advertisement → byte-stable catalog; 409 `no_active_scenario`/unbound-role; response carries `executed_on`); **(3)** the proxy is **universal** — the UI scenario page dispatches through it too (user's unification call: manifest demoted to pure render projection; power zone → `scenario.set/off`; write-proxy/read-direct rule); **(4)** **canonical-first** target architecture — device pages migrate to the canonical grammar too (the exposure gate already made page surface ≡ capability surface, so only grammar differed); system converges on catalog(read)+canonical(write)+state/SSE(read), UI and Irene as peer clients of one contract; `/action` demotes to internal (acceptance-gate decision); **(5)** param metadata **derived, not authored** — native config param specs projected through the capability `param_map` + value-label tables, one projection function feeding catalog AND manifest (discharges the catalog's owed #19 stub; rejected: authoring in capability maps = duplication; moving constraints into maps = churn). **Follow-ups filed:** SCN-6 (phase 1: proxy seam), SCN-7 (phase 2: device pages, gated on VWB-17 — re-scoped up from voice-only future-proofing), VWB-15 annotated (param projection rides it; scenario_manager additive). The original entry's tie-ins (Layer-3 rendering, manual-steps surfacing) had already shipped; reconciliation narrowed SCN-4 to the representation question before the session. Voice-side impact: scenarios reach Irene as one more catalog device — zero new contract concepts (her repo's SCN-4 watch-flag resolves additively). *(Amended same day on the user's note: managers are **per-room** — `scenario_manager_<room_id>`, concurrent rooms; see the journal's amendment entry + doc §3/§4. SCN-6 carries the per-room domain work.)*
- [x] **SCN-5** `[P0]` — **CLOSED OBSOLETE 2026-07-06 (task-start-reconciliation, user-confirmed) — already addressed by SCN-2.** The row claimed the activation-time manual-notes half was still unshipped and load-bearing ("without it the LD/VHS scenarios have no audio") — but SCN-2 (DONE 2026-05-26, backend `79c3588`+`e7cbcb5`, UI `bd80cc5`) already shipped exactly that: the reconciler emits every manual-node instruction on the incoming scenario's topology path at activation, `ScenarioManager` captures them per room and threads them through `get_scenario_state()` → `/scenario/state`, and the UI renders the amber "For this activation" section — locked by `test_transition_to_ld_surfaces_dodocus_note_on_switch_and_clears_on_deactivate` (5 manual-notes tests passing at close). The only literal delta in the row's wording — diff-based suppression (emit only for links NOT already active in the outgoing scenario) — is the phase-2 refinement SCN-2's entry **deliberately declined** ("over-prompting on every activation is correct for load-bearing notes"), and it is near-vacuous in the current fleet anyway (every analog↔analog transition changes the Dodocus position, so the note text always changes). **Origin of the stale row:** the 2026-06-30 re-ID pass re-scoped the self-referential former §5.2 #6 ("file the task") into an implementation task without reconciling against SCN-2's shipped scope. No code change; hardware verification of the notes rides the existing HW-gated rows (SCN-3 / SCN-1 tail).
- [x] **SCN-6** `[P1]` — **DONE 2026-07-04** (same-day implementation of the SCN-4 design, `docs/design/canonical_first.md` §3–§5; first link of the pre-catalog chain SCN-6 → VWB-17 → SCN-7 → VWB-15). **Canonical-first phase 1: the per-room Scenario Manager proxy seam**, shipped in three commits: **(1) `17f2ded` per-room domain state** — `current_scenario` singleton → `active: Dict[room_id, Scenario]`; per-room persistence keys `active_scenario:<room_id>` with a ONE-SHOT legacy-key migration in `_restore_state`; room-scoped switch diffs (another room's scenario untouched — two rooms run concurrently); `deactivate(room_id=None→all)`; `Scenario.validate_configuration` requires `room_id`; role-action ambiguity → new 409 `ambiguous_role`; `/scenario/state` gains `?room=`; SSE payloads carry `room_id`. **(2) `168f820` the proxy seam** — pure-domain `ScenarioProxy` (`domain/scenarios/proxy.py`): entity registry (`scenario_manager_<room>` per scenario-bearing room), fire-time role→device resolution (speakable codes `no_active_scenario`/`role_unbound` → HTTP 409, new `CanonicalErrorCode` values), activation (`scenario.set` validates existence + room ownership, `scenario.off` = room deactivate), config-static `union_actions()`; `SCENARIO_ROLE_DOMAIN` moved to domain (layout engine imports it); canonical endpoint recognizes entities BEFORE device lookup — `scenario` capability handled by the proxy, inherited domains resolve then FALL THROUGH the existing echo-waiting dispatch (response keeps entity identity + new `executed_on` field); catalog gains one `CatalogDevice` per room (`scenario` select with value-enum over the room's ids + `none`; static union of inheritable domains; **byte-stable across switches — locked by test**); WB «Сценарии» card per room (`infrastructure/scenarios/wb_adapter.py`: text `scenario` control = the entity's ONLY state, retained, tracking the room slot via the new `ScenarioManager.on_active_changed` observer; curated play/pause/stop/volume pushbuttons through `proxy.execute`); bootstrap wires proxy + adapter (replaces the WB-re-key deletion placeholder). **(3) UI cutover** — `LayoutManifest.canonicalEntityId` + per-control `canonicalCapability`/`canonicalAction` annotations (reverse-mapped from the role capability at manifest build); `RuntimeScenarioPage` dispatches EVERYTHING canonical: power zone → `scenario.set/off` (legacy switch/shutdown mutations kept only as fallback), annotated controls → canonical tuple at the entity (bridge resolves at fire time — stale-manifest targeting gone), un-annotated controls (list queries) fall back to per-device `/action` until SCN-7. Contract regenerated (openapi + `openapi.gen.ts`); UI check + build green. **Tests:** 14-test two-room proxy suite (room-scoped resolution + concurrency, fire-time semantics after a switch, param translation, catalog stability, WB card setup/executor/value tracking, REST 409s + `executed_on`) + per-room reworks across the scenario suites; **502 passing**, contracts 3/3 throughout. **Docs:** key-concepts (rooms-as-concurrency-unit + Scenario Manager section), devices-and-scenarios, interfaces (WB scenario cards), `ui_backend_contract.md` (per-room lifecycle + the canonical-dispatch contract). **HW verification owed:** the WB cards + a live two-room drill at the next rack session (mock-verified only).

- [x] **SCN-7** `[P2]` — **DONE 2026-07-04** (third link of the pre-catalog chain SCN-6 → VWB-17 → **SCN-7** → VWB-15; same-day follow-through). **Canonical-first phase 2: device pages onto the canonical grammar** (`docs/design/canonical_first.md` §5–§6). **(1) Device manifests carry canonical annotations** — every action-backed control gains `canonicalCapability`/`canonicalAction` (no `sourceDeviceId`: the target is the device itself); `RuntimeDevicePage` dispatches annotated controls via `POST /devices/{id}/canonical`, un-annotated ones (select-form dropdowns) keep the native `/action` fallback. **(2) `wait: false` echo mode** — new `CanonicalActionRequest.wait` (default true, additive): false = fire-and-return-current-state, skipping the ~500 ms echo wait AND the reachability verdict (SSE delivers live state as before). The UI sends `wait:false` on all button dispatch (device + scenario pages — the pre-SCN-6 `/action` UX preserved exactly); voice keeps the default (speakable post-action result); HvacPanel keeps `wait:true` explicitly (it merges the echoed state into the cache). **(3) Option enumeration is a READ** — new `GET /devices/{id}/options/{inputs|apps}` resolves the capability's declared `list` query and executes it internally (`source="system"`, so a dormant list command still answers); `useInputsData`/`useAppsData` switched from POSTing `get_available_*` actions to the GET; the canonical action path is now purely imperative. **(4) One metadata source** — new shared §6 projection (`presentation/api/param_projection.py`): native-view feeds the manifest's `ProcessedParameter` (capability-FIXED params now excluded from specs — they're send-values in `ProcessedAction.params`, not user knobs), canonical-view (reversed `param_map` rename, sequence-step union) ready for the catalog's `CatalogAction.params` in VWB-15 — one code path, two views. **Finding filed as VWB-19:** select-form actions (`input.set` via `select`/`by_value`, app launching) are NOT canonically routable — the endpoint walks `cap.actions` only — so input/app dropdown *selection* stays on `/action` and voice cannot switch inputs canonically yet. **Gates:** suite **515 passing** (6 new in `test_canonical_first_phase2.py`), pyright 0, contracts 3/3, openapi + UI types regenerated, UI check + build green. Docs: `ui_backend_contract.md` gained the phase-2 section; `interfaces.md` canonical row updated (+`wait`) and the options endpoint row added. Phase 3 (`/action` demotion, `/scenario/switch`+`shutdown` internalization) remains acceptance-gate material.

- [x] **SCN-8** `[P2]` — **DONE 2026-07-04** (filed + executed same day off the user's question "why do we need `name` at all?"). **Dropped the legacy flat `ScenarioDefinition.name`; `names` (LocalizedName, ru/en required) is the single name surface** — mirroring the device `device_name` → `names` migration, done the same day `names` was born (VWB-20/G3) so no third dialect ever existed. Consumers switched: the scenario manifest title now renders `names.ru` (matching device manifests' `cfg.names.ru`); the catalog's en-fallback branch removed (labels always `names.model_dump()`); **the UI's long-standing wart fixed** — `useDataSync.ts` had been faking localization from the flat string with the literal comment "API doesn't seem to have translations yet"; it now consumes real `names.en`/`names.ru`, so the navbar's Russian scenario names («Кино с Apple TV») finally render for the ru-default household. 9 scenario configs dropped `name`; ~12 test fixture sites swept to `names`. Contract: `ScenarioDefinition` schema updated (openapi + pin + UI types regenerated); **golden catalog content UNCHANGED** (labels already flowed from `names` since VWB-20 — only STAMP's commit pointer moved). Suite **522 passing**; pyright 0; contracts 3/3; UI check + build green.

- [x] **SCN-9** `[P0]` — **DONE 2026-07-07** (filed at dawn, unblocked mid-day by DRV-1's close, verified by dusk — one sitting). **Scenario lifecycle regression re-verification — start / switch / end on hardware.** The core Harmony loop re-proven on the movie scenarios after ~6 weeks of restructuring (hexagon, canonical dispatch, eMotiva logical sources, DRV-10..14): **(1) start** — `movie_zappiti` activation 14:59 (first run surfaced 3 step findings: the TV-input dialect mismatch `hdmi2`≠`HDMI_2` FIXED in the LG matcher; the amp input fired into its 1 s power gate — gate raised to 4 s; zappiti power ROM26 re-learned + config corrected to a TOGGLE, catalog golden → `d0536f643b783d8a`); **(2) switch** — `movie_zappiti → movie_appletv` 15:22 and back 15:26, diff-only both ways (shared TV/amp untouched; dropped source powered down; the switch-back exposed the ordering-edge settle gap — the Zappiti released 4 ms after the eMotiva's ack → `delay_ms: 5000` on the topology edge, proper feedback-gated edges filed as SCN-10); **(3) end** — deactivate 15:43: five teardown actions, zero failures (TV off, eMotiva zones 1+2 off, amp + zappiti toggles), room physically dark; **(4) restart survival** — 15:19 restart restored `movie_zappiti` as active and re-reconciled with a minimal 2-action diff, 0 failures. Also verified en route: audio/subtitle dispatch correctness (ROM38 carried ROM37's code — re-learned same day). **Pending, riding SCN-3's sitting:** feel-check of the 5 s settle (topology loads at boot; the running instance predates the change). The scenario machinery itself needed zero fixes — every finding was dialect, timing data, or stored IR.

- [x] **SCN-11** `[P1]` — **DONE 2026-07-08** (filed + designed + shipped same desk session, immediately after DRV-5 per the user's sequencing). **Per-device force-reconcile dialog on the active scenario page — user-mediated desync repair.** The scenario-level face of the DRV-5 escape hatch: the desync *symptom* usually surfaces as "some device in the running scenario isn't in the expected state"; the user picks exactly the one wrong device (the precision variant of the rejected blanket scenario-force flag — the human is the feedback channel). **Domain (pure reconciler additions):** `_power_actions`/`_input_action` gained `force=` (skip the believed-vs-desired diff, inject the reserved `force` param; toggle-only power carries **`assume_state` = the plan target**); **`build_forced_device_plan()`** (single-device forced plan through the normal `_order` — cross-device ordering edges drop out *correctly*, both endpoints must be in the plan; test-pinned: the 5 s `processor.input → video.power` settle applies in the full movie_zappiti plan and NOT in the forced video-only plan); **`build_reconcile_preview()`** (believed-vs-desired rows per involved device, zoned power as per-zone dicts, plus the forced chain a confirm would run); `ScenarioManager.reconcile_preview`/`force_reconcile_device` (active-only — on an inactive page the gesture is just "start"). **The toggle-claim correction (the sharp edge):** the IR power-toggle handler honors `assume_state` — a forced toggle claims the TARGET instead of blind-flipping the possibly-wrong belief, which would have recreated the desync mirrored and made a second tap power the device back off (zappiti power is a TOGGLE config since ROM26); `assume_state` joined `force` as a reserved cross-cutting param. **Presentation:** `GET /scenario/{id}/reconcile_preview` (pure read; 404 unknown / 409 inactive; rows carry `in_sync`, `reconcilable`, `steps`, worst-case `eta_ms`) + `POST /scenario/{id}/force_reconcile {device_id}` (runs the gated chain server-side; executed steps + failures back). **UI:** "Device states…" button under the remote (active only) → `ForceReconcileDialog` — out-of-sync rows highlighted, in-sync calm (the inversion: in-sync is exactly where force matters), non-reconcilable greyed; row tap expands to the chain + ETA (nothing fires on the tap — fat-finger safety); "Send anyway" with per-row progress/result. **Tests:** 13 new (5 reconciler, 7 manager+endpoint incl. force/assume_state observed end-to-end through `execute_plan`, 1 driver toggle-claim). Suite 649, pyright 0, contracts 6/6, UI check+build, eval cli 4/4; OpenAPI 36 paths / 90 schemas regenerated. Docs: `ui_backend_contract.md` SCN-11 section; `architecture/ui.md` + `devices-and-scenarios.md` (user-facing voice). **HW feel-check** (open the dialog on a live scenario, force a desynced IR device) rides the next rack sitting / REL-3.
- [x] **SCN-12** `[P1]` — **DONE 2026-07-09** (REL-5 #6 remediation, same session as the review). **Teardown now honours `reconcile:false`.** `build_power_off_plan` had no `reconcile` guard (unlike `build_plan`'s power-on path), so on `POST /scenario/shutdown` / a graceful switch it emitted an IR `power_off` to a `reconcile:false` power capability — the `upscaler` (auto-powers with the LD, "exposed on page/WB/HTTP but skipped by the reconciler"). Benign end-state for the upscaler but a racing unwanted emission, and any future always-on / toggle-power `reconcile:false` sink would have been actively mis-driven. Fix (1 line + comment): `if power_cap is None or not power_cap.reconcile: continue`, mirroring `build_plan`. New test in `test_reconciler.py` (upscaler ON → power-off plan is empty). Suite +1, pyright 0, import-linter 6/6.

- [x] **SCN-14** `[P0]` — **DONE 2026-07-10** (REL-3 rack sitting #1 findings F2+F3, `docs/review/rel3_rack_findings_2026-07-10.md`; must-have pre-release with DRV-30). **Reconciler gates: the comparison canonicalizes and a timeout is a failed step.** **(a)** `_gate` compared the plan's *canonical* target to the device's *raw wire* state — `living_room_tv.input` polled `'hdmi2'` against `'HDMI2'`/`'HDMI_2'`, so the TV-input gate had NEVER confirmed (every activation burned the full 3 s and logged the timeout). Fix: `_gate_reached()` — exact match, then the capability's **value table** (wire→canonical, now carried on `PlannedAction.value_table`, populated at all five build sites from `cap.fields`), then **normalized comparison** (lowercase alnum-only) as the tableless fallback for dynamic-input drivers like the LG. **(b)** a gate timeout on a `feedback:true` step is recorded as a **failed step** in `ExecutionResult.failures` (the step stays in `executed` — dispatched and acked, never confirmed; `success` keys off `failures`), surfacing through the existing switch-response/WB-card failure path instead of "(proceeding optimistically)" — the mechanism that let `tv_on_speakers` report success twice (11:12, 11:19 UTC) while ARC never engaged. Feedback-less steps keep the optimistic path. 4 new gate tests (wire-form normalization ×3 forms, value-table translation, timeout→failed-step with executed+failures both populated, feedback-less exemption); the force-endpoint fake now reflects commands into believed state (a static-state fake correctly *fails* honest gates — deliberate). `devices-and-scenarios.md` execute step rewritten. Suite **586**, pyright 0, import-linter 6/6, contracts untouched. Expected live behavior: `tv_on_speakers` honestly fails until DRV-32 restores CEC (owner decision). The TV's `'arc'` gate remains unconfirmable by design (the LG records no `'arc'` state) — the proper state predicate is a DRV-32 bench question.

- [x] **SCN-15** `[P1]` — **DONE 2026-07-10** (REL-3 sitting #2 follow-up, filed + fixed same session — the tail of the DRV-33 flag). **ALL state-vs-target comparison sites unified on one `_satisfies()` helper — the SCN-11 preview and the build_plan diff must never disagree with the execution gate.** After DRV-33 fixed the LG state vocabulary, the SCN-11 dialog still showed the TV "out of sync" with believed `'HDMI_2'` vs desired `'hdmi2'` (owner screenshot): SCN-14 had canonicalized only `_gate`'s comparison, while `_input_action`/`_power_actions`/`_power_off_actions` diffs and `build_reconcile_preview`'s `in_sync` computation used raw equality — the same wire-vs-canonical disease in its third and fourth homes. Fix: `_satisfies(observed, target, value_table)` (exact → wire→canonical table → normalized fallback) now serves the gate (`_gate_reached` delegates), all five diff sites (zone + single power on/off, input), and both preview comparisons (zoned power per-zone). Consequences: the dialog reads in-sync when the state is honestly on target in wire form, and plans stop re-dispatching phantom TV-input steps entirely (`already_satisfied` now catches the wire form). 2 new tests: the exact screenshot case (preview in-sync with `'HDMI_2'` vs `'hdmi2'`) + plan-diff already-satisfied across vocabularies. Fresh post-DRV-33 logs confirmed the execution side was already clean (zero gate timeouts since redeploy). Full tree **698**, pyright 0, import-linter 6/6, contracts untouched.

- [x] **SCN-16** `[P1]` — **DONE 2026-07-13** (filed + executed same session; DRV-38(b) review remediation — evidence `docs/review/topology_readiness_review_2026-07-13.md` lane R3). **Zone-aware power planning: a zone off the used signal path is never touched.** `ZonePower` gains an optional **`port`** (planner metadata — grep-verified it never reaches the catalog projection: golden byte-identical, `backend/openapi.json` untouched, zero contract drift); the eMotiva capability map declares zone 2 ↔ port `zone2`. `resolve_targets` now also returns **`used_ports`** (BOTH endpoints of every path link — `source_targets` structurally drops the src port when dst wins for a mid-chain device, which is exactly the eMotiva: dst `source2` AND src `zone2` in one start); `_power_actions` skips a ported zone whose port isn't on the path; `build_forced_device_plan` + `build_reconcile_preview` apply the SAME filter (SCN-11 parity — without it the dialog would read movie_ld "out of sync" forever). Effect: `movie_ld`/`movie_vhs` drop the spurious `zone2_power_on` (the 2026-07-12 wedge trigger fired there for zero benefit — audio rides Dodocus RCA); appletv/zappiti/tv_on_speakers keep it (path uses `processor:zone2 → mf_amplifier:aux2`). Teardown untouched (off-when-off is idempotent). Tests: used-ports both-endpoint collection, ld skips / appletv keeps zone 2 at plan level (real topology + real capability maps), preview desired-set parity. Suite 719→724, pyright 0, import-linter 6/6. docs: none — planner-internal zone selection; no manifest node teaches zone-level power planning (the config-glob candidates `arch/devices-and-scenarios` + `howto/new-scenario` describe scenarios above this granularity and stay true).

- [x] **SCN-17** `[P2]` — **DONE 2026-07-13** (filed + executed same session; DRV-38(b) review remediation — the same review, lane R2). **Executor defense-in-depth: per-step dispatch bounded.** `execute_plan` wraps `device.execute_action(...)` in `asyncio.wait_for(DISPATCH_TIMEOUT_S = 60 s)` — generous by design (any legitimate driver readiness hold, the eMotiva's 15 s cap included, plus a slow command must fit comfortably; a guard rail, not pacing). Timeout → the step fails with a named `dispatch timeout` error under SCN-14 semantics (recorded in `failures`, plan continues — steps are globally serialized, so an unbounded hang would have frozen every later device's step, the R2 residual). Test: a hung device stub costs exactly one failed step and the next device still executes. Rides the SCN-16 gates (suite 724). docs: none — internal executor guard rail; no manifest node describes dispatch mechanics.

- [x] **SCN-18** `[P1]` — **DONE 2026-07-15** (owner decision same day: **tracking only**, picked over time-window / deploy-sha / reconcile-observe after the analysis showed restart-vs-deploy is detectable but misclassifies the power-outage case, and reconcile-at-boot is a no-op exactly when it's safe). **Boot restore executes NOTHING: a redeploy / crash / outage recovery never touches hardware.** `_restore_state` now calls the new `_restore_tracking(room, scenario)` — the state-setting half of a switch (active map, persistence, WB card + SSE via `_notify_active_changed`) with zero device dispatch and no activation manual steps — instead of the full `switch_scenario`. Kills the wedge-#3 exposure (2026-07-14 08:07: the boot-restore re-ran `movie_appletv`'s cold-start plan unattended — [`emotiva_wedge_20260714.md`](review/emotiva_wedge_20260714.md) Finding 6) AND the 3am-outage cold-start nobody had hit yet. Drift after an outage shows honestly in the SCN-11 out-of-sync dialog and reconciles on demand. Tests: restore dispatches zero device commands + surfaces no manual steps (2 new); the existing restore/migration/persistence tests pass unchanged (they assert tracking, which was always the documented story). Suite 725→728, pyright 0, import-linter 6/6. docs: arch/key-concepts (the restart passage now states tracking-only explicitly — it already described rehydration without claiming execution; overview + devices-and-scenarios verified still true).

## VWB — Voice-integration + native WB onboarding

- [x] **VWB-1** — **DONE 2026-06-06.** Generic WB-passthrough driver (`infrastructure/devices/wb_passthrough/driver.py`): config-driven (one command = one publish; static `value` OR first-param-derived payload with int/bool/float coercion to match WB UI semantics); subscribes per state_topic AND its per-control `meta/error` companion (`r`/`w`/`p` flags drive `state.reachable`); state mirror flows through `update_state` — no direct `self.state.x =` (chokepoint static guard verified). Loop guard: `enable_wb_emulation` defaults to **False** on `WbPassthroughDeviceConfig` so BaseDevice skips `_setup_wb_virtual_device` (no feedback loop). New `room: Optional[str]` field on `BaseDeviceConfig` (default `None`; single-room model — see A1). 15 driver-pattern tests; full suite 417 passed.
- [x] **VWB-2** — **DONE 2026-06-06.** `backend/config/devices/wb-devices/cabinet/cabinet_spots.json` (first config in the new `wb-devices/<room>/` subtree) declaring `capability_profile: "light_switch"`; new shared profile `backend/config/capabilities/profiles/light_switch.json` (canonical `power.on/off` → native `power_on/power_off`) — every relay-light in the house will reference it (`light_switch`); `backend/config/rooms.json` extended with the `cabinet` entry. `wb_passthrough` entry point registered with the venv. New `tests/unit/test_slice_cabinet_spots.py` (4 tests) + 2 loader tests in `test_capabilities.py` pin Pydantic parse, recursive scanner discovery, profile resolution through `load_capability_map`, AV-path-unchanged regression, and rooms.json shape. Full suite 423 pass.
- [x] **VWB-3** — **DONE 2026-06-06.** `POST /devices/{id}/canonical` endpoint in `presentation/api/routers/devices.py`; new DTOs in `presentation/api/schemas.py` (`CanonicalActionRequest`/`Response`/`Error`/`ErrorCode` enum). Resolves canonical→native through the device's capability map (class → profile → per-device override; uses the #14 mechanism). 6-code error enum with HTTP-status mirror. Synchronous with a 500 ms wait for the value-topic echo via a one-shot `register_state_change_callback`: synchronous AV calls satisfy it during `perform_action`; WB-passthrough echoes satisfy it as the MQTT subscription mirrors the value back. After the wait, checks `state.reachable` — surfaces `device_unreachable` when a per-control `meta/error` `r` flag flips it (A3). 10 endpoint tests pinning the 6 error codes + the two happy paths (sync + async echo) + the `param_map` rename. Full suite 433 pass.
- [x] **VWB-4** — **DONE 2026-06-06.** `device_name → names: {ru, en}` schema widening + one-shot migration of the 13 existing AV configs (Pydantic `LocalizedName` model in `domain/devices/config.py`; configs rewritten; runtime DTOs `BaseDeviceState.device_name` + `LayoutManifest.device_name` preserved as flat strings projected from `names.ru` so the UI surface is unchanged; UI's one config-side read fixed in `useDataSync.ts`). 401 backend tests pass; UI typecheck + lint clean.
- [x] **VWB-5** — **DONE 2026-06-06.** `GET /system/catalog` in `routers/system.py` with the builder in `presentation/api/catalog.py`. Flat capability-shaped projection of devices + rooms (no Layer-3 layout coupling). New DTOs in `schemas.py` (`CatalogRoom`/`CatalogDevice`/`CatalogCapability`/`CatalogAction`/`CatalogResponse`). Rooms come from `RoomManager.list()` verbatim (all locales); devices from `DeviceManager.devices` iteration with capabilities walked from each `CapabilityMap` (class → profile → per-device override; uses #14). Devices + rooms sorted by id before hashing → deterministic `version` (16-hex short SHA-256). Retained `bridge/catalog/version` MQTT topic bumped at the end of `reload_system_task` so Irene's catalog consumer refetches on real change. 9 builder + endpoint tests pinning shape, all-locales, null-room AV devices, version determinism, version-changes-on-content-change, version-independent-of-insertion-order, the live FastAPI route, and the 503 path. Full suite 442 pass.
- [x] **VWB-6** — **DONE 2026-06-06.** End-to-end physically validated against the live wb-mr6c at slave 51 / K4 in the cabinet. `POST /devices/cabinet_spots/canonical {capability:"power", action:"on"}` → publish `1` → relay clicked → value-topic echo received **5 ms later** → `update_state(mirrored={'power':'1'})` → callback chain fired → **200 OK** with post-state. Surfaced two latent bootstrap/MQTT-framework bugs along the way (subscription wiring; see today's journal). Irene ARCH-8 sign-off remains via the sister project once Irene is on the controller.
- [x] **VWB-7** — **DONE 2026-06-08.** Capability vocab profiles + driver enrichment (folded the former #20 "composition layer"). **6 shared profiles** in `config/capabilities/profiles/`: `dimmable_light`, `rgb_light`, `cover`, `heating_loop`, `hvac`, `sensor_room` (5 fields, motion dropped — no v1 voice use case). **Schema widening**: new `StateTopicSpec(topic, type, encoding?, values?, unit?)` in `infrastructure/config/models.py`; `WbPassthroughDeviceConfig.state_topics: Dict[str, StateTopicSpec]` with a `mode="before"` field_validator normalising bare-string form (slice's `cabinet_spots.json` unchanged); new optional `payload_template` on `WbPassthroughCommandConfig`. New `CapabilityField(name, type, encoding?, values?, unit?, labels?)` in `domain/capabilities/models.py`; `Capability.fields: List[CapabilityField]`; `_shape` validator widened to accept stateful + empty actions + non-empty fields (the pure-sensor shape). **Driver helpers** in `wb_passthrough/driver.py` (~70 LOC): `_compose_payload` calls `payload_template.format(**params)` for composite payloads; `_parse_value` scalar-coerces / enum-validates / template-inverses (`"R;G;B"` → `{r,g,b}` via the new module-level `_parse_template`); `_coerce_mirror` looks up the field's spec, parses, logs on failure (does NOT touch `error_flags` — that's WB-protocol-only). `WbPassthroughState.mirrored` widened to `Dict[str, Any]` so typed values land directly. **Catalog**: new `CatalogField` DTO in `schemas.py`; `_project_capability_actions` walks `cap.fields[]` and emits them with type/encoding/unit/labels. Version hash now bumps when a capability `fields[]` entry is added (sensors becoming visible re-triggers Irene's fetch). **Fix the FieldInfo footgun**: `Capability.fields = Field(default_factory=lambda: [], ...)` — `default_factory=list` would resolve to the FieldInfo of the existing `list` field above via class-body name shadow. **Tests**: 11 new (7 capability profile, 4 catalog), 9 new driver tests (parse template, RGB compose + inverse, scalar coerce, slice bare-string regression, parse-failure log path), 1 slice test pin update. **474 passed** (was 453). Hexagonal LAW clean (domain → no infra/presentation imports).
- [x] **VWB-8** — **DONE 2026-06-08.** `rooms.json` bootstrap — full sweep of the WB-UI dashboards from A2 findings (`entrance`, `hall`, `shower` for the WB `wc` dashboard per user's home semantics, `bathroom`, `bedroom`, `wardrobe`) plus the new **`global`** room for whole-house aggregate devices (#22). Existing rooms preserved: `living_room` (WB dashboard `livingroom`), `children_room` (WB dashboard `children`), `kitchen`, `cabinet` — kept their legacy symbolic ids per user direction; the WB-dashboard → bridge-room mapping is documented in each entry's `description` for the importer (a structured `wb_dashboard_id` field can land alongside the importer if needed). All 11 rooms now carry **trilingual `ru/en/de`** names (de added to the new rooms alongside the pre-slice rooms that already carried it). Authored by hand (11 entries is small enough; the full WB-config Python importer is deferred to #23 when device configs need it). 8 new tests in `test_rooms_bootstrap.py` pin: full 11-room set, every entry validates as `RoomDefinition`, every room has trilingual names, `global` starts empty, legacy room device memberships preserved, new rooms start empty, WB dashboard ids documented for the mapped rooms. Full suite **482 pass** (was 474).
- [x] **VWB-9** — **DONE 2026-06-08.** **Room-architecture refactor** (follow-up cleanup from #23 architectural inconsistency). Eliminated rooms.json `devices` duplication: now `device.config.room` (single source of truth) → `BaseDevice.room` (flat attr) → `DevicePort.get_room()` (domain contract) → `RoomManager` derives `room.devices` at load time from `DeviceManager`. **Phase A**: Backfilled `room` on all 13 AV configs (appletv_children → children_room, kitchen_hood → kitchen, others → living_room). **Phase B**: `RoomManager.reload()` now strips JSON-side `devices` arrays and populates `room.devices` from `DeviceManager` via `device.get_room()`; replaces the legacy `_validate_devices_exist` (rooms.json → DeviceManager direction) with a forward `_populate_devices_from_device_manager` (DeviceManager → rooms.json direction) that warns on orphan devices. **Phase C**: `RoomDefinition.devices` widened to `default_factory=list`; dropped 19 `devices` arrays from rooms.json (metadata-only entries now); replaced drift-guard `test_rooms_json_devices_match_wb_passthrough_configs` with forward-direction `test_every_device_config_declares_a_known_room`. **Phase D**: Added abstract `get_room() -> Optional[str]` to `DevicePort` (domain contract, hexagon-clean — no `device.config.room` reach from domain managers), `self.room` flat attr + `get_room()` impl on `BaseDevice` (mirrors the existing `get_id`/`get_name` pattern). **Phase E**: Activated the long-dormant `ScenarioDefinition.room_id` invariant — `ScenarioManager._validate_room_membership()` runs after `load_scenarios()`, walks each scenario's device union (`devices ∪ {source, display, audio} ∪ roles.values()`), asserts each device's `get_room()` matches the scenario's declared `room_id`. **Hard-fail** on mismatch (raises `ScenarioError`) — catches typos, stale references, config drift. All 9 existing scenarios pass cleanly (they all declare `room_id: "living_room"` and reference only living_room devices). Tests: 1 drift-guard removed, 1 forward-direction added, 2 new scenario validation tests, several mocks updated (MockDevice gained `get_room()`, integration test configs gained `room`). Full suite **486 passing** (was 485 at #23 close). Hexagon LAW clean (grep verified zero domain → infra/presentation imports).
- [x] **VWB-10** `[P0]` — **DONE 2026-07-04** (interactive global-room session; scope **redefined by the user at start**: not just the planned aggregates but the whole existing global fleet). **8 devices in `global`, 5 new profiles.** The planned aggregate: **`all_lights`** — fresh-convention inversion of the paste flow (nothing existed controller-side, so the bridge config *defines* the contract: wb-rules virtual device `all_lights`, switch control `power`; bridge writes 1/0 to `…/power/on`) **plus a drafted controller rule `wb-rules/all_lights.js`** (defineVirtualDevice + fan-out over all **36 true lights** = light_switch + dimmable_light configs across the 10 rooms, grouped by room; state = last-commanded; deployment/verification = user tech debt, per the original entry — but now drafted, not just owed). `all_blinds` NOT shipped — absent from the voice command set (only `all_lights` is referenced in `wb-mqtt-voice`). The existing fleet, via the classic paste flow (`docs/wb_device_authoring_log.md` §6 has the full session record): **`all_plugs`** + **`oven_power`** (light_switch profile reused — generic power-switch shape), **`cleaning_mode`** (leakage_settings: momentary УБОРКА + `arm_delay` minutes; first would-be per-device map, corrected to a singleton profile — **rule: passthrough capabilities are ALWAYS profiles**), **`heating_control`** (wb-mwac_46: collector-branch valves ×2 + writable alarm + 5 read-only circuit indicators as a fields-only domain), **`water_supply`** (wb-mwac_54: cold/hot valves + alarm + 3 leak zones), **`seasonal_mode`** (`heating_season`/`cooling_season` — deliberately NOT bare `heating`, keeps voice vocabulary unambiguous vs per-room heating_loops), **`home_mode`** (`presence.home/away` — flagged: highest-consequence future voice command, wants Irene-side confirm-before-execute). All domains `reconcile: false`, voice labels authored, aliases deferred with the voice feature. Known gaps recorded in the authoring log (arm_delay bounds unread; F-input naming semantic; oven_power room placement). Suite 488 green after every device.
- [x] **VWB-11** — **DONE 2026-06-08.** Bulk device configs — **57 WB-passthrough device configs** across all 10 physical rooms, authored interactively from WB-UI widget JSONs. Per-profile distribution: `light_switch` × 23, `dimmable_light` × 13, `heating_loop` × 9, `cover` × 8, `hvac` × 3, `sensor_room` × 1 (the sauna's wb-msw2_100 pack). Per-room counts: bedroom 11, living_room 11, cabinet 6, children_room 6, shower 6, bathroom 5, kitchen 4, hall 3, entrance 3, wardrobe 2. Three Mitsubishi HVAC configs flagged for `ESP32ManagedDevice` migration when that class is introduced. Most multi-sensor onboarding (`wb-msw-v3_*`) deferred to a future global-room session for firmware-doc review; the sauna's wb-msw2_100 (2 fields only) included opportunistically. **Profile changes accumulated during authoring**: `cover.stop` dropped (no native WB control), `hvac` profile rewritten end-to-end against sister-firmware `mitsubishi2wb` to drop fictional enum fields + add `set_widevane`, `heating_loop.mode` dropped from `fields[]` to mirror `light_switch` pattern. **Catalog enhancement (§P3.7 #19 follow-up)**: `_project_capability_actions` gained `mirrored_field_names: Optional[set[str]]` filter so a device using a profile with N fields but mirroring only K<N of them surfaces only K in the catalog (triggered by the sauna's partial sensor_room mirror). **rooms.json drift-guard test added** that walks every WB-passthrough config and asserts its `device_id` appears in the correct room's `devices` list — caught a silent 19-device drift mid-#23 and now prevents recurrence. **Cabinet roller cover semantic fix**: `dooya_dm35eq_x_*` motors invert position (0=open, 100=closed); open/close action values swapped accordingly. **Subfolder convention correction**: `wb-devices/<room>/` uses bridge room_id (matches rooms.json), NOT the WB-UI dashboard id; action_plan A1 paragraph rewritten mid-session 2 to reflect this. **Live authoring log** at `docs/wb_device_authoring_log.md` captures every per-device decision, accumulated cross-room rules (14 entries), friction observations (7 entries), and automation opportunities (9 entries) — input for any future packaged version of this onboarding flow. Full suite **485 passing** (was 482 at #21 close). Two rooms remain conceptually unfinished: `global` (waiting on #22 aggregates) and most rooms' multi-sensors (waiting on focused WB-MSW firmware review).
- [x] **VWB-13** `[P1]` — **DONE 2026-07-09** (closed on the user's spot-check evidence; the sweep's two halves reconciled against live controller reality per `task-start-reconciliation`, user-accepted "close as done now"). **Catalog completeness sweep + bulk end-to-end verification across rooms — done on the deployed WB7 bridge.** Half one (**catalog completeness**) was proven at the REL-2 cutover 2026-07-08: the controller's live `/system/catalog` was a **byte-match** against the committed golden `8159b4b0068d1c63` (79 devices / 11 rooms — including all **8 `global` aggregates** `all_lights`/`heating_control`/`oven_power`/`all_plugs`/`water_supply`/`seasonal_mode`/`cleaning_mode`/`home_mode` and `scenario_manager_living_room`), so every device is enumerated and every canonical action resolvable with zero deployment drift. Half two (**bulk end-to-end across rooms**) rode the user's controller spot-checks 2026-07-08–09: real room devices exercised through the deployed bridge, behaving **identically to the dev-box run** (same binary, same config tree — REL-2 removed the only variable). The 8 `global` aggregates accepted as covered-enough by the user (the task bar is only that the canonical call *publishes to the broker* — several still owe their wb-rules backing, and the wired ones actuate the house, so an exhaustive fire-all was deliberately not run). No code touched — a verification task. Board effect: **REL-3 is now unblocked** (its last non-done prerequisite).
- [x] **VWB-14** — **DONE 2026-06-09.** Value-label translation layer for enum-encoded WB controls. **`ValueLabel`** model (wire/canonical/labels) added to `domain/devices/config.py` next to `LocalizedName`; `CapabilityField.values` and `StateTopicSpec.values` widened to `Optional[List[ValueLabel]]` with `mode="before"` validators that normalise bare `["a","b"]` to `[{wire:"a", canonical:"a"}]` — no existing profile / config / test needed migration. **Driver** (`wb_passthrough/driver.py`) gained `_translate_outbound` (canonical → wire) + `_translate_inbound` (wire → canonical) helpers mirroring the `_apply_inversion`/`_invert_wire_payload` shape; threaded into `_publish_command` (idempotency normalises target into canonical space before compare; final transforms run translation then inversion in that order) + `_coerce_mirror` (state.mirrored always canonical for enum-with-table fields); `_parse_value` enum branch fixed to accept either wire OR canonical (previous `raw in spec.values` silently broke when `values` shape changed). **Catalog** projection emits `CatalogValueLabel` triplet per entry; version-hash bumps on label-table changes. **HVAC profile** + 3 Mitsubishi configs (`bedroom/living_room/children_room`) gained full value tables with firmware wire vocabulary (AUTO/DRY/COOL/HEAT/FAN; AUTO/QUIET/1-4; AUTO/SWING/1-5; SWING/<<<<>>) from sister-repo `mitsubishi2wb` `html_pages.h` L137-179, trilingual ru/en/de labels. Command param types changed from `range` (0-N int placeholder) to `string` since canonical params are now identifiers. Drift-guard test walks the 3 configs vs the profile and fails on any wire/canonical pair mismatch. **Heating_loop.mode left as-is** — the "optionally restore" qualifier; type=bool/invert=true already works and a 2-value bool field doesn't benefit from per-value labels enough to justify the busywork. **UI** — new `HvacPanel.tsx` (~190 LOC) under `pages/appliances/` with the firmware's exact Unicode glyphs inline (♻ AUTO, 💧 DRY, ❄️ COOL, ☀️ HEAT, ❃ FAN, ⚟ SWING, ➟ POS-N + literal widevane symbols); reads value tables from `/system/catalog` and posts canonical via new `useExecuteCanonicalAction` hook (next to new `useSystemCatalog`). Same generic panel services all 3 HVAC instances; React-router device_id param selects the catalog entry. Settings store carries en/ru today; panel falls back en→canonical so de strings (in the catalog) survive a future language-picker extension. **Shipped across 5 commits:** `bb8cca4` (schema + catalog DTOs + 8 tests), `c6c8f67` (uv.lock sync), `1c55007` (driver + 8 tests), `ebc5a07` (HVAC profile + configs + 3 tests + drift-guard), `05371c2` (UI HvacPanel + hooks). Suite **495 pass** in subset excluding the pre-existing `test_layout_manifest.py` stale-oracle-path collection error (unrelated to #26 — same error on the pre-#26 commit `5212809`). Hexagonal LAW preserved at every commit; import-linter 3 contracts kept; pyright 0 errors; AST gate clean. UI `check` + `build` clean. **HW verification deferred** — next rack session fires a canonical `set_mode(cool)` through the panel and confirms wire-level `COOL` publish + echo + state mirror lands correctly.
- [x] **VWB-15** `[P1]` `[house]` — **DONE 2026-07-04** (final link of the pre-catalog chain SCN-6 → VWB-17 → SCN-7 → **VWB-15**; unblocks `wb-mqtt-voice` ARCH-26 / **TEST-17**). **The Irene↔bridge contract artifacts, committed at repo-root `contracts/`:** `catalog.golden.json` (the full house, 79 devices / 11 rooms — incl. the 8 `global` aggregates and `scenario_manager_living_room` with its 9-scenario enum), `openapi.json` (pinned schema of record, byte-identical to the UI-consumed `backend/openapi.json`), `STAMP.json` (bridge commit + version + catalog content-hash — the commit stamp tracks the code build, the content-hash tracks config drift; the stamped commit is the build the artifacts were generated FROM, i.e. the landing commit's parent), plus `contracts/README.md` (consumption rules, one-way outward sync, regeneration commands). **New `wb-catalog` CLI** (`cli/dump_catalog.py`, sibling of `wb-openapi` per the reuse note): builds the catalog **OFFLINE and deterministically** — typed configs + capability maps + rooms + scenario definitions via lightweight stand-ins, no drivers/network/broker, devices sorted by id (verified byte-identical across runs); `--stamp` writes the build stamp. **`CatalogAction.params` FILLED** — the §6 canonical-view projection (`param_projection.project_action_params`) wired into the catalog builder, discharging the owed §P3.7 #19 introspection stub; live examples in the golden: hood `fan.set {level: range 0–4}`, HVAC `climate.set_setpoint {temp: float 16–31 °C}`, eMotiva `volume.set {level: range −96…0 dB}` — canonical names via reversed `param_map`, constraints from the native specs, capability-fixed params excluded. **Drift guard = a test** (`test_contracts_golden.py`, self-contained in the backend CI job): regenerates golden + openapi in-test and compares to the committed copies (both `backend/openapi.json` and the `contracts/` pin), plus stamp↔golden hash coherence; regen hint in the failure message; `contracts/**` added to the CI backend path filter so a hand-edit of the artifacts alone triggers the guard. Validated against a LIVE bridge run same-day (smoke session: the runtime `/system/catalog` matches the offline projection modulo the params landing after). **OWED TAIL — the real WB7 dump:** the deployment-drift realism check (`curl http://<wb7>:8000/system/catalog` vs the golden) waits on the `ops/` compose cutover to the controller (no bridge runs there yet — dev box served the house as of 2026-05-30); recorded in `contracts/README.md` §Realism check. Suite **518 passing** (3 new drift-guard tests); pyright 0; contracts 3/3. **Cross-repo next step (theirs):** `wb-mqtt-voice` TEST-17 pins `contracts/` into `eval-commons` and builds `DeviceCatalog` parsing against the golden; VWB-16 (this repo) then drives the crossover fixtures against the same artifact.
- [x] **VWB-17** `[P2]` — **DONE 2026-07-04** (second link of the pre-catalog chain SCN-6 → **VWB-17** → SCN-7 → VWB-15; gate for SCN-7). **Sequence-form actions route through the canonical seam.** Reconciliation finding at start: the old router docstring's claim that "the reconciler path handles those" was FALSE — nothing executed `CapabilityAction.sequence` anywhere (only the exposure walks read it), so this task introduced sequence *execution* for the first time. **Implementation:** (1) **`CapabilityAction.expand(params) → List[NativeStep]`** on the domain model — THE single canonical→native translation point, shared by the REST endpoint, the Scenario Manager proxy, and any future dispatcher: command form → one step (param_map rename + fixed-params overlay), sequence form → recursive flatten, **each step applying its own param_map/params to the same incoming params**; new `NativeStep` model. (2) New **`delay_after_ms`** field on `CapabilityAction` (default 0; additive schema) — inter-step gaps for IR macros, honored between steps (ignored after the last). (3) **Canonical endpoint**: the sequence-500 branch replaced by a unified step loop (single command = one-step sequence — one code path); mid-sequence failure surfaces `step i/N (<command>)` in the error; the `no_op` echo short-circuit restricted to single-step actions (unchanged behavior); echo-wait semantics preserved (waiter registered before step 1, awaited after the last). (4) **`ScenarioProxy.execute`** rides the same expansion (its sequence rejection removed) — WB-card pushbuttons backed by sequences now work. **Contract:** `openapi.json` verified byte-unchanged (capability models are config-side, not API DTOs). **Tests:** 7 new in `tests/unit/test_canonical_sequence.py` (expand single/sequence/nested + per-step translation; route order + step-failure naming + single-command no_op regression; proxy sequence execution). Suite **509 passing**; contracts 3/3; **pyright 0 errors** (run locally this time — the new gate discipline). Ledger ripples: VWB-16's sequence-form caveat marked RESOLVED; SCN-7's gate marked SATISFIED. Zero shipped maps use sequences yet — this is the insurance SCN-7 required before the UI rides canonical.

- [x] **VWB-18** `[P1]` `[house]` — **DONE 2026-07-02.** **Restart-durability triad** (filed from wb-mqtt-voice's QUAL-56 durability review, `wb-mqtt-voice/docs/review/faf_durable_execution_review.md` Part 2/F7; all three claims verified against live code at intake per `cross-repo-source-of-truth`, plus one aggravation found: `initialize_devices()` persisted boot-default state *before* the restore stub ever ran, clobbering the last-good snapshot at every boot). **(1) Stale-intent resurrection FIXED** (`5d60999`): `deactivate()` now deletes the persisted `active_scenario` key in its `finally` block, atomically with the in-memory clear — previously `StateRepositoryPort.delete` had zero call sites and an explicit "turn it all off" resurrected on the next bridge start, powering the AV gear back on. `deactivate()` only: process `shutdown()` deliberately keeps the key (still-active scenario survives a restart); a test locks the asymmetry in. **(2) Persist-without-restore rot FIXED** (`6b1e9d8`) — decision: *implement restore* (not re-scope the doc; `shutdown()`'s contract already promised assumed-state continuity). New `DevicePort.restore_state(snapshot)` seam; `BaseDevice` applies only fields the concrete state class declares (older-schema snapshots degrade), never restores identity/`last_command`/stale `error`, rides the `update_state` chokepoint; `DeviceManager` re-hydrates per device inside `initialize_devices()` **before** `setup()` (live sources win over the snapshot; the post-setup persist now writes restored state back instead of defaults); the old `initialize()` stub + bootstrap call removed; corrupt snapshots degrade to defaults without blocking device init. **(3) Toggle-power inversion CLOSED by (2):** assumed state surviving the restart makes the reconciler power-on diff a no-op for a blind toggle device that was ON (pre-fix the reset-to-`off` state emitted a toggle that turned the live device OFF); regression test drives mf_amplifier's real capability map. Tests per the QUAL-56 rule (persist + restore + restart test together): 2 scenario restart round trips + delete assertions, 6 device-restore tests (`tests/unit/test_device_state_restore.py`). Suite **502 passing** (was 495); import-linter 3 contracts kept. Docs: architecture overview + devices-and-scenarios + scenario redesign §7.1 (which previously promised persistence that didn't round-trip) now describe restore-at-startup and deactivate-clears-intent.

- [x] **VWB-19** `[P2]` — **DONE 2026-07-05** (pulled forward from `[later]` by user decision, off the chat analysis "which task enables app launch / input selection through canonical?"; design recorded as **`canonical_first.md` §11** in the same change — the task was design-sketched at filing, the addendum froze the decisions). **Select-form capabilities route through the canonical endpoint: `set` is the reserved canonical action.** Reconciliation sharpened the scope first: **app launch needed NOTHING** — `apps.launch` was always an ordinary action (`launch_app` + param_map) and routable since day one; only *input selection* lived in `select` and was unreachable. **Domain:** `CapabilitySelect.expand(value) → List[NativeStep]` — the single select-resolution site, mirror of VWB-17's `CapabilityAction.expand()`: by_value → the value's own action expanded (unknown value raises with the valid set named), parametric → param_map rename + fixed-params overlay; `CapabilitySelect.option_values()` (by_value keys in declaration order, None for parametric); the reconciler's private `_input_action` resolution REPLACED by the shared method (scenario activation and canonical dispatch can no longer drift). **Dispatcher:** `dispatch_device_canonical` resolves `set` through `cap.select` when no authored `set` action exists (authored wins — profile-level override stays possible); missing `params.value` → speakable `400 param_invalid (field=value, required)`, unknown by_value value → `400 param_invalid (unknown_value)` naming the valid set; everything downstream (echo wait, `wait:false`, no_op, room fan-out) inherited via the VWB-23 shared core. **Fleet fact that shaped it:** parametric selects (LG ×2, eMotiva, Auralic) ALL carry a `list` query; by_value selects (mf_amplifier ×7 values, upscaler ×2) NEVER do — so `GET /devices/{id}/options/inputs` gained a **static fallback serving the by_value keys** (same result envelope; 404'd for the amp before), and the catalog advertises `set {value}` with **static `values`** for by_value (wire=canonical=key; Irene validates «переключи на CD» with zero round-trips) vs **`options_from: "inputs"`** for parametric (the VWB-20/G5 open-set pattern). The VWB-20-suppressed TV `input` husk is REAL again (suppression guard stays for future husk shapes). **Contract:** `openapi.json` byte-unchanged (no endpoint/schema churn — verified); golden re-dump `dbfd2855dac52026` + STAMP (pre-pin again — voice pins the then-current `contracts/`); UI types regen no-op, check + build green. **Tests:** new `test_select_canonical.py` (16: expand parametric rename/overlay/default-param, by_value hit/miss-naming-valid-set, option_values order, endpoint set parametric/by_value/unknown/missing-value/no-select-404, authored-set precedence, options fallback + 404-when-neither, catalog static-values/options_from/husk-unsuppressed/no-duplicate). Suite **565**; pyright 0; contracts 3/3. **Split off:** **UI-9** — flip the Layer-3 dropdown seam (manifest + UI) from native `set_action`/`set_param` to canonical dispatch; the dropdowns are the last first-party write consumer of `/action`, so UI-9 gates the §8 phase-3 demotion decision. Voice can now switch inputs the moment a command set wants it.

- [x] **VWB-20** `[P1]` `[house]` — **DONE 2026-07-04** (contract patch v1.1, landed **PRE-PIN** — hours after the voice-side review, before TEST-17 pinned; the cheapest shape-change window). All five gaps + the minor flag: **(G1)** typed **`CatalogParam`** (name/type/required/default/min/max/description/`unit`/`values`/`options_from`) replaces the schema-free `List[Dict]`; BOTH producers unified (the §6 projection now emits the full shape incl. unit; the hand-built `scenario.set` constructs `CatalogParam` explicitly with `required=True`) — Irene gets a contract-backed parser, the drift guard gets pydantic teeth. **(G3)** `ScenarioDefinition.names: Optional[LocalizedName]` (legacy flat `name` = en fallback); **ru names auto-translated for all 9 scenarios** («Кино с Apple TV», «ТВ через колонки», … — user corrects wording at leisure, the drift guard forces the re-dump); the scenario enum's value labels now carry ru+en. **(G4, root-caused deeper than the review)**: `CommandParameterDefinition` had **no `units` field at all** — authored units were silently dropped at typed-config parse (the WB service's `getattr(…,'units')` was always None). Field added; **28 params authored** across configs (setpoints `°C` ×9, brightness `%` ×13, eMotiva volume `dB`, cleaning `min`); `_spec_dict` projects `unit`; bonus: the eMotiva's WB `set_volume` control meta now correctly carries `units: dB` (oracle re-captured, single-line diff verified). **(G5, remedy corrected)**: apps launch params are an intentionally OPEN set — `options_from: "apps"` hint pointing at SCN-7's `GET /devices/{id}/options/apps` (a static enum would drift on every install); pattern documented in `contracts/README.md` §Param semantics. **(G2 schema half, as filed)**: optional `aliases: {locale: [surfaces]}` shipped on `BaseDeviceConfig`/`RoomDefinition` (config side) and `CatalogDevice`/`CatalogRoom` (contract side), projected through when authored — `null` everywhere until **VWB-21** supplies the household vocabulary; the shape is pinned once, pre-pin. **(minor)**: empty capability husks suppressed from the catalog (TVs' select-form `input` — reappears with VWB-19; locked by test). **4 new contract-semantics tests** in `test_contracts_golden.py` (units, ru labels, options_from, no-husks) on top of the drift trio; suite **522 passing**; pyright 0; contracts 3/3; golden + STAMP + openapi + contracts-pin + UI types all regenerated; UI check + build green. **Voice side may now pin** (TEST-17 go).
- ~~**VWB-7**~~ ← #20 — ~~Composition layer above the driver~~ — **folded into #19** (DONE 2026-06-08; driver-side helpers + typed `state_topics` schema cover the RGB/HVAC/sensor cases without a separate adapter layer).

- [x] **VWB-21** `[P1]` `[house]` — **DONE 2026-07-04** (interactive alias-vocabulary session, 5 rounds — the G2 flesh on VWB-20's schema). **3 rooms + 34 device alias entries authored** from the user's actual household vocabulary. Session shape improved over the paste flow: the assistant presented the inventory room-by-room WITH candidate synonyms to react to — confirm/correct/extend — and the user answered in terse positional lists. Rooms: **зал** (гостиная), **сынарник** (детская), **квартира** (весь дом). Devices: телек/кондей/радиаторы/эппл replicated per room; усилок, катушечник, видик, сервант, фартук, жалюзи, треки, полки, тумбочки, **ночники** (= the sconces — the user corrected the assistant's nightstand guess), шторы, пол (generalized to all 5 heated floors on explicit confirmation). **Collectives deliberately on all members** (шторы × 2 curtains, жалюзи × 2 rollers, треки × 2 tracks) — one surface resolving to several devices is Irene's disambiguation/fan-out concern; the catalog records the truth. **Open tails for a later iteration** (recorded in the authoring log §6): bedroom «шторы» placed on the curtain only (not tulle — mirrors the living room); гардеробная + the global masters got nothing (candidate «свет»/«духовка»/«вода» unconfirmed). Golden re-dumped (`7a1149c7657a2f7f`) + STAMP; drift guard green; suite 522; contracts 3/3. The catalog now carries the full VWB-15-promised "names + aliases" — QUAL-35's recall concern has real surfaces to match against.

- [x] **VWB-22** `[P1]` — **DONE 2026-07-05** (filed + executed same day; discussion session off the voice side's open question "what should «включи свет» / «закрой шторы» do?"). **Design: room-scoped group addressing — `canonical_first.md` §10** (addendum; also §1 item 6 + §8 table row). Decisions: (1) a **third canonical address form** `POST /rooms/{room_id}/canonical {group, action, params?, scope, wait?}` — the resolver resolves only as deep as the utterance specifies (named device → form 1; in-scenario relative → form 2 proxy; bare capability noun → form 3), actuation policy lives in the bridge; (2) **scope** `auto` (room default else fan-out) / `all` (forced fan-out — preserves the plural/«весь» signal Irene alone hears) / `one` (default required, speakable 409); (3) **membership = `group` overlay on capabilities**, defaulting to the domain name, overridable per capability — needed because the 36 shipped light switches declare domain `power` (only the hood has `light`), so domain-as-membership would sweep sockets/oven into «свет»; the 3 illumination profiles tag `group: "light"`, hood matches implicitly; (4) **re-profiling `power→light` REJECTED** (reconciler power management, layout power zone, WB service all key on `power`; overlay is forward-compatible with a later migration), per-room master-switch convention REJECTED (drift, undefined state), curated `groups.json` REJECTED (per-capability `group: null` opt-out suffices); (5) room-level **`group_defaults`** in `rooms.json` (the singular «включи свет» → main light); (6) **aggregate honest response** `{scope_applied, results[]}` with `skipped`/`failed` per member + speakable `404 no_group_members`; (7) **fan-out allow-list `light`+`cover` only** — consequential groups refuse fan-out (the fridge/NAS rail; travel case stays with `at_home`). Contract impact additive; interim = voice-side fan-out over the existing catalog. Per `design-then-implement`: implementation filed as **VWB-23** (open, [P1]).

- [x] **VWB-23** `[P1]` — **DONE 2026-07-05** (same day as its design VWB-22). **Room-scoped group addressing — `canonical_first.md` §10 implemented end-to-end.** **Domain:** `Capability.group` (optional; explicit-`null` opt-out distinguished via `model_fields_set`) + `Capability.effective_group(domain)` (omitted → domain name); new pure module `domain/rooms/groups.py` (`resolve_members` — room × group overlay membership, deterministic order; `FANOUT_ALLOWED_GROUPS = {light, cover}`); `RoomDefinition.group_defaults` + `RoomManager._validate_group_defaults()` at load (invalid entries error-logged + dropped → degrade to fan-out/409, never misfire). **Endpoint:** `POST /rooms/{room_id}/canonical` `{group, action, params?, scope: auto|all|one, wait?}` — scope policy per §10.1; members execute **concurrently** through the extracted `dispatch_device_canonical(...)` core (the device endpoint's own path — echo wait, `no_op` short-circuit, `update_state` chokepoint all identical); per-member statuses `executed|no_op|skipped|failed` (`action_not_supported` → skipped, never an error; failures per-member, 200 with `success = any executed/no_op`); speakable `404 no_group_members`, `409 no_default_device`, `409 fanout_not_allowed` (fan-out refused for non-allow-listed groups REGARDLESS of member count; a configured default is a named choice and always allowed). `CanonicalActionResponse` gained the additive `no_op` flag (set by the existing short-circuit). **Config:** `power_switch` profile split out of `light_switch` (identical shape, group defaults to `power`), `global/oven_power` + `global/all_plugs` re-pointed (the other 23 `light_switch` users audited — all genuine illumination); `light_switch`/`dimmable_light`/`rgb_light` tagged `group: "light"` on `power`; **`group_defaults` authored (user decision): every room except `global` defaults `light` to its `<room>_spots`** (naming perfectly regular, 10/10 rooms); `global` none — its lights group is exactly the `all_lights` master, fan-out reaches it. **Catalog (additive):** `CatalogCapability.group` always-explicit (consumers never reimplement the defaulting rule; scenario-manager entities emit it too), `CatalogRoom.group_defaults`; `RoomDefinitionResponse` + bootstrap wiring (`rooms.initialize` now takes `device_manager`). **Contract:** openapi regen + pin, golden re-dump `91909b54bfb4b593` + STAMP (pre-pin — v1 carries the group surface); UI types regen, `npm run check` + build green. **Tests:** new `test_room_canonical.py` (17: overlay default/override/opt-out, membership scoping, all scope policies, allow-list rail incl. default-for-power allowed, skipped/failed/no_op statuses, catalog projection, RoomManager validation). Suite 548; pyright 0; contracts 3/3. **Docs:** `interfaces.md` endpoint row; `rooms.md` voice-scope flow rewritten to the shipped bridge-side resolution (the old text described a never-built client-side `default` lookup) + stale "aggregates not yet authored" corrected.

- [x] **VWB-24** `[P2]` — **DONE 2026-07-05** (voice-side intake — QUAL-35 Slice 2 — filed uncommitted the same day, **verified at intake per `cross-repo-source-of-truth`, accepted and implemented in one session**; row reformatted to this ledger's tag vocabulary on acceptance). **HVAC `set_mode`/`set_fan`/`set_vane`/`set_widevane` action params typed with the field value tables (contract v1.3).** Intake verification confirmed every claim and found the root cause one level deeper (the VWB-20/G4 shape again): `CommandParameterDefinition` has **no `values` field** — the native spec cannot carry an enum table, so the §6 projection had nothing to project; meanwhile the read-side `mode`/`fan`/`vane`/`widevane` FIELDS carry full ru/en/de triplets (VWB-14) and the driver already translates canonical→wire outbound — actuation always worked, only catalog metadata was missing. Full-golden sweep sized the disease at exactly the climate quartet ×3 HVACs (nothing else has an untyped closed-set string param). **Fix = derive, don't author twice** (user decision: the param-rename approach over a `values_from` hint): (1) catalog projection derives a param's `values` from the **same-named enum field's table** (attached before the `options_from` hint pass, which then skips them); (2) the hvac profile's `param_map` renames the canonical params to the field names — `{fan: speed}`, `{vane: angle}`, `{widevane: direction}` (`mode` already matched; natives keep the firmware vocabulary; dispatch pass-through keeps old native names working) — the rename is contract-visible but **free**: the voice side deliberately had not wired these params (the very reason they filed the task); (3) vane/widevane typed for free by the same rule (voice won't wire them, but the catalog now tells the truth). **UI:** `HvacPanel`'s `FIELD_TO_PARAM` indirection collapsed to identity (param name = field name is now a contract invariant, noted in-code); check + build green. **Contract:** golden `a17a63b0c47fdb53` + STAMP (pre-pin again); `openapi.json` byte-unchanged; `contracts/README.md` values-bullet extended (no task IDs — the scrub rule). **Tests:** 2 derivation tests in `test_system_catalog.py` (same-name derivation incl. renamed `fan`, no-match stays untyped), 1 contract-semantics test in `test_contracts_golden.py` (all 3 HVACs × 4 actions mirror their field tables; «охлаждение» present), `test_capabilities.py` param-map assertions updated to the new renames. Suite **568**; pyright 0; contracts 3/3. **Consumer:** wb-mqtt-voice re-pins and wires `set_mode` — «кондиционер на охлаждение» now has catalog truth to validate against.

- [x] **VWB-25** `[P2]` — **DONE 2026-07-06** (filed + executed in the REL-1 sign-off session; the confirmed VWB-21 alias tail). **Wardrobe gains «свет»** — `wardrobe_spots` (the room's main light, most-specific-thing rule) now carries `aliases.ru: ["свет"]`; Гардеробная was the one alias-free room after VWB-21 (the candidate was never confirmed — the user confirmed it at the session). The other tails resolved WITHOUT change: bedroom «шторы» stays curtain-only (living-room pattern confirmed correct); global-master aliases («свет» all_lights / «духовка» oven_power / «вода» water_supply) all **deferred to release 2** — whole-house voice vocabulary waits for real usage + Irene-side confirmation UX (home_mode's confirm-before-execute flag stands). Golden re-dumped `acc1e18beb3f204a` + STAMP (pre-pin; voice picks it up at its next pin).

- [x] **VWB-26** `[P2]` — **DONE 2026-07-06 (pulled forward from `[deferred]` by the user, hours after voice BUILD-12 unblocked it).** **Problem-reporting participation (bridge side)** — both halves shipped: **(1) lens-file co-ownership review** — `.github/claude/lens-bridge.md` in `wb-user-reports` verified against this repo and corrected (its §2 Reproduce named nonexistent test paths — `tests/test_capabilities.py` for what is `backend/tests/unit/...` — and missed the `backend/` cwd, the `uv sync --extra dev` gotcha, the pyright/lint-imports/UI gates, the contract-regen rule, and the never-live-repro warning; all fixed in a commit on the reports repo — the sections that were right stayed: dedup, key bridge questions, outcomes/labels, ping-pong guard, leak fence, no-ledger-edits-in-triage-PRs). **(2) `/inbox` skill + session-start invariant** — `.claude/skills/inbox/SKILL.md` (mirror of voice ARCH-33 adapted to `lens:bridge`: queue from the reports repo as source of truth, one-item-at-a-time walk, "verify the finding independently — never trust the triage" with this repo's gates spelled out, merge-path does the OWNER's ledger half per `every-task-in-the-ledger` + the discipline triad, never-live-repro note); CLAUDE.md gained the `problem-report-inbox` invariant (non-blocking session-start `gh` check, one-line mention + offer `/inbox`, silent skip on gh failure). **Verified live:** both bridge-lens queue queries answer (empty — ticket #2/PR #1 are voice-lens) against the private repo on the owner's `gh` auth. No backend/UI code touched.

- [x] **VWB-27** `[P2]` — **DONE 2026-07-06 (design task — deliverable is the design document, per `design-then-implement`; pulled forward from `[deferred]` by the user: "while we're waiting to get unblocked — let's discuss").** **UI "Report a problem" — bridge-side bundle design** → `docs/design/problem_reports_bridge.md` (AGREED, B-1..B-10). Split off VWB-26 at intake hours earlier; the shared pieces (private `wb-user-reports`, envelope, lenses) come from the voice ARCH-30 design unchanged — this doc decides only the bridge's own surface. **Decisions:** B-1 scope = page-context details (entity + topology neighbors: configs, capability maps, persisted-vs-live diffs) + ALL live states + today's backend logs; B-2 three ring families in v1 — backend dispatch ring (the "what did the system just do" narrative, `execute_action` chokepoint) + filtered/deduped MQTT window + browser buffers; B-3 ONE trigger (the UI button; fully automatic collection behind `POST /reports`; the owner-CLI attach-to-ticket trigger explicitly out of v1 — its ARCH-30 handover-evidence gap recorded in §8); B-4 browser set = the EXISTING `useLogStore` action log (found already wired at every dispatch site) + console/crash buffer + API ring + SSE health + app context, screenshots excluded; B-5 redaction mirror (broker password in `system.json` = the hot item); B-6 D-7-mirror rate limits; B-7 `data/reports/` spool (no durable substrate needed); B-8 endpoint + controller-env PAT; B-9 ring tunables; B-10 hexagonal placement (`ReportSinkPort` in domain). **Implementation filed as VWB-28** (`[deferred]`, blocked on voice BUILD-12). No code touched.

- [x] **VWB-28** `[P2]` — **DONE 2026-07-06 (pulled forward from `[deferred]` by the user, same day the design closed).** **UI "Report a problem" — implemented end-to-end per B-1..B-12** (`docs/design/problem_reports_bridge.md`). **Domain** (`domain/reports/`): `DispatchRing` (depth 50, hooked at the `execute_action` chokepoint via a record-and-return wrapper — every source, every outcome, exceptions included) + `MqttWindow` (`/devices/#`-filtered, per-(direction,topic) last-value dedup, 60 s/500-msg caps, fed by a new `traffic_observer` seam on the MQTT client) + `redaction` (B-5: credential-shaped keys masked, containers recurse so an `auth` dict keeps its username; text-form masks to end-of-line — both shapes bug-caught by tests) + `ReportService` (B-1 scoping = anchor + topology neighbors; collector assembles all live states, scoped persisted-vs-live diffs, active scenarios + manual steps, scoped configs, redacted system config, gzipped+b64 logs, both rings; B-6 rate limit 3/h/10/d; §5 filing: `[bridge-ui]` title, distilled issue body, tar.gz bundle of evidence+context+ui_evidence). Cross-layer inputs (persisted state, system config, catalog version) injected as callables — domain stays import-pure. **Port/adapter:** `ReportSinkPort` in `domain/ports.py`; `GitHubReportSink` (contents+issues API, PAT from env named in config, never in config itself) with the B-7 spool built in (`data/reports/`, retried at startup + hourly by a bootstrap task). **Endpoints:** `POST /reports` (gated by `reports.enabled`, 429 on rate limit, 503 when disabled) + **`GET /reports/evidence`** (B-11 — always available; `EvidenceEnvelope` = the owned contract surface, now in `openapi.json` → `contracts/` pin, **contract v1.4**). **Config:** `ReportsConfig` on `SystemConfig` (+ response DTO; canonical `system.json` gains `"reports": {"enabled": false}` — filing is opt-in, evidence/rings always on). **UI:** B-12 navbar bug button (`BugReport`, far right, muted→amber hover, «Сообщить о проблеме») + minimal dialog (free text, send/cancel, in-dialog confirmation with report id / spooled / rate-limited variants — no toast infra exists, same §2 role) + B-4 browser evidence (`lib/reportEvidence.ts`: console/crash taps installed at app start, axios interceptor API ring, SSE health fed from `useEventSource`, `useLogStore` action log + app context, collected only at send). **Tests:** 12 new in `test_reports.py` (rings bounded/dedup/filtered; redaction both shapes; collector scoping/diff/redaction/logs; filing envelope + bundle round-trip + rate limit + disabled; spool temp-dir e2e with b64 round-trip; endpoint 200/429/503 + evidence-always-on). Suite **583**, pyright 0, import-linter 3/3, UI check + build green; openapi 34 paths (+2), golden byte-unchanged. Docs: `interfaces.md` §Problem reports; `contracts/README.md` v1.4 sentence. **Activation owed to the rack:** set the PAT env on the controller + flip `reports.enabled` at/after REL-2 (noted; not a release gate).
- [x] **VWB-29** `[P1]` — **DONE 2026-07-12** (RESCOPED same day by council HK-5 / PROD-16 bridge delegation (1), intake `17734d8` — the 2026-07-08 GitHub-Release + bare-`contract-vN` intake text superseded by the org contract convention, normative `../locveil-commons/process/contracts.md`). **The catalog contract's owner-side cut: `contracts/catalog/` + `CONTRACT_VERSION` + STAMP core + first family tag `catalog-v1.5`.** **(a) Layout:** the four artifacts (`catalog.golden.json`, `openapi.json`, `STAMP.json`, the contract README) moved to the uniform owned surface `contracts/catalog/`; a direction-labeled **registry README** now sits at `contracts/README.md` (owned: catalog, device-integration; scope kit cross-referenced on the block-pin lane; pins table filled by VWB-40). **(b) Code:** `CONTRACT_VERSION = "1.5"` in `presentation/api/catalog.py` (distinct from the content-hash `CatalogResponse.version` — hashes are not versions); `wb-catalog` writes the STAMP core `{contract, version, tag, date, owner_repo}` from it, build extras (`bridge_commit`, `bridge_version`, `catalog_version`) stay; default output path follows the move. **(c) Guards:** `test_contracts_golden.py` re-pointed + the stamp test now asserts the version triple (constant == STAMP == tag string); found and fixed en passant: the file contained its entire module TWICE (accidental double-paste — the stale second copy shadowed the first and lacked the v1.3 test; Python name-shadowing kept all tests alive, so the suite never noticed) — deduplicated to the current copy, suite count unchanged at 704. **(d) Docs:** catalog README gained the Versioning section (family tags; v1.1–v1.4 prose lineage frozen, not retro-tagged; v1.5 = the convention cut, no surface change), regen commands updated; stale path refs fixed in `device_integration_convention.md` + inbox `SKILL.md`; `productization_bridge.md` §2 carries a dated SUPERSEDED block (annotate-don't-rewrite). **Golden byte-identical** (`5622ba7a1a78102a`), `backend/openapi.json` untouched → no UI work. Tag **`catalog-v1.5`** on the landing commit. Suite 704, pyright 0, import-linter 6/6. Voice's BUILD-24 re-pins against this layout (their delegation).

- [x] **VWB-30** `[P2]` — **DONE 2026-07-09** (REL-5 #13/#14/#15/#16 remediation, pulled forward to `[release]` because reports is LIVE on the WB7; same session as the review). **Reports evidence hardening — four gaps closed.** **(#13)** `redact_mapping` used to recurse into a credential-shaped key's container and leak any leaf lacking its own secret-shaped key (`{"credentials": {"primary": "SECRET"}}` slipped through) — now a credential-shaped key masks its *whole* value via a new `_mask_all` (structure kept, every leaf masked; `auth` username is masked too — "masking too much is safe"). **(#14)** `redact_text` gained a URL-userinfo pattern (`scheme://user:password@host`) so a broker URL's password is masked (it carries no keyword marker the assignment regex needs). **(#15)** `report_id` (which doubles as the spool filename) gained a `uuid4().hex[:8]` suffix — two reports in the same second + room no longer collide/overwrite. **(#16, UI)** `reportEvidence.ts` `actionLog` took `entries.slice(-N)` from a store that `unshift`es (newest at index 0), so it captured the OLDEST N and dropped the most recent (bug-relevant) ones — now `slice(0, N)` (newest). 4 new backend tests (`test_reports.py`: container-leaf masking, URL creds, id uniqueness). Backend suite +3, pyright 0, import-linter 6/6; UI check + build green. No OpenAPI/contract change.
- [x] **VWB-31** `[P2]` — **DONE 2026-07-19** (REL-5 #8 remediation; low-hanging-fruit sweep; ⇒ contract cut `catalog-v1.9`). **Handler-reported reachability failures now surface as `device_unreachable` (503), consistent with the echo-timeout path.** The finding reconciled exactly (line ref drifted, defect intact): the canonical dispatcher's step-failure classifier keyword-sniffed only for param-shaped wording — any other handler failure, including the eMotiva's `"Not connected to processor"` (`self.client is None`), bucketed to `internal_error`/500, while the *same* failure class arising during the echo wait correctly 503'd. Fix: a reachability marker set checked FIRST (`not connected` / `unreachable` / `connection refused|failed|error` / `offline`) → `DEVICE_UNREACHABLE`/503; deliberately narrow — protocol-limitation wording ("… not available on this device", "App list unavailable") stays 500, so bare "available"/"unavailable" is excluded (verified against a sweep of all driver error strings). 2 new tests (`test_canonical_endpoint.py`): the not-connected wedge case → 503, the protocol-limitation boundary → 500; the pre-existing "MQTT client unavailable"→500 test doubles as the false-positive guard. **Contract ripple (owner decision, this session):** the endpoint's OpenAPI description documented the old mapping — truthing it moves the stamped `contracts/catalog/openapi.json` ⇒ deliberate minor cut **`catalog-v1.9`** (STAMP+tag same change; README Versioning narrative continued incl. the previously-unnarrated v1.8; the stale pre-work "A3 convention" ref scrubbed from the description while in there). Golden **byte-identical** (`5622ba7a1a78102a`); UI types regen (comment-only), check+build green. Suite 740, pyright 0, import-linter 6/6. docs: contract/catalog. contracts: catalog-v1.9 cut (minor); re-pin owed: voice+commons (supersedes the still-open v1.8 owe — one re-pin covers both).
- [x] **VWB-32** `[P1]` — **DONE 2026-07-10.** **The retained catalog-version topic is now published at startup and on every MQTT (re)connect — the bridge self-heals the broker-wipe.** Filed from the 2026-07-09 incident: `bridge/catalog/version` was published ONLY from `POST /reload` (`system.py:221`), so the controller reboot (mosquitto runs WITHOUT persistence — every broker restart wipes ALL retained messages, user decision to keep it that way) left the topic missing and voice's catalog-staleness gate blind. **Fix:** (1) a generic **`on_connect_callbacks`** seam on `MQTTClient` — callbacks fire after each (re)connect completes its subscriptions, isolated so a failing callback never takes down the receive loop (the reusable home for anything retained that must survive broker restarts); (2) bootstrap registers `_publish_catalog_version` (via the existing `build_catalog`) on the seam AND calls it once at startup (the first connect precedes the managers, so the immediate call covers boot; the callback covers mid-run broker restarts); the `/reload` publish stays. 2 new tests in `test_mqtt_reconnect.py` (callbacks fire on initial connect + after a reconnect; a raising callback is isolated and the rest still fire). Suite **680**, pyright 0, import-linter 6/6; no contract change. Rides the pending DRV-28/UI-16 image rebuild — after redeploy the live broker carries the retained version again (`5622ba7a1a78102a`).
- [x] **VWB-35** `[P1]` — **DONE 2026-07-11** (filed + executed same session; PROD-14 phase-2 bridge delegation (1), council HK-3). **Reports-repo re-point → `locveil/locveil-reports` + explicit `reports.repo`, slug schema default dropped** (`0279ade` + guide follow-up `7f37dae`). **(a) Slug sweep:** inbox `SKILL.md` (7 sites), the `problem-report-inbox` invariant's repo + session-start `gh` command in CLAUDE.md (dialect section), `problem_reports_bridge.md` operative refs (§preamble/B-8/§6; §7 history frozen), `github_sink.py` docstring; `ops/INSTALL.md` org-move leftovers (clone URL → `locveil/locveil-bridge`, GHCR packages link → org; images already published to `ghcr.io/locveil/*`); no redirect reliance anywhere. **(b) HK-3 q4:** `system.json` carries explicit `"repo": "locveil/locveil-reports"`; `ReportsConfig.repo` → `Optional[str] = None` + fail-fast validator (`enabled: true` without `repo` refuses to load; a reports-less config stays valid — filing stays opt-in), sink constructed only when enabled (service already tolerated `sink=None`), the never-read `ReportsSettings.repo` deleted, `ReportsConfigResponse.repo` de-defaulted; new validator test. `docs/guides/report-a-problem.md` re-truthed (explicit-repo refusal vs the gentler missing-token spool path). **(c) Regen chain:** wb-openapi → `backend/openapi.json` + `contracts/openapi.json`, STAMP re-stamped, golden UNCHANGED (`5622ba7a1a78102a`), UI `gen:api-types`. **(d) Spool-drain verified 2026-07-11 (owner, on the WB7):** `/mnt/data/locveil-bridge-config/data/` has no `reports/` subdirectory — the spool dir is created lazily on first spool (`github_sink.py` `_spool` mkdir; `retry_spooled` handles absence) — nothing was ever spooled, trivially drained (consistent with the phase-1 live smoke). Suite 699→704 with VWB-37, pyright 0, import-linter 6/6, UI check + build green. Voice's own re-pin rides their PROD-14 delegation (`cross-repo-source-of-truth`).
- [x] **VWB-36** `[P1]` — **DONE 2026-07-11** (filed + executed same session; PROD-14 phase-2 bridge delegation (2), the VWB-26 pattern; the board's pre-named VWB-30 was stale at intake — serial consumed 2026-07-09 by the REL-5 remediation — re-serialed per assigned-once). **`lens-bridge.md` co-ownership re-review in `locveil/locveil-reports`** (`locveil-reports@6a5dc62`). Every existing claim verified against the live bridge repo: the four targeted test paths exist (`test_capabilities` / `test_system_catalog` / `test_select_canonical` / `test_contracts_golden`), the gate list matches (suite + pyright 0 + lint-imports + ui check/build), `$CROSS_REPO_TOKEN` matches `triage.yml`'s env mapping of `REPORTS_CROSS_REPO_TOKEN`, the ping-pong guard matches the protocol's `ping_pong_max: 1`; slugs were already clean (PROD-14 phase-0 sweep). One addition: §2 now teaches the protocol-pinned filing surface (`report-protocol.pin.json` + the VWB-37 conformance test — a triage fix must keep it green and never change the wire surface bridge-side) and the explicit no-default `reports.repo` (VWB-35). No bridge code touched.
- [x] **VWB-37** `[P1]` — **DONE 2026-07-11** (filed + executed same session; PROD-14 phase-2 bridge delegation (3) — the PROD-6 consumption half, council HK-3). **`report-protocol-v1` pinned + filing-surface conformance test.** The commons-owned machine core (`locveil-commons/process/report-protocol/report-protocol.json`) pinned byte-identical at the repo root as `report-protocol.pin.json` (tag verified == worktree before copying; same pin convention as the reports repo). The filing hardcodes in `domain/reports/service.py` (labels `problem-report`/`lens:bridge`/`new`, `[bridge-ui]` title prefix, `bundle.tar.gz`, the report-id source token) retired into module-level `REPORT_*` constants; `file_report()` emits only through them. New `tests/unit/test_report_protocol_pin.py` (5 tests) locks the constants to the pin — `filed_with` labels with the lens placeholder resolved, every emitted label protocol-defined, title prefix, bundle path/name shape, and `system.json`'s explicit `reports.repo` == the pin's `repos.reports` (ties VWB-35's cutover value to the protocol); the emitted-value half stays locked by the existing envelope test. On a protocol bump: re-pin first, then adjust constants until conformance passes. Suite **704**, pyright 0, import-linter 6/6; no contract change.

- [x] **VWB-38** `[P2]` — **DONE 2026-07-12** (PROD-15 bridge delegation item 2, council HK-4; design task, `design-then-implement`; interactive session — three owner decisions + the standing HVAC constraint). **Device-integration convention v1 designed and shipped: `docs/design/device_integration_convention.md` + `contracts/device-integration/{README.md, device-descriptor.schema.json, STAMP.json}`, tagged `device-integration-v1`.** The HK-4 two-layer contract realized: convention DOWN (bridge-owned, versioned), descriptors UP (satellite-owned, conforming — their DES-4 consumes; their `contracts/README.md` already reserves the pin row). **wb-mqtt-v1** = promotion of the deck FR-text per the satellite truth pass §2 (FR-5 announce / FR-6 LWT `meta/online` / FR-7 `<control>/on` / FR-8 echo-on-success / C-5 WB topic tree / FR-12 → the declared `requires_arm` interlock / NFR-2 → STATIC required `timing.confirm_latency_ms`), cross-checked against the bridge's own WB surfaces (`wb_device/service.py` emulation + passthrough consumption — same dialect, both ends of the wire); stale-pin tripwire = one retained `meta/locveil` JSON stamp `{app, fw, descriptor, convention}`, monitor-only, never behavior-branching. **Owner decisions (recorded §5 of the design):** **D1** the descriptor CARRIES the canonical capability mapping — class-map dialect with `control` in place of `command` and NO `gate` object (the driver derives its gate from `confirm_latency_ms`; the gate stays internal/re-tunable, the latency promise is contract — the VWB-34 split); zero bridge-side authoring per new satellite device = what makes DRV-36 descriptor-native. **D2** REST leg: asset-plane URLs normative (`GET /esp32/{firmware,models}/{ref}`, mTLS, satellite-owned nginx) + device-hosted `GET /api/status` / `POST /api/control` shapes RESERVED; full REST profile deferred to the first real consumer. **D3** descriptor i18n: ru+en required, de optional (VWB-33 binds descriptors when it lands). **Standing owner constraint recorded: `MitsubishiHvac` is UNTOUCHED — external firmware is out of scope by construction; the only path onto the convention is an owner-decided firmware rewrite** (the `mitsubishi_hvac_driver.md` §8 transport-swap horizon; also the satellite charter's escalation trigger). Vocabulary reconciliation = the satellite DES gate: descriptors never invent canonical tokens; the deck transport/record vocabulary lands bridge-side at DRV-36 + the first deck descriptor (golden catalog untouched now, per HK-4). Schema machine-verified: valid JSON Schema draft 2020-12, the guide's example VALIDATES against it, en-only `names` correctly REJECTED. `contracts/README.md` gained the second-family pointer. Docs + contracts only — no code, no catalog/openapi change, no re-pin owed. Follow-ups were pre-filed at intake: DRV-36 (consumes the convention), VWB-39 (pins it).

- [x] **VWB-40** `[P1]` — **DONE 2026-07-12** (filed + executed same session; PROD-16 bridge delegation (2), council HK-5 q3 — uniform layout immediate, no grandfathering; intake `17734d8`). **The report-protocol pin relocated to the org pin shape: `contracts/pins/report-protocol/`.** The repo-root `report-protocol.pin.json` (VWB-37) became `contracts/pins/report-protocol/report-protocol.json` — bytes verified identical to the commons tag `report-protocol-v1` before AND after the move (`0f095604…`) — joined by the owner's `STAMP.json` verbatim (the commons PROD-16 sidecar) and a **strict `PIN.json`** (full core + `files` sha256 map + `owner_commit` + conformance pointer — no legacy warnings left on this pin) + a small pin README. Path re-taught everywhere it was written: `test_report_protocol_pin.py` (load path + docstring; 5 tests green unchanged), the `REPORT_*` comment in `domain/reports/service.py`, `problem_reports_bridge.md`, the VWB-39 open entry + `device_integration_convention.md` §6 recipe mentions, the registry README's pins table, and the reports-repo lens teaching note (`locveil-reports/.github/claude/lens-bridge.md` — pin path + the `contracts/catalog/` moves from VWB-29, pushed to that repo). No wire-surface change — the pin's bytes and the filing constants are untouched.

- [x] **VWB-41** `[P1]` — **DONE 2026-07-12** (filed + executed same session; PROD-16 bridge delegation (3), council HK-5 — closes the convention model's own gap, pre-VWB-39: no unguarded model layouts, `../locveil-commons/process/contracts.md` §4 layer 2). **Device-integration owner-side guard: committed example fixture + CI check + STAMP core.** **(a)** `contracts/device-integration/example.descriptor.json` committed = the convention README's canonical descriptor example, byte-parity ENFORCED (deliberate narrowing from the intake sketch of a maximal synthetic fixture: the guide's example is THE exhibit — a doc/fixture divergence now fails CI, so the guide can never teach a shape the schema rejects; VWB-38 had validated it once, manually). **(b)** New `tests/unit/test_device_integration_schema.py` (5 tests, rides the normal backend job which already path-triggers on `contracts/**`): schema is valid draft 2020-12, the fixture validates, README example == fixture, en-only `names` rejected (the VWB-38 manual check, now locked), and a full-surface in-test descriptor covering what the README example doesn't reach — stateful capability (feedback/reconcile/state_field), parametric `set {value}` via `param_map`, enum field with `{wire, canonical, labels}` triplets (real pinned vocabulary — the HVAC fan family), range/value control meta. **(c)** STAMP.json normalized to the core `{contract, version, tag, date, owner_repo}` (+ `profiles`/`rest_reserved` extras; `convention_version` folded into core `version`; tag `device-integration-v1` untouched — the commons report-protocol sidecar precedent; the satellite hasn't pinned yet, their DES-4 pends). **(d)** README: links re-pointed to `../catalog/` (VWB-29 move), teaches the committed fixture + the owner-guard rule. Suite 704→**708**, pyright 0.

- [x] **VWB-42** `[P2]` — **DONE 2026-07-18** (repo-to-repo filing by locveil-satellite
  2026-07-18, HK-12 round-1 greenlight, listed in the bridge PROD-26 delegation; executed after
  OPS-32 so contract-guard v3 enforces exactly this shape). **`device-integration-v1.1` cut at
  the VWB-41-normalized state — the post-tag STAMP fix finally has its version.** The wrinkle:
  tag `device-integration-v1` (`d273508`) predates VWB-41's STAMP-core normalization (landed one
  commit later, no tag bump), so the v1 tag's bytes carry the pre-convention STAMP shape and the
  satellite's DES-4 had nothing clean to pin. **(a)** STAMP: `version` 1 → **1.1**, `tag` →
  `device-integration-v1.1`, re-dated 2026-07-18; per the HK-12 forward requirement the v1.1
  STAMP **enumerates `artifacts`** (convention README + descriptor schema + example fixture,
  REPO-ROOT-RELATIVE paths per v3 semantics — the VWB-43 catalog trap avoided from day one) —
  this ARMS CONTENT-DRIFT for the family from here on. **(b)** Annotated tag
  **`device-integration-v1.1`** on the landing commit; its bytes carry the normalized STAMP core
  DES-4 pins against. **(c)** Registry row: version authority `device-integration-vN[.M]`
  (current: v1.1). **(d)** Convention README's versioning bullet re-truthed (major = breaking,
  minor = non-breaking fixes/normalizations; stamp + tag move together; artifacts enumerated so
  a tag's bytes always match its stamp). Schema/manifest/pin tests 18/18 green; contract-guard
  STRICT green post-tag. docs: contract/device-integration + contracts-registry.
  contracts: device-integration-v1.1 cut; re-pin owed: satellite (a FIRST pin — their DES-4
  takes it; their `.repin.toml` already declares the family and nags never-pinned until then).

- [x] **VWB-43** `[P1]` — **DONE 2026-07-18** (filed same day by OPS-32's first v3 run; executed
  on owner go-ahead after the commons IMPL-8 sequencing note — VWB-43-first dissolves the v3.1
  rollout coupling entirely). **Catalog STAMP `artifacts` → repo-root-relative: deliberate minor
  cut `catalog-v1.8`, surface content byte-identical.** The trap being defused: the pre-v3 bare
  names made `catalog.golden.json`/`openapi.json` CONTENT-UNVERIFIABLE and compared the
  REPO-ROOT README on both sides of the drift check — armed to brick every commit (the hook
  runs CONTENT-DRIFT unrelaxed) at the next root-README edit (OPS-28 was a live candidate).
  **(a) The generator, not the STAMP, was the real fix site** — the intake-flagged check
  confirmed `cli/dump_catalog.py` hardcodes the artifacts list, so a hand-edited STAMP would
  have been silently reverted to bare names at the next regen, recreating the post-tag-drift
  scar this arc exists to kill; it now emits `contracts/catalog/…` paths. **(b)**
  `CONTRACT_VERSION` 1.7 → **1.8** (`presentation/api/catalog.py`); STAMP regenerated from the
  repo root — version/tag/date/artifacts/bridge_commit moved, golden BYTE-IDENTICAL
  (`5622ba7a1a78102a`), openapi untouched: a content-free convention cut like v1.7. **(c)** The
  version-triple test's pin-completeness assertion re-pointed at the root-relative set. **(d)**
  Lightweight tag **`catalog-v1.8`** on the landing commit (family style), pushed together.
  contract-guard post-tag: the catalog family is fully verifiable for the first time under v3 —
  0 warnings, CONTENT-DRIFT genuinely armed. Suite **734**, pyright 0, import-linter 6/6.
  docs: none — STAMP/generator internals; no manifest-node behavior changes.
  contracts: catalog-v1.8 cut (STAMP artifact paths → repo-root-relative; surface content
  byte-identical); re-pin owed: voice, commons (voice's multi-dest catalog run updates both).

## UI — config-ui

- [x] **UI-1** — **DONE** 2026-05-19 — backend `ab5402d`, UI `8ab2cfa`. On survey, the 8 modified UI files turned out to be one coherent appliance-category feature (not an unrelated layout refresh as initially thought) plus two unrelated SSE console-log cleanups; `docs/appliances.md` was the matching design doc. Shipped as a single paired commit per repo. `config/system.json` (UI) left untracked pending later check; `data/` added to UI `.gitignore`.
- [x] **UI-2** — **DONE** 2026-05-19 — `ab5402d`. Shipped together with #0a as the backend half of the paired change.
- [x] **UI-3** — **DONE** 2026-05-20 — backend `6bc30fc`, UI `312fa56`. Generate OpenAPI types for the UI. Hit FastAPI's `/openapi.json` and run `openapi-typescript` to produce `src/types/api.gen.ts`. Replace `src/types/api.ts` gradually, starting with the simplest endpoints. Removes the API-surface coupling (hand-maintained types). Does **not** remove the Python AST coupling in device-page codegen — see #3.5.
- [x] **UI-4** — **DONE** 2026-05-20 — backend `6bc30fc`, UI `5a71929`. Eliminate the Python AST dependency in UI codegen. Type the backend's `/devices/{id}/persisted_state` endpoint with a discriminated union of state models (`LgTvState`, `EmotivaXMC2State`, …) so they land in `/openapi.json` automatically. Rewrite `wb-mqtt-ui/src/lib/StateTypeGenerator.ts` (the actual Python-spawning logic lives there — `spawn(python3, ['-c', importlib + ast.parse(...)])` — invoked by `src/scripts/generate-device-pages.ts`) to consume the OpenAPI schema instead of spawning a Python subprocess and AST-parsing imported Pydantic classes. Remove `pip install -e ./wb-mqtt-bridge` and Python from the UI Dockerfile. Closes the "silent break on backend rename" failure mode. See `docs/archive/codegen_alternatives.md` (Option 1).
- [x] **UI-5** — **DONE** 2026-05-20 — UI `395e538`. Parameterize nginx + MQTT URLs. `envsubst` on `nginx.conf.template` at start; runtime `runtime-config.js` shim for the MQTT URL instead of build-time bake. Defaults preserve `192.168.110.250` so existing deploys are unchanged.
- [x] **UI-6** — **DONE** 2026-05-20 — backend `2e5674c`, UI `7c3f3a8`. Moved `device-state-mapping.json` from the UI repo to the backend repo. Paths inside are now relative to the mapping file's own directory; the UI resolves them, so one file serves both CI and local layouts (the `.local.json` was retired).
- [x] **UI-7** — **DONE** — subsumed by the **Layer-3 backend layout manifest** (option 2: backend owns placement; served at `/devices/{id}/layout` + `/scenario/{id}/layout`, consumed by `RemoteControlLayout`). The implicit config-order convention is retired — ordered zones follow capability-declaration order. See `ui_backend_contract.md` "Layout Manifest & Runtime Rendering" + §6 cutover (2026-05-24). _Original task:_ **Design a contract-based button/action placement.** Today, *where* a control renders inside its remote zone is governed by an **implicit, undocumented convention**, not a contract — and we want to replace it. Two mechanisms, both verified in code (2026-05-20): (a) **slot zones** (power / volume / nav-cluster / pointer) fill fixed slots by **action-name substring matching** (`ZoneDetection.createPowerButtonsConfig`, `createMenuZone`, `createVolumeZone`, `createPointerZone` — e.g. name contains `off`→left, `on`→right; `up/down/left/right/ok`→D-pad); (b) **array-order zones** (screen vertical stack, playback row, tracks row) render in the order actions appear, which traces back through `deriveGroupsFromConfig` → `processAllGroupActions` to the **key order of `config/devices/*.json` commands**. This is fragile (reordering a config silently moves buttons; renaming/retyping an action can drop it from a slot or land it in the wrong one) and surprising. **Action: discuss options and design an explicit placement contract.** Candidate directions to weigh — (1) explicit per-action `slot`/`position`/`order` fields in the device config; (2) a dedicated **layout manifest** owned by the backend and served/consumed as a contract (aligns with codegen Option 2 — `docs/archive/codegen_alternatives.md`, runtime-driven UI); (3) command-level UI annotations (`x-ui-*`-style). Trade-off: authoring effort vs. determinism + reviewability. Touches both repos. **Design first — not yet scoped for implementation.**

- [x] **UI-8** `[P2]` — **ABSORBED into OPS-13 at its intake (sprint-01 decision 1), delivered 2026-07-14.** The vite 5 → 6 half of the toolchain pass: bump landed at **^6.4.3** (the first-patched version the filing predicted) + `@vitejs/plugin-react` ^4.7 via OPS-13; the entry's pre-verified readiness claims held (ESM config, explicit `esnext` target, Node 20 builder — vite 6's CJS removal / Node floor / default-target changes didn't bite) and its DoD ran there: `npm run check && npm run build` clean, the **dev-server SSE smoke passed** (the `/events` proxy's `http-proxy` hooks under vite 6 — both `configure()` hooks fired, stream unbuffered), Dependabot's vite/esbuild alerts clear. Details: the OPS-13 DONE entry. docs: none — delivered inside OPS-13, whose entry carries the verdict (ui-readme).

- [x] **UI-9** `[P2]` — **DONE 2026-07-06** (pulled forward from `[later]` by user decision — "do UI-9"; the last first-party `/action` writer). **The Layer-3 dropdown seam is canonical** (`canonical_first.md` §11.3 updated to SHIPPED). **Manifest:** `DropdownConfig` dropped the native trio (`set_action`/`set_param`/`api_action` — the latter dead since SCN-7 moved population to the options endpoint) and carries the canonical tuple instead: `canonical_capability`/`canonical_action`/`canonical_param` — `input.set {value}` for inputs (both select forms, the VWB-19 route), `apps.launch {app}` for apps (endpoint renames `app` → native via param_map). **Option ids are canonical values for BOTH population methods** — a by_value option's id is now the table key (`cd`), no longer the native command name (`input_cd`); `_inputs_dropdown`/`_apps_dropdown` rewritten accordingly. **UI:** `useInputSelection`/`useAppLaunching` switched from `useExecuteDeviceAction` to `useExecuteCanonicalAction` with `wait: false` (button parity; SSE delivers post-switch state); the "commands"-mode special case (option id = native command) is GONE — one dispatch shape for all dropdowns; target stays the dropdown's `sourceDeviceId` on scenario pages (role device — `apps` is not in the proxy's `INHERITABLE_DOMAINS`, so scenario app-launch correctly bypasses the entity, consistent with the read side). **Contract:** `openapi.json` regenerated (DropdownConfig schema swap) + `contracts/` copy synced; catalog golden byte-unchanged (manifests aren't catalog surface); UI types regenerated. **Tests:** 4 new in `test_layout_engine.py` (parametric tuple, by_value ids-are-values + round-trip through `CapabilitySelect.expand`, apps launch tuple); suite **571 passing**, pyright 0, contracts 3/3, UI check + build green. **Consequence:** `/action` has **no first-party write consumer left** — the §8 phase-3 demotion decision (acceptance-gate item 4) is unblocked. Docs: `ui_backend_contract.md` (fate table row + SCN-7 section), `docs/architecture/ui.md` (media-stack row); stale ui.md scenario-routing narrative filed as **DOC-11**.
- [x] **UI-13** `[P1]` — **DONE 2026-07-09** (REL-5 #7 remediation, same session as the review). **SSE stream never gives up permanently.** `useEventSource`'s `onerror` scheduled no reconnect once `reconnectAttemptsRef >= maxRetries` (~10 attempts / a few minutes), and nothing re-armed — a network outage longer than the back-off window left `/events/devices` dead until a full page reload, silently freezing device state on a wall panel (a user acting on stale state double-actuates). Fix: fast exponential back-off for the first `maxRetries` attempts (transient blips), then a slow keepalive reconnect every `SLOW_KEEPALIVE_MS` (60 s) forever, so the stream self-recovers when the outage ends; the back-off budget (`retryRef`) also resets on a successful event. UI check + build green.
- [x] **UI-14** `[P2]` — **DONE 2026-07-09** (REL-5 #17/#19 remediation, pulled forward to `[release]`; same session as the review). **Two UI runtime nits.** **(#17)** a shipped `TEMPORARY DEBUG` default branch in `Layout.tsx` logged and surfaced *every* unrecognized device SSE event into the progress feed — removed; unknown event types are now ignored. **(#19)** `ForceReconcileDialog.close()` reset `expanded`/`results` but not `pending`, so closing during an in-flight force and quickly reopening left a device stranded as "pending" (the component returns null but keeps its state) — `close()` now also `setPending(null)`. UI check + build green.
- [x] **UI-16** `[P2]` — **DONE 2026-07-10** (pulled forward from `[deferred]` by the user to ride the DRV-28 image rebuild; run as a **three-iteration artifact-based icon review** — a live review page with per-icon approve/alternate/comment + a general-instructions field, the user's exported review feeding each next iteration). **Enum-value icons now resolve through the shared `IconResolver` — the AV mechanism extended to canonical values; `HvacPanel`'s hardcoded text-glyph maps are GONE.** **Review outcome (all 27 values settled):** it. 1 approved 21 (cool→`AcUnit`, dry→`WaterDrop`, heat→`WbSunny`, fan_only→custom `fan`, quiet→`Nightlight` (the AC-remote moon convention), speeds/positions→the custom number ladder, widevane arrows→`KeyboardArrow*`/`KeyboardDoubleArrow*`, split→`SyncAlt`, power pair = the AV remotes' exact icons); it. 2 approved the **custom `auto-recycle`** (♻-faithful chasing arrows, one icon for mode/fan/vane auto) + **custom `center-bar`** (the user's "keyboard |") + the **fixed-color trio** (dry `#42A5F5`, cool `#4FC3F7`, heat `#FFB300`) with the color rule pinned: everything else = theme ink (currentColor — black on light / white on dark); it. 3 approved the **swing redraw** — DETACHED rays (the user rejected the joined pivot; the firmware's ⚟ implies convergence, never draws it), orientation-aware pair: custom **`swing-v`** (vane) / **`swing-h`** (widevane). **Code:** 4 new custom SVGs (`AutoRecycle`/`SwingV`/`SwingH`/`CenterBar`) registered in the `customIcons` map; `IconResolver` gains `valueIconMappings` (scoped `capability.value` → bare-value fallback lookup — collision-proof: vane.swing ≠ widevane.swing) + `resolveValueIcon()` with optional `color`; `HvacPanel` renders `<Icon>` via a small `ValueIcon` component (fixed-color entries wrapped in a colored span; unmapped values fall back to a neutral dot), incl. the power button. Any future enum-carrying device gets value icons for free. **UI-only** — no contract/backend change; UI check + build green. Rides the pending DRV-28 image rebuild; visual check on the WB7 after redeploy.

- [x] **UI-17** `[P1]` — **DONE 2026-07-14** (filed at PROD-24 intake the same day — the sprint-01 "(files at intake)" UI-surfaces row grown by the shell council; NB repo-local serial, distinct from voice's UI-17). **UI surfaces → Workbench/operations split: the bridge-side design** → [`docs/design/ui/workbench_split.md`](design/ui/workbench_split.md). **Decisions:** (1) classification consumed as ruled — `ui/` + appliance/room pages = **operations**, unchanged, the admin-route/auth-shell scope DELETED from operations; device-setup (IR learning as sidebar child) + topology-setup + voice-setup = the **Bridge plugin** under one Workbench tab (voice-setup stays under Bridge — bridge-backend surface). (2) Plugin package = new top-level `workbench-plugin/` (vite-6 **library** build → ESM dist + embedded generated API types; the shell consumes the BUILT artifact via `file:` dep, never TS sources; no imports from `ui/`; ui-kit restyle rides `ui-kit-v1`). (3) Contract-v1 mapping: static `pages()`, RU/EN bundles (device-sourced `de` data untouched — the chrome contract is RU/EN), status slot from `GET /system` + catalog version hash, `reportHook` → the live `POST /reports`, and **every config-writing verb DORMANT under the named gate `PROD-4-auth`** — rendered disabled with its gate, never hidden. (4) The **v1 read-only cut**: importer *propose* = compute endpoint (conf source dev-phase answer = paste/upload; SSH-from-backend rejected pre-PROD-4 secrets posture), topology read + dry `resolve_targets` preview, voice status + test-utterance over the existing `/canonical`; IR *capture* = existing action surfaces, only *save* waits. (5) **Staged-write API shape**: one envelope per target in `data/staged-config/` (`{target, base_sha256, content, staged_at, note}`; the `:ro` config mount enforces no-live-write structurally), `GET/PUT/DELETE /staged[/{target}]` with stage-time full-tree validation via the existing loaders (422 on failure), stale base = surfaced conflict never a merge, self-cleaning once promotion lands (live hash == proposal hash), promotion = human commit + `update.sh`; hexagonal placement presentation → app staging service → infrastructure store, **zero new import-linter exceptions**; endpoints unreachable until PROD-4's auth decision (binding condition, verbatim). **Planned-docs follow-up (same change):** all four `docs/planned/` pages re-pointed — admin-shell rows superseded (setup pages) / deleted (appliance-room, operations); device-setup + topology-setup carry "Apply stages via the controller API; promotion is a commit"; topology's live-vs-file open question ANSWERED = staging; chassis/route mentions updated — and the flow diagrams regenerated: `device-setup-flow` + `topology-setup-flow` gained the staged hop, `voice-setup` the Workbench chassis label. **Follow-ups filed:** **UI-18** (plugin package + read-only cut), **CORE-12** (staged-write API, PROD-4-gated), **DOC-17** (pre-existing planned-pages status staleness + the diagrams' old `wb-mqtt-bridge` titles — discovered, not caused). docs: diagram/device-setup-flow, diagram/topology-setup-flow, diagram/voice-setup (the planned pages themselves are not manifest nodes).

- [x] **UI-18** `[P1]` — **DONE 2026-07-15** (filed at UI-17 completion; refined + unblocked at the
  HK-11 intake the same day; design: `docs/design/ui/workbench_split.md` §2 as amended). **The
  Bridge Workbench plugin: `workbench-plugin/` package + the read-only v1 cut, loading in the
  commons shell.** New top-level `workbench-plugin/` (`@locveil/bridge-workbench-plugin` 0.1.0):
  vite-6 library build → single-file ESM `dist/index.js` (17 kB) + `style.css` (plugin-run
  Tailwind, ui-kit preset, preflight OFF) + the build-emitted fragment `dist/manifest.json`
  (`{id: bridge, version, entry, styles[], peers}` — peers `react ^18 / react-dom ^18 /
  react-router-dom ^6 / locveil-ui-kit ^0.1`, matching the shell's strict check); HK-11 singleton
  set external, everything else bundled; embedded generated API types (own `gen:api-types` off
  `backend/openapi.json`); descriptor compiles against `locveil-workbench/contract` (dev dep,
  types only); eslint-9 flat config mirroring `ui/eslint.config.js`; no imports from `ui/`.
  **Descriptor:** id `bridge`, RU/EN via a typed message table (plugin-local by design — no i18n
  lib needed at this size), `status()` = `GET /system` + `GET /system/catalog` (device count +
  catalog version; error → "нет связи"), `reportHook` → prompt + the live `POST /reports` with
  plugin/route context. **Pages:** voice-readiness (catalog + bridge versions; canonical-command
  test pane — device/capability/action selects fed from the catalog, params JSON, `wait` toggle →
  `POST /devices/{id}/canonical`; hosts the controller-address setting: injected global →
  localStorage override → `hostname:8000` fallback, the voice plugin's pattern) + device-setup
  (device inventory from `GET /config/devices`) + topology (rooms from `GET /room/list`); every
  config-writing verb dormant via a shared `DormantVerb` (disabled + `PROD-4-auth` chip — never
  hidden). **Reconciliations recorded:** the design's "minimal local primitives until ui-kit-v1"
  fallback was overtaken — ui-kit (0.1.2, in the singleton set) is consumed from day one, the
  restyle debt never accrued; the shipped contract v1 is leaner than the design sketch (no
  per-page `verbs`/`backendTarget` fields) — dormant verbs render in-page, the target address is
  plugin-level per §2.2's "workbench-level configuration"; §2.2's sidebar-children idea has no
  contract surface — Import/IR-learning render as sections of device-setup. **Registered with the
  shell:** the bridge location added to commons `workbench.config.json` (the mount its `$comment`
  reserved). **Verified:** plugin `npm run check` (tsc + eslint-9 type-aware, 0 problems) + build
  green; the shell's serve path live end-to-end — `/runtime-config.json` lists the plugin,
  `/plugins/2/manifest.json` + entry + styles all 200 with correct MIME (the IMPL-1 verification
  bar). Honest caveat: machine checks stop at the HTTP layer + static gates — browser render and
  live-backend page interaction are one `npm run serve` + WB7 target away. docs: none — the
  workbench is a dev-phase workstation tool; no manifest node describes it, `workbench-plugin/**`
  maps to no docs surface, and the package README is the consumer doc (the commons ui-kit/shell
  precedent).
- [x] **UI-19** `[P1]` — **DONE 2026-07-15** (filed + executed same day; commons **IMPL-6**
  consumer note, repo-to-repo — no board write-back asked). **The plugin's backend address comes
  from `PageProps.backends.api` (workbench-v1.2).** IMPL-6 landed deployment-facts injection:
  per-plugin `backends` in the owner-edited shell config, passed config → runtime-config →
  loader → `PageProps.backends` — the UI-18 plugin's ad-hoc `window.__LOCVEIL_BRIDGE_API__`
  global (which nothing ever set) anticipated the need but was the wrong mechanism, and its
  hostname fallback was the same shell-origin trap the IMPL-6 note warns about. Change (~10
  lines): the global is GONE; the page wrapper feeds `backends.api` into the module client
  **synchronously** (`setShellBase` — child effects run before parent effects, so a `useEffect`
  would fire after the pages' mount-time loads; the voice plugin's reasoning, adopted verbatim);
  `apiBase()` precedence is now localStorage override (the user-level escape hatch, kept — a
  non-empty address on the voice-readiness page stores it, emptying the field returns control to
  the shell config) → shell-configured backend → `hostname:8000` fallback (now covering only a
  shell with no backends configured). Plugin 0.1.1; README resolution paragraph updated.
  Verified: `npm run check` + build green; serve.mjs runtime-config hands the bridge entry
  `backends.api = http://192.168.1.50:8000` (the owner-configured WB7 origin). docs: none — same
  scope as UI-18 (dev-phase workstation tool; package README is the consumer doc, updated in
  place).

## OPS — Docker / CI-CD / deploy / ops

- [x] **OPS-1** — **DONE** 2026-05-19. Deleted local + origin: backend `code_structure`, backend `feature/wb-virtual-device-emulation`, UI `code_structure`. Both repos now have only `main`.
- [x] **OPS-2** — **DONE** 2026-05-19 — backend `36b54d8`, UI `5be5bd2`. Wired tests into CI on amd64, gated ARM build. Final state: 107 pass / 109 skip / 0 fail. **109 tests skipped with explicit `reason=`** are pre-existing API drift / collection hangs from the scenario+state-store refactors — deferred to a future cleanup PR, not deleted. UI side wires `npm run lint + typecheck:all + validate:generated-code + validate:components`; did **not** wire `npm test` (jest preset misconfigured, no test files exist). See commit body for the full skip inventory.
- [x] **OPS-3** — **DONE 2026-05-26** — CI pushes images to GHCR (`ghcr.io/droman42/wb-mqtt-bridge` + `wb-mqtt-ui`) with tags `:latest`/`:sha-<short>`/`:vYYYYMMDD-<short>`; auth via the workflow's GITHUB_TOKEN (no PAT). One-time manual flip in repo settings to make packages public. See §6 entry + commit `6a61766`.
- [x] **OPS-4** — **DONE 2026-05-26** — `ops/docker-compose.yml` (host network, mem/cpu limits) + `ops/wb-mqtt-bridge.service` (systemd) + `ops/update.sh` (~10 lines) + `ops/INSTALL.md` (WB-side cutover, recovery, rollback). Retires `ops/manage_docker.sh` (1081 lines) + the docker_manager_config.json PAT. See `fd61c93`. **WB-side cutover gated on the user** at the rack (INSTALL.md walks it).
- [x] **OPS-5** — **DONE** 2026-05-22 — chose **monorepo** (Phase 2 migration: `backend/`+`ui/`+`wb-rules/`+`ops/`+`docs/`; old UI repo archived read-only). See §6 (2026-05-22).
- [x] **OPS-6** — **#8 — Clean shutdown (SSE drain + pyatv teardown). DONE 2026-05-28 — HW-verified fully clean on a single Ctrl-C.** The diagnosis below was correct, but the fix grew from 2 parts to **4** as each layer came off at the rack: **Part 1** `c3f0305` — SSE generators poll uvicorn's `Server.should_exit` (new `sse_manager._shutdown_signaled()`; `app/main.py` switched to low-level `Config`+`Server` so the live server can be handed to the SSE manager) → 1st Ctrl-C drains the long-lived SSE connections instead of hanging forever. **Part 2** `bfa9614` — pyatv's `AppleTV.close()` is **sync but returns `Set[asyncio.Task]`** (per-protocol cleanup tasks the caller must await); the driver was dropping that set → 2 orphaned aiohttp ClientSessions per shutdown. Now captured + `asyncio.wait_for(asyncio.gather(*tasks), timeout=2.0)` + cancel stragglers on timeout. **Part 3** `1ada043` — bootstrap lifespan-shutdown blanket-cancelled `asyncio.all_tasks()` except the current one, which **includes uvicorn's own serve task** (parked in `lifespan.shutdown()` awaiting our completion); cancelling it produced a `CancelledError` traceback out the top of `asyncio.run` + an orphaned `_GatheringFuture` warning, and prematurely killed the MQTT task before the ordered disconnect. Block removed — the ordered teardown stops every task we own; `asyncio.run` mops up stragglers after the lifespan returns cleanly. **Part 4** `607b544` — uvicorn's `Server.capture_signals` deliberately **re-raises the captured SIGINT** after a graceful shutdown (server.py:326-330) so an embedder sees standard Ctrl-C semantics; the asyncio runner turns that into a `KeyboardInterrupt` out of `server.run()`. uvicorn's CLI relies on click catching it — our console_script `main()` now does the same (try/except `KeyboardInterrupt` → quiet exit). **Verified at the rack:** single Ctrl-C → full bootstrap sequence, both Apple TVs "pyatv cleanup tasks completed cleanly (2 task(s))", "System shutdown complete" → "Application shutdown complete." → "Finished server process", prompt returns immediately, **zero** `Unclosed client session`, **zero** `Exception ignored in: threading`, **zero** trailing traceback. 343 tests pass throughout; hexagonal LAW clean (changes confined to composition root + presentation). *Original diagnosis kept below for the record.* Promoted-and-diagnosed 2026-05-27 evening (subsumes the original P4 #6 "Teardown noise" sub-item — see §6 entry). **Full causal chain confirmed by terminal-traceback evidence during the post-HW-pass shutdown attempt:** SSE generators never respond to a shutdown signal → uvicorn's 1st Ctrl-C "graceful shutdown — waiting for connections to close" hangs forever on the 3 long-lived SSE connections (scenarios/devices/system) → 2nd Ctrl-C raises `KeyboardInterrupt` synchronously via uvicorn's signal handler (`asyncio.runners._on_sigint`, `runners.py:157`) → starlette lifespan is suspended at `await receive()` (waiting for a `lifespan.shutdown` message that never arrives in the queue) → the `@asynccontextmanager`-wrapped lifespan generator gets **`GeneratorExit`** at the `yield` point (NOT `CancelledError` — different exception class; the bridge's `try/except asyncio.CancelledError` at `bootstrap.py:359` cannot catch it) → after-`yield` block (orchestrated shutdown — `device_manager.shutdown_devices()`, `state_store.close()`, etc.) is structurally unreachable → Apple TV's `shutdown()` never runs → pyatv's underlying zeroconf/aiohttp resources are never properly torn down → Python's `threading._shutdown()` blocks indefinitely waiting for pyatv's non-daemon threads to terminate (observed as a **3-minute 2-second silent hang** in this session; the morning session was 50 s — varies with whatever pyatv was holding) → 3rd Ctrl-C interrupts `threading._shutdown()` (visible as `Exception ignored in: <module 'threading'> ... KeyboardInterrupt:` on stderr) → GC dumps **2 `Unclosed client session`** aiohttp warnings (one per AppleTV instance: `appletv_children` + `appletv_living`). The 2 unclosed sessions are the smoking gun — pyatv holds an aiohttp ClientSession per device that's only properly closed inside its `disconnect()` flow, which the lifespan would have triggered. **2-part fix** (estimated ~40 LoC total, 2 small focused commits): **Part 1** — make SSE generators in `presentation/api/sse_manager.py` poll uvicorn's `Server.should_exit` flag (or react to a shared `asyncio.Event` set on shutdown). Effect: 1st Ctrl-C drains the connections cleanly, uvicorn proceeds to send `lifespan.shutdown` via the protocol, after-`yield` block runs as designed, `device_manager.shutdown_devices()` is reached. **Part 2** — wrap pyatv teardown in the Apple TV driver's `shutdown()` with an explicit `await asyncio.wait_for(self.atv.close(), timeout=2.0)` + finally-clear; on timeout, log + return without blocking the rest of the shutdown sequence. Effect: even if pyatv's internal teardown is sluggish, we bound it, and the lifespan's other devices still get their `shutdown()` called. **Verification** (single rack-press test post-fix): single Ctrl-C produces the complete log line sequence `"System shutting down..."` → `"Shutting down SSE connections..."` → `"Cancelling background tasks..."` → `"Found N background tasks to cancel"` → `"Flushing pending persistence before shutdown..."` → `"Preparing device manager for shutdown..."` → `"Shutting down scenario manager..."` → `"Shutting down room manager..."` → `"Disconnecting MQTT client..."` → `"Shutting down devices..."` → `"Closing state persistence connection..."` → `"System shutdown complete"`, the process exits within ~5 s, no `Unclosed client session` errors, no `Exception ignored in: threading`. **Not blocking the per-driver HW pass** — workaround at the rack is `kill -TERM` or accept the 3-Ctrl-C dance (state is preserved either way, see [[state-sync-chokepoint]] and the original 0306898 P4 #6 promotion note).
- [x] **OPS-7** `[P2]` — **DONE 2026-07-08** (night session; reconciled against a radically improved reality — the entry was written at **88 alerts** on 2026-05-31, only **5 remained** at execution). **Dependency refresh — Dependabot triage.** 83 of the 88 resolved themselves through ordinary lockfile evolution between filing and now: the axios cluster, the whole aiohttp/urllib3/starlette backend set, the react-router pair — all gone; the **backend lockfile is fully clean** (zero alerts). The original bump plan (axios/aiohttp/urllib3 targets) is therefore obsolete and was not executed. The 5 survivors — vite ×3 (two Windows-only dev-server CVEs + a dev-server path traversal), esbuild (dev-server CORS), minimatch (lint-time ReDoS) — are **all `scope: development`**, in `ui/package-lock.json` only, never in the deployed nginx container (which serves the prebuilt `dist/`) or the backend image. None is fixable in-range (`npm audit fix` + `npm update minimatch` both no-ops, lockfile untouched): the vite/esbuild trio needs the **vite 5→6.4.3+ major**, and minimatch `9.0.3` is pinned *exactly* by `@typescript-eslint/typescript-estree` 6.21 — fixed only via **@typescript-eslint ≥7.5.1 ⇒ the eslint 9 flat-config migration**. Disposition per the entry's own standing rule ("bump build-chain deps only for a CVE in the actual runtime path; mass-bumping the toolchain risks major churn without security benefit"): **all 5 dismissed on GitHub as `tolerable_risk`**, each with a triage comment pointing at **OPS-13** (the deliberate post-release toolchain migration: eslint 9 + @typescript-eslint 8 + vite current — filed same session, `every-task-in-the-ledger`). **Open Dependabot alerts: 0**; the per-push vulnerability banner is gone. No code or lockfile change — GitHub state + ledger only.

- [x] **OPS-8** `[P1]` — **DONE 2026-07-08** (desk session; scope narrowed interactively per `task-start-reconciliation` — the user's own "it might be greatly outdated by now" was right). **Lifecycle-robustness leftovers (from the 2026-05-22 hardware session) — reconciled, narrowed, shipped.** The original five sub-items resolved as: **(1) Defensive startup-failure cleanup — STILL VALID, SHIPPED:** the lifespan startup body is now wrapped; an unexpected mid-startup error releases the already-acquired resources via the new module-level `_release_partial_startup()` (report-retry cancel → device shutdown → MQTT disconnect → state-store close, each independently guarded) then re-raises — no more leaked sockets/hung process; `report_retry_task` predeclared before the try; the body reindent verified pure-whitespace (`git diff -w`). 3 tests pin the helper's contract. **(2) Teardown noise / Ctrl-C×3 hang — ALREADY ADDRESSED:** that's OPS-6 (DONE 2026-05-28, 4-part fix, HW-verified single-Ctrl-C clean); the sub-item was already marked superseded in place. **(3) Auto-reconnect for failed-setup devices — ALREADY ADDRESSED piecewise since filing:** eMotiva reconnects on command (`setup()` in handlers — the item's own example), Apple TV `_ensure_connected()` on every command, LG close-callback + health loop + WoL power-on (2026-05-30), Auralic cadenced probe + halt detection (DRV-12/14); keep-registered is test-pinned (`test_device_manager_resilience`). No generic mechanism — none needed, every real device self-heals. **(4) Apple TV hygiene — MOSTLY OVERTAKEN, lite fix shipped:** the dead `device_update`/`device_error` methods were already gone; the remaining tail (app-list fetch failure logged at ERROR + wrote `state.error` when the box was merely asleep — made a sleeping Apple TV look broken) demoted to WARNING with no `state.error` write, old list kept; the "§15 tvOS Who's watching" tie-in is stale and dropped. **(5) WB virtual cards stay `available=1` after the bridge stops — STILL VALID, SHIPPED (embarrassingly cheap):** `BaseDevice.cleanup_wb_device_state()` existed with ZERO callers (and the "Last Will" setup is a misnomer — retained `1`s only, no actual MQTT will); now wired into bootstrap shutdown deliberately **before** the MQTT disconnect (the offline publishes need the connection), after scenario/room teardown; broker metadata only — hardware-transparent shutdown unchanged; service-level publish behavior already pinned in `test_wb_virtual_device_service`. Suite 652, pyright 0, contracts 6/6, no API surface change; `architecture/overview.md` lifecycle sweep updated (shutdown marks WB cards offline; startup failure-symmetric). **HW observation** (stop the bridge, watch device cards grey out in the WB UI) rides REL-2/REL-3.

- [x] **OPS-9** `[P2]` — **DONE 2026-07-02.** **Retired the docker_manager leftovers** (follow-up to OPS-3/OPS-4, GHCR + compose flow DONE 2026-05-26 — the repo transition was complete but three tails remained; filed + executed same day, surfaced answering deployment questions in chat). **(a)** Deleted the **untracked local** `ops/docker_manager_config.json` (gitignored, never committed — only the placeholder sample ever was, removed with the script in `fd61c93`) which still carried the live GitHub PAT the migration was meant to kill; **PAT revocation on GitHub = user action, flagged in chat** (nothing uses it: CI auths with `GITHUB_TOKEN`, the WB pulls public GHCR packages anonymously — cannot be executed or verified from the repo). **(b)** `backend/README.md`: replaced the ~460-line retired-flow walkthrough (~30 `manage_docker.sh` references — artifact download dance, scp transfer, per-container `docker run`, custom-volume config via the script's JSON, full command reference) with the current strategy (CI → GHCR `:latest`/`:sha-<short>`/`:vYYYYMMDD-<short>`; WB deploy via `ops/docker-compose.yml` + systemd + `ops/update.sh`; compose-accurate volume mounts `../backend/config` + `../.state/{data,logs}`; runbook pointer to `ops/INSTALL.md`); kept the still-true lean-image build subsection; rewrote the trailing "Deployment" stub. **(c)** `ui/README.md`: "Docker Deployment" rewritten off the artifact `gunzip`/`docker load` flow (now GHCR + compose + standalone-`docker run` variant; local build from the **monorepo root** — `ui/Dockerfile` copies `backend/config` + `backend/openapi.json` from the repo-root context); removed two **dead** `ui/docs/deployment*.md` links; also fixed the adjacent pre-monorepo staleness in the same doc ("sibling `wb-mqtt-bridge` checkout" / "clone both repos side by side" → the monorepo `../backend` layout, matching the Contributing section). `ops/INSTALL.md` untouched — its docker_manager mentions are the intentional cutover/comparison content. Docs-only change: no code, no contract, no UI build impact.
- [x] **OPS-10** `[P2]` — **DONE 2026-07-02.** **Path-filtered CI** (pattern borrowed from `../stockvision/.github/workflows/deploy.yaml`, user request; filed + executed same day). `.github/workflows/build-arm.yml` restructured: a **`changes`** job (`dorny/paths-filter@v3`, `fetch-depth: 0` so multi-commit pushes diff against the push base) emits `backend`/`ui`/`ledger` outputs that gate everything downstream. **`backend-test`** runs on `backend/**`; **`ui-validate`** on `ui/**` **plus the backend contract it consumes** (`backend/openapi.json`, `backend/config/**` — `config-ui-stays-functional` made mechanical in CI); the scope guard moved out of `backend-test` into a standalone **`ledger-guard`** job gated on `docs/**` + `scripts/check_scope.py` (scope drift lives in docs — the guard must not ride a job that docs-only commits skip). Workflow changes themselves (`.github/workflows/build-arm.yml`) trigger both test filters. **Docs-only ledger commits — the most frequent kind — now run only the ~10 s guard** instead of the full pytest suite + UI build. Manual `workflow_dispatch` gained per-image boolean toggles (`build_backend`/`build_ui`, default true — old both-images behavior preserved) so one ~14-min QEMU image can be dispatched alone; a dispatch forces the matching fast checks since each image build `needs` its green gate (also fixes the would-be trap of a skipped `needs` job silently skipping the image build). Added per-event `concurrency` cancellation (`workflow-event-ref` group: rapid pushes cancel each other's stale fast checks; a push can never cancel a dispatched image build). Docs: `CONTRIBUTING.md` §CI rewritten to the gated-jobs shape; CLAUDE.md `single-task-ledger` scope-guard reference updated (`backend-test` gate → `ledger-guard` job). Verified live on the landing push (touches workflows + docs → all three fast jobs ran green, filters `backend/ui/ledger` all true).

- [x] **OPS-12** `[P1]` — **DONE 2026-07-07** (filed + executed same session, user-requested at the rack pre-REL-2). **Voice-style startup log rollover + fixed the dead rotation cleanup.** Each startup now renames the previous live log aside (`service.log.<YYYYmmdd_HHMMSS>.log` — voice's `_rotate_log_file` behavior) and begins a fresh file; daily rotation kept for permanent controller operation. Deliberate naming choice: the startup rename stays in the **same `service.log.*` family** the daily rotation uses, so VWB-28's evidence collector (`_collect_logs`: live file + newest sibling glob) needed **zero changes** — verified by the reports suite. **Retention bug fixed (found during analysis):** `setup_logging` set a custom `TimedRotatingFileHandler.suffix = "%Y%m%d.log"` without updating `extMatch`, so `getFilesToDelete()` matched nothing and **`backupCount=30` had never deleted a single file** — rotated dailies accumulated unbounded; now `extMatch = ^\d{8}\.log$`. Added `_prune_old_logs` (startup sweep of `service.log.*` older than `LOG_RETENTION_DAYS=30`) covering the startup-renamed files the handler's cleanup can't see. New `tests/unit/test_logging_setup.py` (4 cases: rename + family-naming, missing/empty skip, prune expiry boundaries incl. live-file-never-pruned, fresh-file + extMatch fix). Two-startup smoke run verified live (run-one content lands in the renamed file, fresh live file holds run-two only). Suite 587, pyright 0, contracts 6/6; no OpenAPI/UI impact; `report-a-problem.md`'s "day's service log" wording still true (live + newest sibling spans a restart).
- [x] **OPS-13** `[P2]` — **DONE 2026-07-14** (pulled forward from `[deferred]` by sprint-01 decision 1 — runs before ui-kit so the toolchain migration happens once; PROD-24 (5) fixed the shared target: **eslint-9 flat config**, vite majors per-consumer). **UI dev-toolchain migration: eslint 8 → 9 (flat config) + @typescript-eslint 6 → 8 + vite 5 → 6; absorbs UI-8.** **Config:** `.eslintrc.cjs` → `eslint.config.js`, a faithful translation — same lint scope (ts/tsx only via global ignores; `wb-mqtt-bridge` + plain-JS tooling excluded, as `--ext ts,tsx` had it), same type-aware tuning (no-unsafe-* family off, `no-floating-promises`/`no-misused-promises` kept as errors). **Deps:** `@typescript-eslint/{eslint-plugin,parser}` ^6 → the `typescript-eslint` **^8.64** meta package; `eslint` **^9.39** + `@eslint/js` (NB eslint **10** is already out — pinned to the 9.x line per the ratified shared target) + `globals`; react-hooks plugin ^4.6 → **^5.2** (flat `recommended-latest`, same two rules), react-refresh ^0.4 → ^0.5; **vite ^5.4.21 → ^6.4.3** + `@vitejs/plugin-react` ^4.7 (esbuild rides to 0.25.5); lint scripts drop `--ext`. **New-rule fallout fixed in code, not silenced (26 errors):** 17 custom-icon `interface XProps extends React.SVGProps<SVGSVGElement> {}` → type aliases (`no-empty-object-type`); 7 redundant `as 'material'` assertions auto-removed (`RemoteControlLayout`); `ForceReconcileDialog.fmtValue` rewritten with positive typeof-narrowing — `no-base-to-string` was RIGHT, negative `typeof` doesn't narrow `unknown`; exactly ONE documented inline disable (`useApi` axios interceptor re-rejects the original AxiosError — wrapping it would hide `.response` from downstream). **Verification:** `npm run check` + `npm run build` green (`config-ui-stays-functional`); **dev-server SSE smoke PASSED under vite 6** — mock SSE backend on :8000, `curl -N` through the :3000 proxy: stream unbuffered, both `configure()` `http-proxy` hooks fired, forced headers (`text/event-stream`, `x-accel-buffering: no`) intact — UI-8's declared risk surface; `/api` proxy fine. **Alerts:** `npm audit` 0; minimatch 9.0.3 GONE from the lockfile — the five OPS-7-dismissed alerts (#155/#154/#113 vite, #101 minimatch, #81 esbuild) resolve on the post-push scan. Backend untouched (no Python/pyright/import-linter surface). docs: ui-readme (stack line Vite 5 → 6); CONTRIBUTING's UI-gates + tests-deferred passages verified still accurate (the dead-jest preset was already recorded there — rediscovered, not new).
- [x] **OPS-16** `[P1]` — **DONE 2026-07-11** (third board-as-outbox delegation consumed —
  `locveil-commons` PROD-5 / HK-2 council decision; **redefined at intake with owner approval**: the
  original 2026-07-08 uncommitted-intake text referenced `check_scope.py` (deleted by OPS-22), planned
  a separate drift-guard script (dead — HK-2 put the rule inside scope-guard from scope-v3), and
  expected the slug to split in two (HK-2: rename apart per side); original text annotated in
  `docs/design/productization_bridge.md` §2). **CLAUDE.md harmonization — pinned shared blocks
  adopted.** Executed: `shared-invariants` + `cross-repo-board` blocks inserted byte-identical between
  `locveil:begin/end` markers at `scope-v3`; the duplicated long-form ledger/rotation/guard mechanics
  removed (shared invariants keep dialect-only bullets) — CLAUDE.md **175 → 164 lines**, the HK-2
  net-must-not-grow criterion holds; sovereignty preamble rewritten (two layers: commons-normative
  shared rules as hash-guarded blocks, repo-owned LAW outside); `cross-repo-source-of-truth` rewritten
  (board-as-outbox vs direct operational filings; commons acknowledged as co-owned ground);
  **`config-master-canonical` → `config-master-tree`** (voice takes `config-master-file`; no repo
  keeps the bare slug); `scripts/scope_guard.py` re-pinned at `scope-v3` (1.1.0) + `[claude]` enabled
  in `.scope-guard.toml` with `--hash-blocks` hashes (match commons' pins exactly; tamper test: a
  one-word in-block edit fails with CLAUDE-BLOCK drift, exit 1); CLAUDE.md added to the CI `ledger`
  paths-filter so block edits trigger `ledger-guard`. Consumption noted in the commons board journal
  (bridge ID was pre-written into PROD-5; PROD-5 stays open on voice BUILD-23).

- [x] **OPS-17** `[P1]` — **DONE 2026-07-09** (filed + executed same session; user-flagged from the voice deployment work — "we run the containers as root, which is wrong"; mirrors the voice repo's BUILD-15 uid fix). **Both containers now run non-root as uid 1000 (`domovoy`).** Previously the backend + UI containers ran as **root** — a compromised process could write the bind-mounted host tree as uid 0. **Backend** (`backend/Dockerfile`): `useradd -m -u 1000 domovoy` + `chown -R domovoy:domovoy /app` + `USER domovoy` before the CMD; clean port because the runtime deps already live in `/opt/venv` (world-readable) — only the throwaway builder stage ever touched `/root`. **UI** (`ui/Dockerfile`, `nginx:alpine`): `adduser -D -u 1000 domovoy` (BusyBox — Debian's `useradd` isn't in alpine) + chown of the paths nginx touches at start/run (`/var/cache/nginx`, `/var/log/nginx`, `/etc/nginx` for the entrypoint's envsubst render, `/usr/share/nginx/html` for `runtime-config.js`) + `USER domovoy`; the compiled-in default pid path `/run/nginx.pid` isn't writable by non-root, so `nginx.conf.template` gains `pid /tmp/nginx.pid;`. **`ops/update.sh`**: chowns the writable runtime mounts (`data/`, `logs/`) to `1000:1000` before `docker compose up` (update.sh runs as root on the controller; without this the non-root container would EACCES on the state DB / reports spool / logs). The `:ro config/` mount is left alone (world-readable from the rsync). uid 1000 is unassigned on a stock Wirenboard; on the existing box this migrates the tree's prior `1001` ownership. **The container NAME is provisional** — it tracks the final product-name decision (Domovoy), but the UID is the real cross-boundary identity, so name churn is free. **Verified:** a stage-2-only build of the UI image booted with nginx **master + worker both as `domovoy`**, pid at `/tmp/nginx.pid` (no `/run/nginx.pid`), envsubst render clean, **HTTP 200** served. Backend change is the mechanically-simple half (venv already relocated), matching the proven voice pattern. **Activation owed to the rack:** the change is inert until the images are rebuilt (CI) and `git pull && ./ops/update.sh` runs on the WB7 — update.sh's chown and the new non-root images land together in that one command, so the ordering is safe. `sh -n` + `docker compose config` clean; no backend/Python change (no suite/contract impact). Docs: `ops/INSTALL.md` layout section gains the non-root note.

- [x] **OPS-18** `[P2]` — **DONE 2026-07-19** (REL-5 #11 remediation; low-hanging-fruit sweep). **Partial-startup failure now marks WB virtual cards offline — the OPS-8 shutdown symmetry completed.** Reconciled valid: `_release_partial_startup` released task/devices/MQTT/store but never ran the WB-card offline pass, so a failure after device setup left retained `available=1` cards looking live in the WB UI with the bridge down. Fix: the release helper mirrors the normal shutdown's loop (`cleanup_wb_device_state` per device, individually guarded, before `shutdown_devices`/disconnect — while MQTT is still connected), gated on both `device_manager` and `mqtt_client` being acquired (no client → nothing was published this run and no way to publish markers). 2 new tests in `test_bootstrap_startup_cleanup.py` (offline pass runs and a per-device failure doesn't stop the loop or the remaining releases; no-MQTT skip). Suite 742, pyright 0, import-linter 6/6. docs: none — internal failure-path hygiene; no doc describes partial-startup behavior. contracts: none — no versioned surface touched.
- [x] **OPS-20** `[P1]` — **DONE 2026-07-11** (filed + executed same session; arrived as the commons
  board's FIRST board-as-outbox delegation — `locveil-commons/board/BOARD.md` PROD-2, D-5 mechanism;
  verified per `task-start-reconciliation` before acceptance). **Locveil re-point: this repo is
  `locveil-bridge` now.** The owner locked the product name **Locveil**, claimed the `locveil` GitHub
  org, transferred/renamed all three repos + local dirs; the commons restructured to the D-2 layout
  (`locveil-commons@52126da` — eval framework under `eval/`, distribution `locveil-eval`). This change:
  **(1)** eval/ refs re-pointed to `../../locveil-commons/eval` (framework) — `make cli` 4/4 green from
  the new paths; **(2)** `backend/.venv` REBUILT — the dir rename had broken every console-script
  shebang (absolute paths to `wb-mqtt-bridge/`), exactly as the voice execution warned; `uv sync
  --all-extras` + eval `make setup`; backend suite 698 passed post-rebuild; **(3)** name sweep of
  operative docs/comments (`wb-mqtt-voice`→`locveil-voice`, `eval-commons`→`locveil-commons[/eval]`,
  repo self-references + clone URLs → `locveil/locveil-bridge`; Wirenboard's own `wb-mqtt-serial`/
  `wb-rules` and all frozen history untouched); **(4)** `domovoy`→`locveil` container user in
  `backend/Dockerfile` + `ui/Dockerfile` + the `ops/update.sh`/`ui/nginx.conf.template` comments
  (uid 1000 unchanged — OPS-17 called the name provisional; lands at the next image rebuild);
  **(5)** GHCR pull refs/docs → `ghcr.io/locveil/*` (CI already publishes via
  `github.repository_owner`; **one CI publish must run before the next controller `update.sh`** —
  old `ghcr.io/droman42/*` images stay pullable); **(6)** live deployment identifiers deliberately
  KEPT (image basenames `wb-mqtt-bridge`/`wb-mqtt-ui`, container names, `wb-mqtt-bridge.service`,
  `/mnt/sdcard/wb-mqtt-bridge`, the `wb-mqtt-bridge` distribution + console script, INSTALL.md flow)
  → filed OPS-21, mirroring voice BUILD-29. `docs/design/ui/deployment.md` left unswept (describes the
  legacy split-repo UI flow; audit belongs to OPS-21 or a doc-pruning pass). Local ID written back to
  the commons board (PROD-2 closes: both delegations now carry IDs).

- [x] **OPS-21** `[P1]` — **DONE 2026-07-11 (repo side; controller migration = owner script run; filed +
  executed same day — owner decision: complete the re-pointing down to the metal). Deployment-identity
  rename to Locveil** (OPS-20 residue; coordinated with voice BUILD-29, same session). Renamed in one
  pass: images → `ghcr.io/locveil/locveil-bridge` + `ghcr.io/locveil/locveil-bridge-ui` (build-arm.yml
  jobs + comments; UI name chosen `locveil-bridge-ui` for symmetry with `locveil-voice-ui` — owner pick),
  `container_name: locveil-bridge` / `locveil-bridge-ui`, runtime tree `/mnt/data/mqtt-bridge-config` →
  `/mnt/data/locveil-bridge-config` (compose volumes, update.sh RUNTIME, INSTALL.md), systemd unit →
  `ops/locveil-bridge.service` (git mv + content), clone path `/mnt/sdcard/locveil-bridge`, **Python
  distribution `wb-mqtt-bridge` → `locveil-bridge`** + the alias console script (`locveil-bridge`;
  `wb-api` kept — Wirenboard-domain name, the Dockerfile CMD; import package `wb_mqtt_bridge` and the
  `wb_mqtt_bridge.devices` entry-point group deliberately unchanged — internal, same convention as the
  commons keeping `eval_commons`); uv.lock regenerated; docstring/comment sweep (`backend/openapi.json`
  contains zero old-name strings — no regen needed; ADRs left frozen). **NEW `ops/migrate-to-locveil.sh`**
  (mirror of voice's): retire old unit → down old stack from old tree → mv runtime tree (SQLite state +
  .env intact) → `update.sh` under the new identity → install+enable new unit → drop old-name images.
  Gates: backend suite 698 passed on the renamed distribution, eval `make cli` 4/4, `sh -n`/`compose
  config` clean, check_scope green. Sequencing: CI publish must create + owner must make PUBLIC the new
  GHCR packages before the migration script runs on the WB7.

- [x] **OPS-22** `[P1]` — **DONE 2026-07-11** (filed + executed same session; the second board-as-outbox
  delegation consumed — `locveil-commons` PROD-13 / HK-1 council decision; normative convention
  `locveil-commons/process/ledger-discipline.md`). **Scope-guard cutover: the commons ledger guard
  vendored, `scripts/check_scope.py` retired.** Executed: `scripts/scope_guard.py` vendored +
  `.scope-guard.toml` authored from the PROD-13-verified starter (aliases + tombstones ON); cutover
  proof green BEFORE deleting the old checker; `ledger-guard` job + `ledger` paths-filter re-pointed
  in `build-arm.yml`; committed-hooks mechanism live (`hooks/pre-commit` + `git config core.hooksPath
  hooks` — with `work-on-main` the hook is the only pre-CI gate); `single-task-ledger` /
  `one-active-journal` invariant text updated in the same change (DONE-ledger rotation rule new for
  this repo: watermarks 3000/2000/4000, archived IDs stay in the known-ID set); overdue journal
  rotation executed via `--rotate` as its own commit (1625 → 990 lines, 6 day-sections →
  `docs/archive/journal/2026-06-09_2026-07-04.md`, zero content loss verified line-by-line).
  **Found + fixed upstream:** the first real `--rotate journal` run caught a scope-v1 defect —
  archives written character-per-line + silent truncation of the kept journal (double-indexing after
  tuple unpacking in `rotate_journal`); journal restored from git, fixed in commons (2 lines,
  validated on a copy of this tree), tagged **scope-v2** (1.0.1) — the final pin. v2 also adds the
  explicit `--check` flag the docs already promised (v1 had none; the bare invocation was the check).
  Both PROD-13 delegations re-pointed at scope-v2 (the voice cutover would have hit the same bug on
  its own overdue rotation); OPS-22 written back into the PROD-13 board entry.

- [x] **OPS-23** `[P1]` — **DONE 2026-07-12** (filed + executed same session; PROD-16 bridge delegation (4), council HK-5; convention `../locveil-commons/process/contracts.md` §4 layer 1 — the scope-guard consumption model verbatim). **contract-guard vendored @ `contract-guard-v1` + hook + path-gated CI job — green with ZERO warnings.** `scripts/contract_guard.py` copied byte-identical from the commons tag (sha256-verified against both the tag object and the commons worktree); `hooks/pre-commit` now chains both vendored guards (scope-guard then contract-guard, `--check` only — hooks never mutate); `build-arm.yml` gains a `contracts` paths-filter (`contracts/**` + the vendored script + workflow) and a standalone `contract-guard` job modeled on `ledger-guard` (contracts-only commits must not ride a skippable job), job-graph comment updated. First full run on the finished VWB-29/VWB-40/VWB-41 layout: **0 failures, 0 warnings** — stricter than the commons baseline (their 3 by-design legacy pin warnings await re-pins). scope-guard stays ledger-only; the two tools version independently (re-pin to move).

- [x] **OPS-24** `[P1]` — **DONE 2026-07-12** (filed + executed same session; PROD-17 bridge delegation (4), council HK-6; the `process/claude-md.md` §3 re-pin flow, landed LAST as planned — the verdict rule presupposes the DOC-13 manifest). **scope-guard re-pinned @ `scope-v5` (1.2.0) + the reshaped `shared-invariants` block + `docs_verdict_since`.** **(a)** `scripts/scope_guard.py` byte-identical to the commons tag (sha256-verified against tag object + worktree); 1.1.0 → 1.2.0 = the docs-verdict presence rule (ledger lane only — manifest-aware checks stay in the DOC-13 coherence test, the HK-6 lane separation). **(b)** The `shared-invariants` CLAUDE.md block re-pinned from the commons source (marker `scope-v3` → `scope-v5`): the block now names `user-docs.md` as a companion and carries the org-wide **`user-facing-docs-are-done`** invariant — the bridge-local dialect bullet (rewritten by DOC-13 to point at the manifest) is now formally the dialect of a shared invariant, dissolving the name collision by design. `.scope-guard.toml` hash updated via `--hash-blocks`; `cross-repo-board` untouched at scope-v4. **(c)** `docs_verdict_since = "2026-07-13"` — the guard bites from tomorrow's completions; today's frozen PROD-15/16 entries predate the manifest and stay untouched (the PROD-17 entries carry verdicts voluntarily — four are already in this file). CI comment re-truthed (`ledger-guard` job note: scope-v5 since OPS-24; the paths filter already covered the script/config/CLAUDE.md). Guard 1.2.0 run bare: green. **PROD-17's bridge delegation is fully consumed** (DOC-13/DOC-14/DOC-15/OPS-24). docs: none — guard/config/CLAUDE.md changes; no manifest node describes them.

- [x] **OPS-25** `[P2]` — **DONE 2026-07-13** (evidence: `docs/review/emotiva_wedge_20260712.md` finding 3; plan owner-approved — option A + the retention decision). **Production log hygiene: INFO root + pinned library logger + structurally silent keepAlive + 2-day retention.** **(a) Levels:** `system.json` `log_level` DEBUG→**INFO**; the EXISTING per-logger `loggers` map (already quieting pyatv/upnp — `pymotivaxmc2` had simply never joined it, so no model change was needed) gains `"pymotivaxmc2": "WARNING"` — a future forensic root-DEBUG flip no longer reopens the 68 k-line/day library firehose unless deliberately unpinned; `OVERRIDE_LOG_LEVEL` env override untouched. **(b) keepAlive:** the driver's two per-beat `[EMOTIVA_DEBUG]` lines (hardware callback + property-change) early-exit for the stateless `keepalive` property — **zero lines per beat even at full DEBUG** (~11.5 k beats/day; the watchdog's INFO/ERROR transitions carry the life/death evidence, its timing bookkeeping untouched); locked by a caplog test (beat = no records, a real property change still logs). **(c) Forensic tags:** all 34 `[EMOTIVA_DEBUG]/[MQTT_DEBUG]/[SCENARIO_DEBUG]/[BASE_DEVICE_DEBUG]` sites KEPT at DEBUG (deliberate flip-on instrumentation, free at INFO); the raw `print("DEBUG: …")` boot diagnostic in `bootstrap.py` deleted (it bypassed logging into docker stdout on every start). **(d) Retention (owner decision):** `LOG_RETENTION_DAYS` 30→**1** — today + yesterday only (cross-midnight analysis stays possible; the sdcard pays for every retained MB); container stdout stays capped by json-file 10m×3. Measured effect on the 2026-07-12 corpus: 138 k lines / 20 MB → ~1.5 k lines (≈300 KB) at the new levels. Deployment: reaches the WB7 at the next `git pull` + `update.sh` + restart (`config-master-tree`). Suite 724→**725**, pyright 0, import-linter 6/6, zero contract drift. docs: none — README's config example already shows `log_level: "INFO"` (now true in the tree); no manifest node teaches retention or logger pins.

- [x] **OPS-26** `[P1]` — **DONE 2026-07-13** (board delegation **PROD-21**, council **HK-8**; `../locveil-commons/process/python-layout.md` §3 — wire/deployed identifiers are deployment identity). **`meta/driver` wire-identity cutover `wb_mqtt_bridge` → `locveil-bridge`.** Owner-gate lifted by the user; the LAST of the PROD-21 bridge share. A **separate, separately-revertible commit** (never bundled into CORE-10/11 — the ONLY task allowed to touch the retained `meta/driver` string). Reconciliation confirmed the delegation's model against the code: `driver_name` flows ONLY into the in-memory `_active_devices` dict and the `_publish_wb_device_meta` publish (`meta.driver`, `retain=True, qos=1`) — it is **never a persisted-state key** and no doc/wb-rules/config states the value, so the flip is a pure **republish-in-place** (no broker migration, no topic-content contract change). Changed the two default literals — `wb_device/service.py:77` (`driver_name: str = …`) and `devices/base.py:122` (the production caller, `driver_name=…`) — from `"wb_mqtt_bridge"` to `"locveil-bridge"` (hyphen, matching the image/dist/product name). **Functionally verified locally:** driving the real `_publish_wb_device_meta` path with the default now publishes `/devices/<id>/meta` with `meta.driver == "locveil-bridge"` (`retain=True, qos=1`). The live retained topic flips to `locveil-bridge` on the **next WB7 image deploy** — the bridge republishes device meta at startup (`git pull` + `update.sh` + restart); the only visible effect is one string in the WB device UI, reverted by reverting this commit. Gates: import-linter 6/6, pyright 0/0/0, pytest **725 passed** (`test_wb_virtual_device_service`/`test_wb_rekey` green — the existing meta test passes its own `driver_name`, unaffected). docs: none — no manifest node states the `meta/driver` value; it is deployment identity, not a documented contract surface.

- [x] **OPS-27** `[P2]` — **DONE 2026-07-14** (board delegation **PROD-22**, executed by the
  commons session on owner instruction). **Re-vendor contract-guard @ `contract-guard-v2`** —
  the rule the bridge itself requested after catching the catalog-v1.7 false green:
  `TAG-MISSING`, an owned STAMP naming a git tag that doesn't exist now FAILS. Vendored
  byte-identical to the tag (1.1.0). **And the rule fired immediately on THIS repo:**
  `contracts/docs-manifest/STAMP.json` named `docs-manifest-v1` with no tag behind it — the
  second instance of the exact class PROD-22 was filed about; tag created at `3592282` (the
  commit that landed the STAMP), check now green, 0 warnings. docs: none — vendored tool only;
  CONTRIBUTING's contract-guard text is version-agnostic.
- [x] **OPS-29** `[P2]` — **DONE 2026-07-15.** **Forensic logging middle ground: the load-bearing eMotiva transitions survive INFO** (wedge #3 finding 5 — OPS-25's hygiene blinded the `source → arc` claim and the power transitions at the 2026-07-14 incident). Driver-side: `_handle_property_change` logs **device-reported transitions** of `power` / `zone2_power` / `input_source` at INFO (`"{name}: {field} old -> new (device-reported)"`) — only on actual value change, so it's a handful of lines per scenario switch; keepAlive stays fully silent and non-forensic properties (volume etc.) gain nothing. The ARC grab — the readiness gate's trigger condition — is now visible in a production log. Ops-side: `ops/INSTALL.md` gains the **"Full protocol forensics"** section — the deliberate temporary flip-on (runtime `system.json`: root `DEBUG` + unpin `pymotivaxmc2` from the loggers map, restart, reproduce, **revert**; ~20 MB/day, and `update.sh` re-syncs the repo copy over the edits anyway). Test: caplog — source/power transitions INFO-visible, same-value non-transitions quiet, keepAlive silent at INFO, volume gains no INFO line. Suite 728, pyright 0, import-linter 6/6. docs: install (the forensics section; the manifest has no dedicated logging node — verified).
- [x] **OPS-30** `[P2]` — **DONE 2026-07-15** (filed + executed same day; surfaced by the first
  post-OPS-27 `workflow_dispatch`). **Contract-guard CI job gets tags: `fetch-tags: true` on its
  checkout.** The OPS-27 `TAG-MISSING` rule resolves STAMP tags via `git tag -l`, but the CI job's
  bare `actions/checkout@v6` produces a tag-less shallow clone — the check could never pass in CI
  (all three STAMP tags exist locally AND on origin; nothing was wrong with the contracts). Latent
  since OPS-27 landed: the very push that vendored contract-guard-v2 already failed this job in CI
  (run 29317709478, 2026-07-14, unnoticed), every later push skipped it via the `contracts` path
  filter, and the 2026-07-15 backend-image dispatch (which forces all gates) re-exposed it. Fix is
  workflow-only — the vendored guard is untouched (commons-owned; and its logic is correct — the
  checkout just has to provide tags). Verified: guard green locally at the same commit; the fix
  push itself re-runs the job (the `contracts` filter includes the workflow file). docs: none —
  CI job internals; no manifest node describes workflow mechanics.

- [x] **OPS-31** `[P2]` — **DONE 2026-07-15** (filed + executed same day; commons **scope-v6** /
  IMPL-2 published — no board delegation, consumers adopt at re-pin). **scope-guard re-pinned @
  `scope-v6` (1.3.0) — the UNREFERENCED-evidence rule (HK-10, the fourth anchoring direction).**
  **(a)** `scripts/scope_guard.py` byte-identical to the commons tag (`cmp` against
  `scope-v6:packages/scope-guard/scope_guard.py`); 1.2.0 → 1.3.0 = a doc on disk under an
  `evidence.dirs` root that no active/DONE entry names is flagged as forgotten scope (referenced =
  its repo-relative path OR basename appears in the ledgers). **(b)** `.scope-guard.toml`: new
  `[evidence] unreferenced = "warn"` — the consumer default (commons runs `error`); `dirs` already
  carried `docs/review` since OPS-24, so the HK-10 evidence-dirs widening was a no-op here. **(c)**
  The CLAUDE.md pinned blocks are untouched: the `shared-invariants` / `cross-repo-board` block
  *sources* (`process/claude-blocks/`) are byte-unchanged between scope-v5 and scope-v6, so the
  markers stay `scope-v5`/`scope-v4` and the `[claude.blocks]` hashes still match — a script-only
  re-pin. **(d)** First green run surfaced **four** pre-existing unreferenced evidence docs
  (`docs/design/conditioner/mitsubishi.md`, `docs/design/ui/deployment-network-config.md`, plus two
  since retired to `docs/archive/` by DOC-18 — the remote-layout spec and the wb-alice-bridge brief)
  — discovered forgotten scope, filed as **DOC-18** (naming them anchors them; the reconcile decides
  keep-and-reference vs retire). Guard 1.3.0 run bare: green (0 warnings once DOC-18 anchors the four). CI comment
  re-truthed (`ledger-guard`: scope-v6 since OPS-31). docs: none — guard/config/CI internals; no
  manifest node describes them.

- [x] **OPS-32** `[P1]` — **DONE 2026-07-18** (filed + executed same day; PROD-26 bridge
  delegation (1), HK-12 execution; guard + block halves as ONE commit per the keepers' round-1
  condition). **Guard + block sweep: scope-guard v6 → v7.1, contract-guard v1 → v3, the
  contract-triad block pinned, contracts-verdict cutover.** **(a)** `scripts/scope_guard.py`
  byte-identical to `scope-v7.1` (1.3.0 → 1.4.0: the CONTRACTS-VERDICT presence rule +
  UNKNOWN-PREFIX; v7.1 is the additive block release — script bytes == v7). **(b)**
  `scripts/contract_guard.py` byte-identical to `contract-guard-v3` (1.1.0 → 3.0.0: ORPHAN-TAG,
  CONTENT-DRIFT for `artifacts`-carrying STAMPs, VENDORABLE-UNREGISTERED, `--relax-tags`; script
  major tracks the tag family from v3). **(c)** NEW `.contract-guard.toml` — `vendorable_roots =
  []` EXPLICIT (the keeper's recorded posture: no heuristic package-manifest scanning; a dir
  becomes a surface only by deliberate registry row + STAMP). **(d)** `.scope-guard.toml`:
  `contracts_verdict_since = "2026-07-18"` (ledger-discipline §7 cutover; earlier entries frozen)
  + the third `[[claude.blocks]]` hash. **(e)** CLAUDE.md pins **`contract-triad`** @ scope-v7.1
  between fresh markers — `a3fe8d6b…d1fc2`, byte-identical to the commons source and to the
  voice/commons pins; the two existing blocks verified byte-current (sources unchanged since
  scope-v5/scope-v4), so this stayed a one-block adoption. **(f)** `hooks/pre-commit`:
  contract-guard gains `--relax-tags` (a bump commit cannot carry its own tag — warns locally, CI
  strict). **(g)** CI: guard-job comments re-truthed (scope-v7.1 / contract-guard-v3 since
  OPS-32), `.contract-guard.toml` joins the contracts path filter, and a belt `git fetch --tags`
  step (the shallow-SHA checkout path has been seen to deliver no tags — voice BUILD-41 finding;
  v3's tag rules make that fatal). Both guards run bare post-sweep: green — and the first v3 run
  DISCOVERED that the catalog STAMP's pre-v3 `artifacts` entries are bare names where v3 expects
  repo-root-relative paths (2 CONTENT-UNVERIFIABLE warnings + a latent root-README false-drift
  trap) — filed as **VWB-43**, fix rides the next catalog cut (a STAMP is tag bytes).
  docs: none — enforcement tooling + process pins; no manifest node describes them.
  contracts: scope + contract-guard consumed-tool pins bumped v6→v7.1 / v1→v3; contract-triad
  block FIRST CONSUMED (pinned into CLAUDE.md + hashed); the `[[tool]]` manifest rows recording
  these tags land with OPS-33's `.repin.toml`.

- [x] **OPS-33** `[P1]` — **DONE 2026-07-18** (filed + executed same day; PROD-26 bridge
  delegation (2), sequenced after OPS-32 so the manifest records the post-sweep tags). **repin
  adopted @ `repin-v1` — `.repin.toml` becomes the family registry.** **(a)**
  `scripts/repin.py` vendored byte-identical to the commons tag (registry-keyed newest-tag
  parser, fetch-at-tag + sha256 + PIN.json writer, remote-first tokenless `ls-remote` with
  offline sibling fallback). **(b)** `.repin.toml`: family **`report-protocol`** (owner
  locveil-commons; dest `contracts/pins/report-protocol`, conformance
  `backend/tests/unit/test_report_protocol_pin.py`) + the `[[tool]]` vendored-tools manifest
  (scope-guard @ scope-v7.1, contract-guard @ contract-guard-v3, repin @ repin-v1 — the
  tag↔vendored-copy relationship stops living in prose). Owned families (catalog,
  device-integration, docs-manifest) deliberately absent — repin tracks consumption. The
  **workbench** family joins when commons ships its machine schemas at the next workbench bump
  (declared now it would nag never-pinned on prose-only artifacts; recorded in the config
  header). **(c)** `hooks/pre-commit` warn stage (`--check --fail-on none || true` — offline-safe,
  never bricks a commit). **(d)** Ordinary CI: a `--fail-on major` step in the contract-guard job
  (§5 ladder); `scripts/repin.py` + `.repin.toml` join the contracts path filter; release flows
  run `--fail-on any` manually (no standing release workflow to wire yet). **(e)** First
  `--check --fail-on any` run: ALL GREEN — report-protocol current @ v1, all three tools current
  at their pinned tags. docs: contracts-registry (staleness paragraph added to the registry
  README, same change). contracts: repin FIRST CONSUMED as a vendored tool @ repin-v1;
  `.repin.toml` becomes the family registry (no pin content moved — everything verified current).

- [x] **OPS-34** `[P1]` — **DONE 2026-07-18** (filed + executed same day; PROD-26 bridge
  delegation (4), HK-12 round-2 finding — owed regardless of the contract question).
  **`workbench-plugin/` gets CI coverage: gated `workbench-plugin-validate` job (typecheck +
  build).** The plugin consumes the commons `ui-kit` + `workbench` packages as `file:` deps two
  levels up, so the job lays out **bridge + commons as sibling checkouts inside the workspace**
  (both org repos public — tokenless), builds `ui-kit` first (`npm ci && npm run build` — its
  package `exports` point at an uncommitted `dist/`; the workbench package needs no build,
  `./contract` types resolve from `src/`), then `npm ci` + `gen:api-types` + `tsc` + `vite build`
  in the plugin. New `workbench_plugin` path filter (`workbench-plugin/**` +
  `backend/openapi.json` — the plugin consumes the OpenAPI contract like `ui/` does + the
  workflow file); npm cache keyed on both lockfiles (both committed). Verified locally before
  wiring: typecheck + build green, `gen:api-types` deterministic (committed `openapi.gen.ts`
  byte-unchanged after regen); workflow YAML parse-checked (an unquoted `file:` colon in a step
  name was caught by the check). First LIVE run then caught what the local run couldn't: `tsc`
  follows the `file:` link into `workbench/src/contract.ts`, which imports React types —
  resolvable on the dev box (sibling `node_modules` present), absent in a bare CI checkout;
  fixed same day with an `npm ci` step in the workbench package (no build — types only), after
  which the job is green end-to-end. Commons rides its default branch in this job until the
  workbench contract pin exists (the OPS-33-recorded next-workbench-bump hook). docs: none — CI
  wiring only; no manifest node describes the plugin build. contracts: none — coverage for an
  existing consumer; the workbench family's pin (first consumption on record) lands with the
  commons machine-schema bump per OPS-33.

- [x] **OPS-36** `[P1]` — **DONE 2026-07-18** (filed + executed same day; commons IMPL-8
  published `contract-guard-v3.1`, owner go-ahead — no board delegation, consumers adopt at
  re-vendor; the OPS-31 pattern). **contract-guard re-vendored @ `contract-guard-v3.1`
  (3.0.0 → 3.1.0 — the ARTIFACTS-PATH rule).** v3.1 adds exactly one rule (IMPL-8, owner ruling
  Option B): every STAMP `artifacts` entry MUST be repo-root-relative and resolve to a file at
  HEAD — bare names now FAIL instead of silently corrupting the drift comparison (the VWB-43
  trap, promoted from bridge scar to org-wide guard); the legacy singular `artifact` field stays
  informational, never validated. **Sequencing-free by construction:** VWB-43 landed first, so
  the Option B rollout coupling ("don't re-vendor v3.1 while the catalog STAMP has bare names")
  had already evaporated — verified on the first strict run: **0 warnings, 0 failures** (catalog
  v1.8 + device-integration v1.1 root-relative; docs-manifest carries only legacy `artifact`).
  `.repin.toml` `[[tool]]` pinned_tag → contract-guard-v3.1 (the nag the manifest existed to
  raise, discharged the day it appeared); CI comment re-truthed. Hook/CI invocations unchanged
  (`--relax-tags` local, strict CI). docs: none — enforcement tooling.
  contracts: contract-guard consumed-tool pin bumped v3→v3.1 (no pin content moved).
- [x] **OPS-37** `[P2]` — **DONE 2026-07-19** (filed during the low-hanging-fruit sweep off the
  repin `[[tool]]` staleness warning; executed next session on owner go-ahead — the OPS-31/OPS-36
  pattern). **scope-guard re-vendored @ `scope-v7.2` (1.4.0 → 1.4.1).** The v7.1→v7.2 diff is a
  single rotation-parser fix (commons IMPL-9): the `DATED` regex demanded the date flush against
  the `#`s, so `##`-heading-style journals parsed as ZERO dated sections and `--rotate` refused —
  the pattern now allows whitespace after the heading marks. No new check rules, no pinned-block
  or hashing changes (the CLAUDE.md block markers keep their `scope-v7.1` provenance — block
  content is unchanged between the tags). Bridge exposure: none today (our journal is
  bullet-style, which always parsed), but future rotations are covered either way. Vendored
  byte-identical from the tag (sha `294cf411…`), first run green (1.4.1, all block hashes hold);
  `.repin.toml` `[[tool]]` pinned_tag → scope-v7.2 (`repin --check --fail-on any` fully green —
  every pin + all three tools current); `.scope-guard.toml` header + CI comment re-truthed.
  Hook/CI invocations unchanged. docs: none — enforcement tooling.
  contracts: scope-guard consumed-tool pin bumped v7.1→v7.2 (no pin content moved).

## CORE — Backend core / architecture

- [x] **CORE-1** `[P2]` `[deferred]` `HW-GATED` — **System-router adapter cleanup — Item A only (Item B DONE 2026-05-26).**
  **CODE HALF LANDED 2026-07-19 (owner directive: implement now, close only after the device test).**
  Shipped: `app/reload_service.py` (`ReloadService.reload()` = the former router background task,
  verbatim sequence); the system router only schedules it (`set_reload_service` wiring seam); the
  **`ignore_imports` exception is DELETED** — 6/6 contracts pass with an EMPTY exception list;
  `overview.md`/`interfaces.md`/`pyproject` comment/CLAUDE.md dialect re-truthed. The extraction also
  closed four latent reload gaps found at reconciliation: (1) the replacement client is built by a
  bootstrap factory IDENTICAL to boot (maintenance guard + traffic observer — the inline version
  silently lost both); (2) the rewire hook re-points ALL router globals (devices/mqtt/scenarios kept
  publishing via the STOPPED old client); (3) the VWB-32 on-connect catalog publish is re-registered
  on the new client; (4) a fresh `WBVirtualDeviceService` rides the swap (the old one was bound to the
  dead client). Dead write dropped: `device_manager.config_manager = …` assigned an attribute
  `DeviceManager` never declares or reads. 5 new tests (`test_reload_service.py`); suite 747, pyright
  0, openapi/golden byte-identical. **Residual RETIRED same day (owner directive, pre-bench):** gap
  (5) — `ScenarioWBAdapter` held the boot client + boot WB service — closed by a
  `rebuild_scenario_cards` hook the service fires once the new client is CONNECTED (startup's
  ordering): bootstrap builds a fresh adapter over the new composition and runs `setup()`, which
  republishes the cards, re-subscribes their command topics, and re-points
  `scenario_manager.on_active_changed` at the new adapter (the assignment displaces the old hook; the
  old adapter is unreferenced and collects). Connection-timeout path skips it, same as boot.
  **REMAINING (the HW gate): rack verify `POST /reload`** — reconnects cleanly against the live
  broker, devices command OK after, WB cards + **scenario «Сценарии» cards** + catalog version topic
  correct. Close only on owner confirmation. **UNBLOCKED 2026-08-02: the CORE-14 deploy put the
  ReloadService code live on the WB7** (the old image's inline reload was caught live 2026-08-02
  restoring no cards — exactly gap (4); that failure mode is what this verify now checks against).
  NB the 2026-08-02 live `/reload` attempt ran on the OLD image and does NOT count — run it fresh
  on the deployed image. **VERIFY RAN 2026-08-04 — mechanics PASS, one gate item FAILS on a
  pre-existing bug now tracked as CORE-15.** PASS: ReloadService drove the full sequence against
  the live broker (reconnect clean, all 14 WB cards + the `scenario_manager_living_room` card
  rebuilt, catalog republished, zero reload-path errors). FAIL: "catalog version topic correct" —
  the reload published `95e24c…` not the golden, because `/reload` never re-attaches capability
  maps (NOT a CORE-1 regression: the verbatim extraction faithfully preserved the old inline
  path's omission — the OLD image produced the identical hash 2026-08-02). Post-reload the fleet
  ran capability-less until a heal restart (verified restored: golden + 157 capabilities). Close
  decision = owner's: the extraction itself verified clean; the failing item re-verifies with
  CORE-15's fix (LANDED 2026-08-04 — re-run the reload verify at the next deploy: catalog must
  stay golden across `/reload`). Item A: `POST /reload`'s `reload_system_task` constructs + drives a concrete `MQTTClient` inline; extract an application-layer reload service (e.g. `app/reload_service.py`) so the router stays a thin adapter. **Gated on hardware** — touches the live MQTT-reconnect path; can't be safely HW-verified without you at the rack. **Completion goal = 100% clean hexagon (explicit, added 2026-07-07):** this task owns the **only** `ignore_imports` exception in the import-linter config (`presentation.api.routers.system -> infrastructure.mqtt.client`, backend `pyproject.toml`); done means (1) the reload service extracted and the back-edge gone from the code, (2) the **`ignore_imports` entry deleted** — the contract set (6 since CORE-6) passes with **zero exceptions**, (3) the "one documented exception" passages updated in `docs/architecture/overview.md` + the contract name/comment in `pyproject.toml` + the [[hexagonal-layering]] memory, (4) HW-verified at the rack: `POST /reload` still reconnects cleanly against the live broker. Item B (response DTO for `/config/system`) done in `73ee8d5` — new presentation `SystemConfigResponse` + nested DTOs; wire shape field-identical; `presentation/api/schemas.py` no longer imports the infra `SystemConfig`.
  **CLOSED 2026-08-04 — the rack verify re-ran on the CORE-15-fixed image and EVERY gate
  item passed:** ReloadService sequence clean against the live broker; all 14 WB cards + the
  «Сценарии» card (`scenario_manager_living_room`) rebuilt; capability maps survive (157
  capabilities, canonical dispatch functional); **catalog version stays golden
  `5622ba7a1a78102a`** (live API + retained topic — the item that failed the morning verify,
  cleared by CORE-15); rooms (11) + scenarios (9) + topology (16 links / 6 ordering edges)
  reload in the same pass.
  docs: none — completion is verify-only; overview/interfaces/pyproject/CLAUDE.md were
  re-truthed at the code-half landing.
  contracts: none — no surface moved; golden byte-identical throughout.

- [x] **CORE-2** `[P1]` — **DONE 2026-07-04** (filed + executed same day; the acceptance-gate item-4 dead-code sweep, scoped against live code). **Removed:** the legacy imperative scenario path — `Scenario.initialize/execute_startup_sequence/execute_shutdown_sequence`, the shared-device `switch_scenario` legacy branch, the string-condition evaluator (`_evaluate_condition`/`_safe_evaluate_condition`/`_parse_condition_value`), `_validate_parameters` + the sequence/condition validators, `_is_power_command` (~330 lines out of `scenario.py`); the **`WB_SCENARIO_RECONCILER` kill-switch** (`switch_scenario`/`deactivate` are reconciler-only now; the already-active early return aligned to the reconciler result shape `{success, powered_off, failures}` — no consumer read the old keys); the **`CommandStep` model + `ScenarioDefinition.startup_sequence`/`shutdown_sequence` fields** (contract regenerated: `openapi.json` −76 lines, `CommandStep` schema gone; UI types regenerated, dead `CommandStep` alias dropped from `ui/src/types/api.ts`; `npm run check` + build green); the **vestigial `DeviceState.output`** field (scenarios model — **correction to the filing text**, which wrongly declared it already-gone after grepping only the devices models; no device state ever had an `output` field, so it was permanently `None`); the **Phase-B `log_migration_guidance()` shim**. **Added guard:** `validate_configuration` now requires a thin `source` selection (a sourceless scenario could never activate; rejected at load with the Bug-2 non-fatal skip). `_convert_device_state` uses `Scenario._safe_get_device_field` as a `@staticmethod` (temp-instance fallback dance removed). **Narrowed (not removed):** the wb_device **capability-less classification path** — the filing text called it the "`group` transitional fallback", but reconciliation showed the config `group` *field* is already extinct (no model defines it, no config carries it, nothing reads it); what remains is the `capabilities=None` branch, which is **live** for `kitchen_hood` (no capability map — the gate-item-1 coverage gap; mapping it is capability/catalog work for the DRV-1 kitchen_hood row or VWB-13, not dead-code removal) **and** for `_build_state_field_to_control_map`, which enumerates controls without capability context for every device. Stale "legacy config group" docstrings corrected in place; `_DOMAIN_GROUP_ALIAS` + the group-vocabulary heuristics stay (live, domain-keyed). **Tests:** legacy-path tests removed / rewritten to thin fixtures (`test_scenario.py`, `test_scenario_models.py`, `test_scenario_manager.py` — manager tests now assert manager-level behavior, transition content stays with `test_scenario_switch_reconciler.py`); new coverage: sourceless-scenario rejection. Suite 487 passing (was 502 — the delta is deleted legacy tests); import contracts 3/3; docs updated (`key-concepts.md` + `devices-and-scenarios.md` legacy-path passages removed). **Left for their owners:** `MQTTClient.stop()/start()` shims → CORE-1; piwheels `pip.conf` → OPS-11; gate item 4's "thorough code review" half + final contract audit remain part of the acceptance-gate pass itself.

- [x] **CORE-3** `[P1]` — **DONE 2026-07-05** (filed + executed same day, off a live-controller diagnosis session — user request "fix the guard here"). **Maintenance-guard fix: survive the real midnight wb-rules restart burst.** Diagnosis (SSH to the WB7 + journal + broker evidence): the midnight event is the user's own root crontab (`0 0 * * * systemctl restart wb-rules`); the restart burst spans ~00:00:05 (driver ready, `meta/driver` republished) through 00:00:10+ (rule files loading, virtual devices + values republished, rule-startup side-effect writes) — the old **fixed 5s window closed mid-burst**; additionally the retained trigger copy **opened a bogus window at every bridge connect** (log-proven 2026-06-06: trigger 1ms after subscribe ate reel_to_reel startup topics), and the client's retained-skip can't substitute because live republishes arrive retain=0 ([MQTT-3.3.1-9]). **Fix (`infrastructure/maintenance/wirenboard_guard.py` + `mqtt/client.py`):** `maintenance_started(topic, retain)` — the client now passes `message.retain`; only a **live (retain=0)** publish of the trigger topic opens a window (the subscribe-time retained replay never does); the window is **sliding** (`duration` = quiet-time in seconds; every in-window message extends it) with a `MAX_WINDOW_S = 60.0` hard cap so a periodic publisher on a subscribed topic can't hold it open forever. Docstrings de-lied (the old ones claimed `meta/online` + `warmup_s`); `MaintenanceConfig.duration` description corrected ("minutes" → seconds/quiet-time semantics — infra-model only, DTO untouched, **no OpenAPI/contract churn**). `config/system.json` keeps `duration: 5` — now meaning "close after 5 quiet seconds," which covers the measured burst with the cap as backstop. **Tests:** new `tests/unit/test_maintenance_guard.py` (9 cases, fake `time.monotonic`: retained-trigger-ignored, live-trigger-opens, quiet-close, activity-extension, hard-cap, mid-window re-arm, retained-mid-window, no-window passthrough, subscription topics). Suite 531; pyright 0; contracts 3/3. **Controller-side (user-owned, out of repo scope):** delete the broken `enable_online_meta_for_wbrules` rule in `/etc/wb-rules/01_initial.js` (`@reboot` unsupported → error spam every load); telegram2wb curl-35 failures + IR_Trainer datatype error noted in passing.

- [x] **CORE-6** `[P1]` — **DONE 2026-07-07** (filed + executed same session, off the chat-requested voice-vs-bridge enforcement comparison). **Import-linter parity with the voice repo + the `domain ⇄ utils` cycle broken.** The comparison found identical gate machinery (`py-dev-gates@v0.1.1`: import-linter + no-`TYPE_CHECKING` + pyright) but 3 contracts here vs voice's 11 — plus one live violation of the unenforced foundation rule: `utils/types.py` + `utils/validation.py` imported `domain.*` while `domain/ports.py` + `domain/devices/service.py` imported `utils`, a package-level cycle that made `overview.md`'s "utils depends on nothing upward" claim untrue. **Relocations (git mv, imports rewritten at 14 sites incl. 2 tests + the howto-new-driver guide):** `utils/types.py` (pure domain vocabulary — `CommandResult`, `CommandResponse`, `StateT`, `ActionHandler`) → `domain/devices/types.py`; `utils/validation.py` (config-file validation, sole importer `infrastructure/config/manager.py`) → `infrastructure/config/validation.py`. **Contracts 3 → 6:** the infrastructure contract extended to also forbid `app`/`cli`; new "Presentation does not import the composition root or the CLI"; new "Utils (foundation) depends on nothing upward" (voice ARCH-12 analog; canary-verified to fire); new `independence` contract across the 8 device-driver packages (voice ARCH-4 analog). **Assessed non-goals (recorded in the contract comments + filing):** seam-pinning contracts (the bridge's chokepoints are behavioral, locked by tests — [[state-sync-chokepoint]]) and a port-purity contract (the domain rule carries no ignores, so ports are already covered). Gates: contracts **6/6 kept**, suite 583, pyright 0, OpenAPI byte-identical (no UI/contract churn).
- [x] **CORE-7** `[P2]` — **DONE 2026-07-18** (filed 2026-07-08; scope narrowed at intake
  2026-07-16 to the PROD-8 council decisions; gate "PROD-8 / core-py exists" DISCHARGED by
  voice's 2026-07-18 write-back — ARCH-58 closed the voice half; executed same day on owner
  go-ahead). **The core-py entry-point registry adopted for the driver axis — the estate's
  first vendored RUNTIME code bridge-side (consumer #2).** **(a) Pin, taken BY THE VENDORED
  REPIN TOOL** (dogfooding OPS-33): `.repin.toml` gained the `core-py` family and
  `scripts/repin.py core-py` wrote `contracts/pins/core-py/` (artifact + owner STAMP verbatim +
  strict PIN.json — hashes bit-identical to voice's pin) at **`core-py-v1.1`, NOT v1** (the v1
  tree predates its STAMP, pins-complete-and-verbatim cannot hold against it — the board's
  consumer note, honored). Importable copy `utils/entry_point_loader.py` byte-identical; NEW
  `tests/unit/test_core_py_pin_identity.py` (4 tests, the voice ARCH-58 shape): runtime copy ==
  pin, pin == PIN.json sha256s, PIN/STAMP coherence, singleton serves the pinned class.
  Registry row added. **(b) The swap** (per §5 of
  `locveil-voice/docs/design/core_py_loader_extraction.md`): singleton in NEW
  `utils/entry_points.py` (utils = foundation — `domain/` imports it like
  `serialization_utils`; the loader never lives in `domain/`, **zero new import-linter
  exceptions**); `load_device_modules` = `clear_cache()` (fresh discovery per call — startup,
  `POST /reload`, device-test CLI keep pre-swap semantics) + `discover_providers
  ("locveil_bridge.devices", base_class=DevicePort)` — the hand-rolled issubclass check became
  the engine's native rejection path, and `get_discovery_failures` per-entry reasons are now
  logged (reporting the old loop didn't have; groundwork for UI-20's "surface" option). The
  registry re-keys by CLASS name (configs carry `device_class: "LgTv"`, the engine keys by
  entry-point name — the one preserved subtlety). The py<3.8 `importlib_metadata` fallback
  import deleted. **(c) Fold:** dead `validate_class_references` removed
  (`infrastructure/config/validation.py` — zero call sites, re-verified at execution) + its
  now-unused imports; `utils/class_loader.py` UNTOUCHED (council: stays local, no second
  consumer). **(d) Verified live, not just by suite:** discovery driven through the singleton —
  all **9 drivers, 0 failures**, EP→class map checked; `locveil-catalog --stdout` → **79
  devices, golden `5622ba7a1a78102a`** (the no-drift condition observed, not assumed). Suite
  734→**738**, pyright 0, import-linter **6/6**, `repin --check --fail-on any` green.
  Follow-ups filed same sitting off the 2026-07-18 findings analysis: **CORE-13**
  (config-driven activation) + **OPS-35** (per-driver extras / slim images).
  docs: none — internal infra swap; no manifest node describes driver discovery.
  contracts: core-py FIRST CONSUMED @ `core-py-v1.1` (strict pin + `.repin.toml` family; the
  estate's first vendored runtime code bridge-side).

- [x] **CORE-9** `[P0]` — **DONE 2026-07-09** (REL-5 #2 remediation, same session as the review). **MQTT reconnect budget is now per-disconnect-episode, not per-process-lifetime.** `infrastructure/mqtt/client.py` `_run_mqtt_client` accumulated `retry_count` across the whole process life, so five *separate* transient drops over a long-running controller's life (broker restarts, Wi-Fi blips — each self-healing) exhausted `max_retries=5` and the loop permanently gave up on MQTT, leaving the house uncontrollable until a manual restart. Fix (2 lines + comment): reset `retry_count = 0` immediately after a successful connect (right after `self._connection_event.set()`), so `max_retries` only bounds retries within a single failed-to-connect episode. New `tests/unit/test_mqtt_reconnect.py` drives `_run_mqtt_client` with a mock `Client` that connects-then-drops 8 times (> max_retries) and asserts the loop keeps reconnecting (9 connect episodes) instead of giving up at 5. pyright 0, import-linter 6/6, mqtt tests green.

- [x] **CORE-10** `[P1]` — **DONE 2026-07-13** (board delegation **PROD-21**, council **HK-8**; normative `../locveil-commons/process/python-layout.md` §2 satisfied design-then-implement). **Import package renamed `wb_mqtt_bridge` → `locveil_bridge`; console scripts + the deliberate `catalog-v1.7` cut.** ONE tree churn (bridge was already src-layout — no layout move owed, as PROD-21 reconciliation predicted). **Rename:** `git mv backend/src/wb_mqtt_bridge → locveil_bridge` (98 files, history preserved) + identifier sweep across **104 `.py`** (51 src + 53 tests), `pyproject.toml` (`[project.name]` was already `locveil-bridge`; the entry-point **group** `wb_mqtt_bridge.devices` → `locveil_bridge.devices` + all module paths + the 6 import-linter contract module refs + `root_packages`), `pyrightconfig.json`, `config/device-state-mapping.json` (9 `stateClassImport` paths), the CI `python-health` `src:` path, `docs/manifest.json` (7 path globs), and the live docs. **Console scripts:** `wb-openapi` → `locveil-openapi`, `wb-catalog` → `locveil-catalog`; **`wb-api` retired** (all callers in-repo, hard cut); `locveil-bridge`/`mqtt-sniffer`/`device-test`/`broadlink-*` kept their names (module paths only). **Wire identity PRESERVED (OPS-26 territory, NOT this task):** the two `driver_name="wb_mqtt_bridge"` literals (`wb_device/service.py:77`, `devices/base.py:122` — the `meta/driver` value) were explicitly restored after the sweep and left unchanged; verified no test asserts the string. **catalog-v1.7 (deliberate minor cut):** the rename moved the module-qualified names of the two `ManualInstructions` schema variants in `openapi.json` (`wb_mqtt_bridge__…` → `locveil_bridge__…`) — a schema-name rename, no field/structural change, **golden byte-identical** (`5622ba7a1a78102a`). Bumped `CONTRACT_VERSION` 1.6→1.7; regenerated via the renamed CLIs (`locveil-openapi` → `backend/openapi.json` + `cp` to `contracts/catalog/openapi.json`; `locveil-catalog --stamp` → golden + STAMP now `catalog-v1.7`, date 2026-07-13) + `ui gen:api-types`; contract README Versioning narrative continued. One voice re-pin after this covers catalog v1.6+v1.7 (staleness lane, never a push gate). **Stale build artifacts:** removed the gitignored `backend/src/*.egg-info` dirs (they carried the old entry-point group and polluted `importlib.metadata` on the local `src`-on-path install; reinstalled editable to refresh `dist-info`). **sys.path shims:** the delegation estimated 4 inert; the real count was **9** (all in collected `test_*.py`; their inserts are redundant given the editable install + `conftest.py`'s path insert) — removed all 9 + their orphaned `sys`/`os`/`Path` imports (user-confirmed the clean sweep); `conftest.py` + `auto_wrap_devices.py` (load-bearing for `from tests import …`) and the standalone scripts `debug.py`/`device_test.py` kept. Gates: import-linter **6/6**, pyright **0/0/0**, pytest **725 passed** (`not requires_device`), UI `npm run check` + `build` green, eval `locveil-openapi --stdout` valid. docs: backend-readme, contributing, ui-readme, eval-readme, quickstart, arch/overview, howto/new-driver, contract/catalog.

- [x] **CORE-11** `[P1]` — **DONE 2026-07-13** (board delegation **PROD-21**, council **HK-8**; normative `../locveil-commons/process/python-layout.md` §1 — product data lives at the repo root, config tree is `config/` singular, Dockerfiles in repo-root `docker/` built with root context). **Config tree → repo root; Dockerfiles → `docker/` with root build context.** Two commits, **NO contract cut** (golden byte-identical `5622ba7a1a78102a`). **(a) Config move** (`git mv backend/config → config`, all files pure renames): the `ConfigManager`/CLI default is the CWD-relative `"config"` and **stays relative** — the container resolves it via WORKDIR `/app` + the `/app/config` mount (a repo-relative anchor would break the container), so the only code change is the deployment-root convention, not the default value. **Cert-path coupling found in reconciliation** (this is what the delegation's "loader/CLI defaults" item was pointing at): LG TV device JSONs store cert paths as `config/devices/certs/*.pem`, validated relative to CWD = the deployment root; that root is now the repo root (was `backend/`), matching how the container resolves from `/app`. So the offline catalog build + the real `locveil-catalog`/`locveil-openapi` regen now run **from the repo root** (`uv run --project backend …`); `dump_catalog`'s `--output` default moved `../contracts/…` → `contracts/…`, `test_contracts_golden` builds under `monkeypatch.chdir(REPO)`, and the regen hint + `contracts/catalog/README` + `CONTRIBUTING` + `QUICKSTART` say so — verified the repo-root regen reproduces the byte-identical golden with the LG TVs present. **~15 test config anchors** retargeted to repo-root/`config` (`parents[2]→[3]`, `BACKEND→REPO`, the `"backend"/"config"` segment drop, a bare `open('config/…')` in `test_kitchen_hood_parameters`); walk-up discovery tests auto-adapt. `ops/update.sh` rsync source, compose comments, the CI path filters (**both** backend + ui jobs now list `config/**`), the `config-master-tree` invariant text, the manifest globs, and the two setup-flow diagrams (`.dot` + regenerated `.png`) all followed the move. **(b) Dockerfiles** (`git mv backend/Dockerfile → docker/Dockerfile.backend`, `ui/Dockerfile → docker/Dockerfile.ui`, both built with **root context**): backend COPYs re-prefixed (`src/`→`backend/src/`, `pyproject.toml uv.lock`→`backend/…`); **fixed the CORE-10 miss** `CMD ["wb-api"…]` → `["locveil-bridge"…]` (the retired script would have failed the container at start); the UI Dockerfile's `COPY backend/config` + `COPY backend/openapi.json` were **vestigial** (the build is typecheck + vite only, reading nothing outside `ui/` — its own comment said so) and were removed; CI `context`/`file` repointed for both jobs; the two component `.dockerignore`s merged into one root `.dockerignore` (both images now share the root context) and deleted. Both images **build clean** locally against the root context (native amd64). Gates: import-linter 6/6, pyright 0/0/0, pytest **725 passed**, contract-guard 0, docs-manifest 8/8, UI `check`+`build` green. docs: backend-readme, ui-readme, contributing, quickstart, contract/catalog, arch/ui, howto/new-device, howto/new-scenario, install, diagram/device-setup-flow, diagram/topology-setup-flow. Remaining PROD-21 bridge work: **OPS-26** (owner-gated `meta/driver` wire cutover).

- [x] **CORE-14** `[P1]` `HW-GATED` — **Boot race: MQTT connect-timeout skips WB emulation + scenario cards
  PERMANENTLY — move their setup onto the on-connect callback** (filed 2026-08-02 off the
  power-outage incident investigation; live evidence in `service.log.20260801.log` on the WB7).
  **CODE HALF LANDED 2026-08-02 (same session).** Shipped: `make_wb_cards_publisher`
  (`app/bootstrap.py`, module-level for testability) — WB device cards + scenario cards move
  into a ONE-SHOT on-connect callback; bootstrap registers it on the client and also calls it
  directly when the boot connect already happened, gated on the LIVE `mqtt_client.connected`
  flag (not the wait's snapshot — a connect can land between the 30 s wait timing out and
  registration). The wait itself stays (normal-boot ordering: cards before uvicorn serves) but
  a timeout now defers with a WARNING instead of skipping. One-shot by DESIGN: setup publishes
  config-derived initial control values, so re-running on later reconnects would clobber live
  values — consequence: a mid-run broker restart still wipes the cards without healing (only
  the catalog version self-heals, VWB-32); explicit non-goal here, file a follow-up if it ever
  bites (the heal needs current-state value republish — the never-wired
  `WBVirtualDeviceService.handle_mqtt_reconnection` republishes metas only). Secondary
  DECIDED: failed driver init at boot (the Broadlink `Errno 101`) gets NO retry trigger from
  this task — stays OPS-18 offline-marking + manual `/reload`. 5 new tests
  (`test_wb_cards_on_connect.py`: latch, containment ×2, fire-time adapter resolution,
  lost-race end-to-end through the reconnect-loop harness); suite 752, pyright 0, contracts
  6/6, openapi golden byte-identical; `arch/overview` startup sequence re-truthed same change.
  **DEPLOY HALF DONE 2026-08-02 (owner-confirmed 2026-08-04):** current main deployed on the
  WB7 (container restart 09:48 UTC, minutes after the image build); verified live 2026-08-04 —
  `kitchen_hood/set_light` card retained with full meta, the «Kitchen Light Switch Control»
  rule fires clean (zero `unexisting control` errors since, surviving wb-rules' own midnight
  restart), and `bridge/catalog/version` is back to the golden `5622ba7a1a78102a` (the
  `95e24c…` drift was the OLD image's catalog code — resolved by the deploy, no config issue).
  The incident that filed this task is CLOSED. **REMAINING (the narrowed HW gate): verify the
  race fix itself** — cards present after a cold boot that loses the network race (the next
  power outage proves it for free, or simulate: stop mosquitto across a bridge container
  restart, start it >30 s later, confirm cards appear on connect — disruptive to the live
  house, owner's call on timing; NB on the RUNNING image the broker must be back within ~50 s
  of the bridge's FIRST connect attempt or the old retry budget exhausts — CORE-16 (DONE
  2026-08-04) removed that cap, so once the next deploy lands the simulation needs no timing
  window at all: any broker-return delay works).
  The 2026-08-01 cold boot brought the container up before the host network: bootstrap's 30 s
  wait for MQTT timed out at 08:01:39 UTC (`WB emulation will be skipped` + `scenario WB cards
  skipped`), then connect attempt 5/5 SUCCEEDED at 08:02:00 — 20 s too late. Everything
  connect-driven recovered (subscriptions, the VWB-32 on-connect catalog republish), but the WB
  virtual cards + scenario cards publish exactly ONCE at bootstrap behind that gate — and since
  the in-box mosquitto runs without persistence (deliberate, see wb7 deployment notes), the boot
  wiped every retained bridge card with nothing to repopulate them. Symptom: wb-rules logged
  `failed to SetValue for unexisting control kitchen_hood/set_light` on every switch press (the
  «Kitchen Light Switch Control» rule) — the house-visible break. Any cold power-on can
  reproduce this: the bridge's MQTT client targets the LAN IP (`192.168.110.250`), which doesn't
  exist until the interface is up, so all early attempts fail unreachable. Scope: fire
  WB-emulation + scenario-card setup from the MQTT on-connect callback (one-shot latch — the
  VWB-32 pattern; CORE-1's `rebuild_scenario_cards` hook is already connect-ordered on the
  reload path) instead of the one-shot 30 s bootstrap gate; a bigger timeout only shrinks the
  window. Same-boot secondary: the kitchen_hood Broadlink driver init failed (`[Errno 101]
  Network is unreachable`) and the device sat `connection_status: error` until reload — decide
  whether failed-setup devices get a retry trigger here, or whether that stays OPS-18
  offline-marking + manual `/reload`. NB `/reload` is NOT a recovery hatch on the deployed
  07-15 image: the 2026-08-02 live reload restored no cards — the old inline router calls
  `set_runtime_services(mqtt_client=...)` which resets `wb_service` to `None`, so per-device
  emulation setup silently skips publishing (exactly CORE-1's gap (4), fixed on main by the
  `ReloadService` extraction). Deploying current main is the companion op — and doubles as
  CORE-1's pending rack verify of `POST /reload`.
  **CLOSED 2026-08-04 — the lost-race simulation PASSED on the deployed image (CORE-16
  live-proven in the same run):** mosquitto stopped, bridge restarted; attempt 1 refused
  08:16:31 UTC, the 30 s gate timed out 08:17:01 with the new deferral WARNING (`WB virtual
  cards will be published on first connect`) — the exact 08-01 scenario; the loop survived
  past the old 5-attempt budget (attempts 6 and 7 observed — pre-CORE-16 code would have
  given up permanently after 08:17:22); broker returned 08:18:01, attempt 7 connected
  08:18:17, and the one-shot on-connect callback published everything seconds later
  (emulation 08:18:21, scenario card 08:18:24). Post-state: 14 cards retained,
  `kitchen_hood/set_light` present, catalog golden. House-dark window ≈5.5 min,
  owner-sanctioned.
  docs: none — arch/overview's startup-sequence passage was updated at the code-half landing
  (same change as the behavior); nothing further at close.
  contracts: none — no surface moved.

- [x] **CORE-15** `[P1]` — **DONE 2026-08-04** (filed + executed same day off the CORE-1 rack
  verify; owner directive "fix both problems", scope extended at execution to the ENTIRE config
  bundle on owner instruction — topology explicitly). **`/reload` now re-wires the full
  composition: capability maps AND rooms/scenarios/topology.** The audit against bootstrap found
  FOUR parity gaps, not one — the re-initialized fleet lost: (1) **capability maps** (canonical
  answered `capability_not_supported` fleet-wide, the catalog published `capabilities: []` and
  the retained version flipped golden→`95e24c546adc0dac` — confirmed live on both images);
  (2) the **SSE `event_publisher`** (device state changes stopped reaching the UI event stream);
  (3) the **problem-report `dispatch_ring`** (evidence rings empty); (4) the post-fleet config
  consumers — `RoomManager.reload()` and `ScenarioManager.initialize()` were never called, so
  rooms.json / scenarios / **topology.json** edits silently did not take effect on `/reload`
  (the owner's suspicion, confirmed: topology loads ONLY in `ScenarioManager.initialize`).
  **Fix shape — one recipe, no drift:** the post-`initialize_devices` wiring extracted to
  module-level `wire_fleet(...)` in `app/bootstrap.py`, called by BOTH boot and reload (drift
  between the two compositions is the root cause of this whole bug class — CORE-1's gaps (1–5)
  plus these four); reload gains a `rewire_fleet` composition hook (the established CORE-1 hook
  pattern — ReloadService stays composition-free) running `wire_fleet` + `room_manager.reload()`
  + `await scenario_manager.initialize()`, deliberately NOT connection-gated (config reloads
  even with the broker down). Scenario re-init is live-safe: restore is tracking-only (SCN-18,
  zero device commands), observers survive, the WB `on_active_changed` re-point follows in
  `rebuild_scenario_cards`. Tests: `test_wire_fleet.py` (collaborator assignment, attach +
  exposure-check invocation, violations-warn-not-raise) + `test_reload_service.py` extended
  (rewire called with the new composition; NOT connection-gated on timeout; init→rewire→
  subscribe ordering). Suite **757**, pyright 0, contracts 6/6, openapi/golden byte-identical.
  **Live re-verify rides the next deploy** — CORE-1's failing gate item (catalog stays golden
  across `/reload`) re-runs then.
  docs: none — `/reload`'s documented promise ("reload configs at runtime", interfaces.md) is
  what this fix makes TRUE; no manifest node describes the internal reload composition.
  contracts: none — no surface moved; golden byte-identical.

- [x] **CORE-16** `[P1]` — **DONE 2026-08-04** (filed + executed same day; found by reading
  `_run_mqtt_client` to time the CORE-14 simulation window). **The MQTT connect loop never
  gives up.** Was: `max_retries = 5` (≈50 s of 5/10/15/20 s backoff) then `Max retries reached.
  Giving up MQTT connection.` and the loop EXITED — a boot whose broker wasn't reachable within
  ~50 s of the first attempt left MQTT dead for the process lifetime while the container
  healthcheck stayed GREEN (HTTP-only probe; uvicorn serves fine with MQTT dead, so neither
  docker nor systemd applies restart pressure). The 2026-08-01 outage survived on attempt 5/5
  by luck. CORE-9 had made the budget per-episode for ESTABLISHED connections; the
  never-yet-connected boot episode kept the hard cap. Now: `while True` with backoff
  `min(5 s × attempt, 60 s)` — one attempt per minute steady-state (the broker is on the same
  box); a successful connect still resets the episode (CORE-9 semantics preserved);
  `CancelledError` still exits (shutdown path). CORE-14's on-connect card publishing now
  composes correctly: whenever the broker appears — 50 seconds or 50 minutes later — the
  connect lands and the cards publish. Test: `test_boot_episode_never_gives_up_and_caps_backoff`
  (20 refusals then success: loop survives far past the old budget, the on-connect callback
  fires on the eventual connect, backoff ramps from 5 s and caps at 60 s). Healthcheck stance
  recorded as a decision: HTTP-only STAYS — a broker outage must not restart-loop the bridge;
  log-and-retry is the correct posture. Suite **757**, pyright 0, contracts 6/6.
  docs: none — no manifest node describes connection-retry mechanics.
  contracts: none — no surface moved.

## LIB — pymotivaxmc2 library (sibling repo)

- [x] **LIB-1** `[P1]` — **DONE 2026-07-15** (executed in `../pymotivaxmc2` per the workstream convention; commit `08073b3`, shipped in **pymotivaxmc2 0.8.0**, tag `v0.8.0` → PyPI via the library's gated CI; bridge repin same change). **Control-port transactions serialized — one in flight at a time (subsumes reply-correlation).** `Semaphore(5)` → `asyncio.Lock`: the device has vendor-confirmed limited processing power (openHAB: "subscribes to all channels simultaneously → grind to a halt") and one unkeyed reply queue, so concurrency both overloaded it and let transactions steal each other's replies (observed in production at wedge #3). Plus **stale-frame hygiene**: `SocketManager.drain()` before every send, and `_recv_expected()` discards mismatched frames within the deadline instead of burning a retry — a late reply from a timed-out attempt can no longer fail a fresh transaction (that conversion was the false-retry driver). Public API unchanged (concurrent callers queue). Library tests: serialization ordering, drain-before-send, stale-discard for command+subscribe; commands guide updated. Library gates: 276→295 tests across the sweep, import-linter 4/4, no-TYPE_CHECKING, pyright 0. docs: none — sibling-library internals; no bridge manifest node describes the library's transaction model (the library's own guides updated in its repo).

- [x] **LIB-2** `[P1]` — **DONE 2026-07-15** (commit `0f2d111`, in pymotivaxmc2 0.8.0). **Retry damping + pacing + `ack="no"`.** Per-call `retries=` on every facade command + `status()`/`get_input_names()` and all protocol transactions (`retries=0` = exactly one packet — the DRV-39 consumer); `ack=False` fire-and-forget (the spec makes the ack optional; the always-awaited ack was the retry-ladder entry point exactly when the device is busy); `EmotivaController(max_retries=, min_send_interval=)` constructor knobs — pacing enforced at one `_send_control` chokepoint (pace → drain → send); `request_properties_full` retries **only the missing properties** with results accumulating across attempts, and an empty request no longer sends an empty Update packet. Commands guide gained the "Retries, ack, pacing" section. docs: none — same rationale as LIB-1.

- [x] **LIB-3** `[P2]` — **DONE 2026-07-15** (commit `f7ff11a`, in pymotivaxmc2 0.8.0; pulled forward from `[deferred]` to ride the single release). **API + hygiene batch.** (1) `keepalive_interval_ms` public accessor — the bridge driver's `getattr(client, "_info")` replaced with the public property (driver cleanup landed with the repin). (2) `disconnect()` now ACTUALLY unsubscribes: the controller tracks the session's subscribed set and names every property (spec §2.1.5 has no "unsubscribe all"; the old empty `<emotivaUnsubscribe>` cleared nothing); nothing subscribed → no frame. (3) `SO_REUSEADDR` on the fixed 7002/7003 binds (watchdog-recovery reconnects can't die on "address already in use"). (4) **Notification sequence numbers surfaced** (spec §2.6): `Dispatcher` tracks `last_sequence` + `gap_count` (jumps = missed notifications; reorders/duplicates don't count); controller exposes `notification_sequence`/`notification_gaps` — feeds the DRV-39/DRV-32 readiness work (targeted refresh on staleness instead of blind full polls). Connection + subscriptions guides updated. docs: none — same rationale as LIB-1.

## DOC — Docs / ledger / process

- [x] **DOC-1** — **DONE** 2026-05-20 — archived `docs/TODO.md` → `docs/history/phase1-2.md` (open items migrated to §5.1); Roborock bullet kept (planned feature, not a false claim). P2 fully done.
- [x] **DOC-2** — **DONE** 2026-05-19 — committed as-is in UI `8ab2cfa` as part of the appliance-category feature. The doc accurately describes the current code direction.
- [x] **DOC-3** — **ADOPTED THEN DROPPED** 2026-05-20 (too slow for a solo project — see §6 revision log; the dependency-hardening work it produced was kept). ~~**Adopt GSD** ([gsd-build/get-shit-done](https://github.com/gsd-build/get-shit-done)) as the dev workflow.~~ *(Reverses the earlier "out of scope" verdict — re-study 2026-05-20 found GSD is built for solo devs, has a brownfield path (`/gsd-map-codebase` + `/gsd-ingest-docs`), and handles multi-repo via `/gsd-workspace --repos`.)* Sequencing: **(A) archive stale docs — DONE**; **(B) fix living docs to match code — DONE**; **(C) author the GSD-seed artifacts** (PROJECT vision, ARCHITECTURE, the UI↔backend CONTRACT, CONVENTIONS) + ADRs 0001–0005 — DONE; **(D) install GSD + map-codebase + ingest-docs — DONE.** Outcome: backend-primary, `.planning/` **tracked** in git (no `/gsd-workspace`). 6-phase ROADMAP generated; **Phase 1 = Fix the Scenario Layer** (= P0.5 #12). See the Step D runbook below for the as-run sequence.

#### Step D runbook — GSD bootstrap (DONE 2026-05-20; as-run record)

Bootstrapped **backend-primary** (`.planning/` lives here and is **tracked** in git; UI
referenced via the contract doc; no `/gsd-workspace` — revisit only if two-repo
coordination gets painful). As-run sequence:

1. `/gsd-config` → committed `b931430`. model profile **balanced**, **branching off**
   (push to `main`), `commit_docs=true` (`.planning/` tracked), `auto_advance=false`.
   ⚠️ **Ordering gotcha:** in the installed SDK version `/gsd-config` **cannot create
   `.planning/` on its own** — `config-ensure-section` / `config-set` both require the
   file to pre-exist. The config workflow must bootstrap via
   `gsd-sdk query config-new-project '{"commit_docs": true}'` (creates `.planning/` +
   canonical `config.json`). So config is only "step 1" *after* something creates
   `.planning/`; either run it via `config-new-project` (as done) or run step 2/3 first.
2. `/gsd-map-codebase` → committed `4223f39`. 4 parallel mapper agents wrote 7 docs:
   `.planning/codebase/{STACK,INTEGRATIONS,ARCHITECTURE,STRUCTURE,CONVENTIONS,TESTING,CONCERNS}.md`.
3. `/gsd-ingest-docs` (mode=new) → committed `98664f3`. Used a **curated 10-doc manifest**
   (5 ADRs + `ui_backend_contract` SPEC + `project`/`action_plan` PRDs +
   `architecture`/`conventions` DOCs) to stay on-intent and under the 50-doc cap — the raw
   `docs/` tree (53 `.md`) would have swept in the 29 archived "don't ingest" docs.
   Pipeline: 10 classifier agents → synthesizer → roadmapper. 0 conflicts. Produced
   `.planning/{PROJECT,REQUIREMENTS,ROADMAP,STATE}.md` + `intel/`. **Subsystem specs
   (`docs/design/scenarios/*`, `docs/devices/*`, etc.) were intentionally excluded** — read them
   at plan-time, or merge-ingest later with a narrow manifest.
4. `/gsd-progress` — **next** (routes onward). ROADMAP already finalized by ingest, so a
   separate `/gsd-new-project` is not needed.

First real phase to tackle via GSD: **ROADMAP Phase 1 = Fix the Scenario Layer**
(= P0.5 #12), the top functional priority
(`/gsd-discuss-phase 1` → `/gsd-plan-phase 1` → `/gsd-execute-phase 1`).
- [x] **DOC-4** — **Scope-drift guard. DONE 2026-06-30.** `scripts/check_scope.py` (pure stdlib) enforces
  `single-task-ledger`. Re-scoped from the filed spec ("freeform numbered-markdown-table format") to the
  new `PREFIX-N` two-file model the DOC-9 re-ID created (which made it a near-port of the sister repo's
  guard). Five checks, fail the build: **duplicate** id (declared >1×), **misplaced** status (`[x]` left in
  the active file / non-`[x]` in DONE), **orphan** finding (a `PREFIX-N` id in `docs/design`|`docs/review`
  not in the ledger), **dead** evidence link (a `docs/design`|`docs/review` path in the ledger missing on
  disk — with a negative lookbehind so sibling-repo `wb-mqtt-voice/docs/...` paths don't false-positive),
  **phantom** alias (an `action_plan_aliases.md` target not declared). Plus a per-workstream status summary.
  Wired into CI as the first step of the `backend-test` gate (`.github/workflows/build-arm.yml`); runs
  standalone too. Verified: clean pass on the live ledger (52 tasks) + positive tests for the orphan and
  dead-link checks. CLAUDE.md `single-task-ledger` note updated from "deferred follow-up" → implemented.

- [x] **DOC-5** — **#1 — Ledger-format convergence design (DESIGN GATE). DONE 2026-06-30.** Design recorded in
  **[`docs/design/ledger_format_convergence.md`](../design/ledger_format_convergence.md)**
  (`design-then-implement` — design done, no re-ID executed). Decisions: (D1) identity = stable
  **`PREFIX-N`** workstream-serial, assigned once / never renumbered — fixes the `#22→#23` renumber
  and the four live `#7`/`#8` cross-section collisions; (D2) **subsystem-shaped buckets**
  `DRV/SCN/VWB/UI/OPS/DOC` + a proposed 7th **`CORE`** (cross-cutting backend); (D3) priority a
  separate `P0/P1/P2` tag, P-bands dissolved; (D4) milestone tags `[house]/[later]/[parked]`
  (bridge's "house works" = voice's release gate); (D5) status legend + a bridge-specific `HW-GATED`
  marker; (D6) two-file split by workstream. The doc carries the **complete old→new ID mapping** (the
  reviewable artifact for #5) + the migration mechanics. **Open confirmations before #5 runs:** the
  `CORE` bucket, the tag names, and whether to merge #3 into #5. _Mapping note: §5.2 #1–#7 themselves
  re-ID to `DOC-5/6/7/8/9` + `SCN-5` + `DOC-10` per the doc._
- [x] **DOC-6** — **#2 — Apply the two-file split. DONE 2026-06-30.** Created frozen `docs/action_plan_DONE.md`
  and moved the **seven fully-complete early phase bands** (P0, P0.5, P1, P2, P2.5, P2.6, P3) there,
  by section — verified byte-identical, no content loss, every moved ID now in exactly one file; §4
  carries a pointer in their place. **In-flight phases (P3.6, P3.7, P4) deliberately left whole** —
  splitting a partially-done phase is §5.2 #1's call, so their done rows move when the phase closes.
  No re-ID. CLAUDE.md `single-task-ledger` two-file-split note updated to "applied."
- [x] **DOC-8** — **#4 — Extract the narrative sections. DONE 2026-06-30 (RE-SCOPED: archive, not promote).**
  Reconciliation found §1/§2/§3/§7 were all **superseded 2026-05 starting-survey analysis**, not live
  reference — promoting them would publish stale content as current truth (the live equivalents already
  exist in `docs/architecture/*`, `ui_backend_contract.md`, `ops/`). So they were **archived verbatim**,
  not promoted: §1–§3 → `docs/archive/initial_survey_2026-05.md`; §7 → `docs/archive/codegen_alternatives.md`
  (decision settled, codegen deleted at the Layer-3 cutover). §7's 3 inbound refs (DONE #3.5, DONE #10,
  `ui_backend_contract.md`) + the §1.2 ref repointed. Plan is now a **true spine** (§0, §4, §5, §6 only)
  — 900 → 681 lines. Extraction verified byte-identical.
- [x] **DOC-9** — **Full re-ID execution. DONE 2026-06-30.** Re-keyed the whole live ledger onto the
  stable `PREFIX-N` scheme per `ledger_format_convergence.md`: `action_plan.md` restructured from P-bands
  into workstream sections (DRV/SCN/VWB/UI/OPS/CORE/DOC) + a "How to use this file" legend + an Acceptance-gate
  section (ex-P4 #1–#5); `action_plan_DONE.md` reorganized by workstream + re-IDed; one-time
  `action_plan_aliases.md` added (old→new, ~50 tasks); the journal kept frozen with the alias map resolving
  back-refs; CLAUDE.md `single-task-ledger` ID example updated. Folded the former DOC-7 (conventions applied
  in this pass). Builder verified every task body survives byte-exact (distinctive-substring check, ALL OK).
- [x] **DOC-10** — **Retire the frozen scenario/Layer-3 ledgers. DONE 2026-06-30.** Four dispositions:
  (1) `scenario_redesign_progress.md` → **archived** to `docs/archive/scenarios/` (git mv, history kept) +
  FROZEN header (its "branch not merged / 274 tests" status was years-stale). (2) `layer3_step0_layout_analysis.md`
  → **archived** likewise (Step-0 working artifact, fully embodied in the as-built spec §17 + the placement
  engine). (3) `scenario_system_redesign.md` → **kept as the as-built spec**, freeze-edited: status note
  marking §11/§12/§14/§17.4 historical, and the §13-"decision 5 still open" vs §14-"resolved" contradiction
  fixed (explicit ordering edges only). (4) `ui_backend_contract.md` → **split**: the living seam contract
  stays; the per-commit Layer-3 *rollout* ledger (Migration → Step-2 hardening → Step-3 rollout → Step-4
  cutover, 641→429 lines) moved verbatim to `docs/archive/layer3_rollout_record.md` + a pointer. All inbound
  refs repointed (§0 doc-map, SCN-1 body, the as-built spec, and the contract's internal "see Step 4 below"
  pointers). SCN-5 (transition-aware notes) is the standing home that unblocked this. Completes the §5.2
  reconciliation series.

- [x] **DOC-12** `[P2]` — **DONE 2026-07-06 (voice-side filing — intake-verified + implemented same session, at the user's "implement right away").** **Ledger-discipline guard triad ported from wb-mqtt-voice** (their QUAL-72/73/74) into `scripts/check_scope.py` + CLAUDE.md, adapted to this ledger's formats (`### PREFIX — …` headers active / `## PREFIX — …` in this file; level-aware section tracking so a `####` runbook inside a section doesn't reset it). **Intake correction:** the filing's check (1) — stranded `[x]` completions in the active plan — **already existed here** as DOC-4's MISPLACED-status check (both directions); the real port was **(2) MISFILED** (ID prefix must match the enclosing section, both files — catches the insert-before-next-header slip) and **(3) OUT-OF-ORDER** (IDs ascend within each section; completions insert at sorted position, never append). **The new checks immediately caught 7 real violations from the 2026-07-05/06 tempo:** active OPS (11,7,8) + CORE (4,1) mis-ordered; DONE VWB-17/18/19 and DOC-4 appended out of order — fixed by a one-time mechanical resort (scripted block moves; **zero-entry-loss asserted**: identical line multisets + ID counts before/after, 24+59 entries preserved). All three checks **canary-verified in both directions** (violation → gate fails with the named row; revert → clean pass). CLAUDE.md: the stale "in-flight phase rows stay DONE in place" carve-out (contradicted move-not-flip; phases retired at the re-ID anyway) replaced by the explicit discipline triad. Reference: `../wb-mqtt-voice/scripts/check_scope.py` + their commits 203daa5/341494d/5f9820d.

- [x] **DOC-13** `[P1]` — **DONE 2026-07-12** (filed + executed same session; PROD-17 bridge delegation (2), council HK-6; normative `../locveil-commons/process/user-docs.md` §4 — **the org's FIRST manifest implementation**; commons/voice pend in their own repos). **The docs manifest + coherence test + dialect update.** **(a)** `docs/manifest.json`: 36 nodes over 14 roots — front-door README (banner), CONTRIBUTING, the backend/ui/eval READMEs, QUICKSTART, `ops/INSTALL.md`, architecture ×6, guides ×4 (report-a-problem = the end-user class), the contracts registry, catalog + device-integration READMEs as **canonical-reference with the `{invariant, stamp, guard}` triple**, the docs-manifest pointer README, and all 15 `docs/images/` diagram pairs as diagram nodes; 10 surfaces exactly (`rest-api`/`catalog`/`device-integration`/`drivers`/`scenarios`/`config`/`ui`/`mqtt`/`reports`/`deployment`); floor fully staffed (this repo has every capability). **(b)** `contracts/docs-manifest/`: STAMP core @ `docs-manifest-v1` (INTERNAL — no git tag, repo-internal contract class) + pointer README + a **verbatim copy of the commons schema** so CI stays hermetic; registry row labeled INTERNAL. **(c)** `tests/unit/test_docs_manifest.py` (8 tests, drift-guard pattern): schema validation against the pinned copy, node↔tree bijection over the roots (passed on the first sweep — the inventory is complete), path-existence unless pending-gate, `.dot`↔`.png` pairing, floor-class presence, surfaces ≤10 + covers resolve, DONE-ledger verdict node-ids resolve. **Deferred slice recorded, not silently dropped:** the falsifiability check (verdict `none` vs touched globs) needs per-completion diffs — arrives with the shared tooling at rule-of-two per the spec. **(d)** CLAUDE.md: the repo-local `user-facing-docs-are-done` bullet rewritten as the dialect of the incoming org invariant — manifest as scope authority, the verdict-line rule, caused-vs-discovered, registration-rides-the-causing-task. One schema lesson upstream-worthy: top-level `$comment` is rejected (`additionalProperties: false`) — the commons template skeleton carries one and will fail validation at fill-time unless stripped or the schema admits it. Suite 708→**717**, pyright 0, import-linter 6/6, contract-guard 0 warnings. docs: contracts-registry, contract/docs-manifest

- [x] **DOC-14** `[P1]` — **DONE 2026-07-12** (filed + executed same session; PROD-17 bridge delegation (1), council HK-6). **The DOC staleness pass — CONTRIBUTING, the OpenAPI descriptions (⇒ contract cut `catalog-v1.6`), and NINE stale diagrams (not 5).** **(a) CONTRIBUTING.md:** pre-VWB-29 regen paths fixed; "three import-linter contracts" → the real six (CORE-6); the CI-gates section gained the missing `contract-guard` job; the model-specific commit-trailer wording and the "7 AV driver classes" count made durable. **(b) OpenAPI field descriptions:** every task ID (`SCN-7`, `VWB-23`, `DRV-5`, `DRV-28`, `VWB-18`, `VWB-20/G*`…), `§`-ref, and the `scenario_system_redesign.md §6` pointer scrubbed from Pydantic docstrings/`Field` descriptions across `schemas.py`, `devices/config.py`, `devices/models.py`, `scenarios/models.py`, `infrastructure/config/models.py`, and four routers — rewritten reader-first, verified by an exhaustive regex scan over the regenerated schema (**CLEAN**); the `/scenario/{id}/layout` route docstring also lost the dead role-routing narrative (controls dispatch canonically via `canonicalEntityId`). Internal `#`-comments deliberately untouched (not a user surface). Regen chain: `backend/openapi.json` + `contracts/catalog/openapi.json` + UI `gen:api-types`, `npm run check` + `build` green. **Artifact bytes moved ⇒ deliberate minor cut: `CONTRACT_VERSION` → 1.6, tag `catalog-v1.6`**, STAMP gains the §2 forward-requirement **`artifacts` list** (pin-completeness; stamp test extended), README Versioning narrative continued; golden byte-identical (`5622ba7a1a78102a`). NB: voice re-pinned at v1.5 earlier today — their next re-pin picks up v1.6 (staleness lane, never a push gate). **(c) Diagrams:** REL-4's unrecorded "5 flagged" resolved by verifying ALL 13 non-REL-4-rendered `.dot` sources against code via three parallel read-only agents — **9 stale, 4 accurate** (device-setup-flow, mqtt-integration, topology-setup-flow, room-membership). Fixed + re-rendered: hexagon (4→5 ports incl. `ReportSinkPort`, 8→9 drivers, `GitHubReportSink` node), driver-flavors (fourth flavor: MitsubishiHvac native-MQTT-dialect; stale `hvac_*` dropped from passthrough), scenario-lifecycle (dead `/{id}/activate|deactivate` → `/scenario/start|switch|shutdown`, `scenario_id` field, per-room `active_scenario:<room_id>` key, room-scoped framing), scenario-reconciler (dead trigger endpoint), declarative-composition (`load_scenarios`/`load_topology`/`switch_scenario`, carries video/audio), appliance-pages (HVAC panels SHIPPED ×3, registry by device_id via `getAppliancePage`, real routes, `/canonical` in the backend surface), layout-manifest (canonical dispatch atom; `sourceDeviceId` demoted to informational), rooms-in-use (global aggregates BUILT — real device list), voice-setup (57→78 configs/11 rooms; stale pending-list reduced to the wb-msw sensor side). Renders spot-checked visually. Suite 717, pyright 0, contract-guard 0 warnings. docs: contributing, contract/catalog, diagram/hexagon, diagram/driver-flavors, diagram/scenario-lifecycle, diagram/scenario-reconciler, diagram/declarative-composition, diagram/appliance-pages, diagram/layout-manifest, diagram/rooms-in-use, diagram/voice-setup

- [x] **DOC-15** `[P1]` — **DONE 2026-07-12** (filed + executed same session; PROD-17 bridge delegation (3), council HK-6 q3: the ADR class is ABOLISHED org-wide). **ADR dissolution: 0006's policy → CONTRIBUTING, 0001–0005 verified-and-archived, `docs/adr/` retired.** **(a) 0006:** the four dependency-pinning rules distilled into a new **`CONTRIBUTING.md` → "Dependency policy"** section (durable wording, current facts, no tracking language — the normative home now); recovery-runbook pointer carried over. **OPS-19 re-pointed** (its entry cited "ADR 0006 Rule 2" as the policy source three times — now cites the CONTRIBUTING section; the mirror-vs-exception decision itself unchanged, still `[deferred]`). **(b) 0001–0005 verify-and-archive:** REL-4 audited all six against release reality 2026-07-10 (fresh evidence); re-checked at archival — 0001–0004 in force (living homes named in each banner: the OpenAPI-contract coupling, the additive openapi override, backend-owned state mapping, runtime URL config); **0005's voice half recorded as overtaken by events** (voice was NOT delegated to the WB Alisa bridge — the shipped path is the catalog contract; the Miele/SprutHub prune half stands). All seven files (incl. the class README) moved to `docs/archive/adr/` with dated supersession banners naming where living truth sits. **(c) References re-pointed:** CONTRIBUTING's two `docs/adr` sites (workflow bullet + source-of-truth pointers — now `docs/design/` + the frozen-archive note), `dependency-recovery.md` §see-also, the plan §0 document-map line; grep-verified zero live `docs/adr` links remain. Manifest untouched (ADRs were never nodes — abolished class; `docs/archive/` outside the roots). Coherence test + both guards green. docs: contributing
- [x] **DOC-16** `[P2]` — **DONE 2026-07-14** (filed + executed same change; commons PROD-23
      delegation — the HK-9 round-1 dependency audit's stale-dep side-find; claims verified at
      intake). **VWB-39's dependency line re-anchored to reality.** The old text was stale twice:
      "Once VWB-38's versioned artifact exists" (it has existed since 2026-07-12 —
      `device-integration-v1`, tagged) and "Depends on … DRV-36's implementation" (DRV-36 was
      design-only by owner decision; the implementation is **DRV-37**). Rewritten: the artifact
      exists, the task activates alongside DRV-37 at the satellite's first conforming descriptor
      (the PROD-20 chain), with a dated re-anchor note. No scope change — VWB-39 stays
      `[deferred]`, its deliverable untouched. docs: none — ledger prose only; no manifest node
      describes individual plan entries.

- [x] **DOC-17** `[P2]` — **DONE 2026-07-15** (filed at UI-17's planned-docs pass 2026-07-14;
  executed same session as the scope-v6 re-pin). **Planned-pages prose staleness + the diagram
  rename-era title fix.** **Reconciliation narrowed the scope twice, both at owner direction:**
  (i) **device-setup.md + topology-setup.md left untouched** — the owner classes them as dummy
  placeholder pages for now, not content to reconcile; (ii) DOC-14 (2026-07-12) had already fixed
  the diagram *content* (voice-setup 57→78 configs/11 rooms, appliance-pages HVAC-shipped×3), so
  DOC-17's diagram half was purely the title rename — content was already accurate. **(a)
  voice-setup.md:** the §P3.7 tail was stale — `#22` (global aggregates, VWB-10 DONE 2026-07-04),
  `#25` (catalog sweep + bulk e2e, VWB-13 DONE 2026-07-09), `#26` (value-label layer, VWB-14 DONE
  2026-06-09) all shipped but listed pending/"Not built"; only `#24` (wb-msw sensor side, VWB-12)
  remains, deferred post-release. Fixed the header, the Pillar-C row, the "what's pending" section
  (three shipped → one deferred), the status table, and the stale counts (6/7 profiles → **12**;
  57 configs/10 rooms → **62/11**; 8 drivers → **9**, MitsubishiHvac added; 70 devices → **78** =
  13 AV + 3 HVAC + 62 WB). **(b) appliance-pages.md:** `HvacPanel.tsx` shipped (3 instances,
  registered by `device_id` in `ui/src/pages/appliances/index.ts`, routed through `DevicePage`)
  but listed "Not built / Planned" — flipped the header, roadmap row, "next to land" paragraph, and
  two status-table rows; added an honest note that the shipped routing is the interim device-id path,
  the `/appliance/:id` + `AppliancePage` (`device_class` dispatch) container still the planned
  end-state. **(c) Diagrams:** all 15 `docs/images/*.dot` titles `wb-mqtt-bridge` → `locveil-bridge`
  (33 occurrences), PNGs regenerated in the existing style via `dot`; folded in the one same-era
  sister-repo label in `voice-setup.dot` (`wb-mqtt-voice` → `locveil-voice`). `wb-mqtt-serial`
  (the genuine Wirenboard daemon) correctly left alone. Manifest coherence test 8/8 (descriptions
  don't embed titles; no manifest edit needed). docs: diagram/hexagon, diagram/rest-surface,
  diagram/ui-architecture, diagram/appliance-pages, diagram/layout-manifest, diagram/driver-flavors,
  diagram/device-setup-flow, diagram/mqtt-integration, diagram/scenario-lifecycle,
  diagram/scenario-reconciler, diagram/declarative-composition, diagram/room-membership,
  diagram/rooms-in-use, diagram/topology-setup-flow, diagram/voice-setup (the planned pages
  themselves are not manifest nodes).

- [x] **DOC-18** `[P2]` — **DONE 2026-07-15** (filed at the scope-v6 re-pin, OPS-31, same session).
  **The four unreferenced evidence docs reconciled — two anchored, two retired.** Scope-v6's new
  UNREFERENCED-evidence rule flagged four docs under the `evidence.dirs` roots that no ledger entry
  named. Verified each against the live tree and made the keep-vs-retire call: **(a) KEPT + anchored
  here** — `docs/design/conditioner/mitsubishi.md` (the CN105 brown-out / power-rail hardware note;
  the `MitsubishiHvac` driver + 3 HVAC panels are live, so the reference stays useful) and
  `docs/design/ui/deployment-network-config.md` (the consumer-UI container networking —
  `ui/nginx.conf.template`, `docker-entrypoint.sh`, `src/config/runtime.ts` all still present and
  accurate). **(b) RETIRED to `docs/archive/`** — the layout spec now at
  `docs/archive/ui-docs/remote_layout.md` described the removed build-time codegen renderer
  (`ZoneDetection.ts` / `RemoteControlTemplate.ts` / per-device `*.gen.tsx` / `deviceHandlers/`, all
  gone), superseded by the runtime Layout Manifest (`RuntimeDevicePage.tsx` + `layoutManifestAdapter.ts`);
  the research brief now at `docs/archive/review/wb-alice-bridge.md` was retired at owner decision —
  the shipped voice path is the Irene catalog contract, not the WB-native Alisa bridge (the same call
  ADR 0005's archived voice half records). Both gained a dated retirement banner naming where living
  truth sits before the move. With the tree clean, `[evidence] unreferenced` flipped `warn` → **`error`**
  in `.scope-guard.toml` to match commons and lock HK-10's fourth anchoring direction in. Guard bare:
  green. docs: none — the two kept docs are internal design/hardware notes (not manifest nodes; the
  manifest roots are architecture/guides/images, not `docs/design`), and archival moves nothing a
  manifest node describes.

## REL — Release

- [x] **REL-1** `[P0]` — **DONE 2026-07-06 — Definition of release 1 signed off (interactive session).** Mirror of the voice repo's REL-1. Deliverable = the **"Definition of release 1 (exit criteria)"** section at the top of `action_plan.md`: scope gate ("every `[release]` task is `[x]`", `check_scope.py` clean), release artifact (version tag + armv7 GHCR images deployed on WB7 via `ops/` compose, serving the house; other platforms = release 2), 7 exit criteria. **Tag migration:** `[release]`/`[deferred]` replaced `[house]`/`[later]`/`[parked]` (seeded by remap, then verified per row) — final `[release]` set: DRV-1, DRV-2, DRV-5, SCN-3, VWB-13, VWB-16, OPS-8, REL-2/3/4 (DRV-5 + OPS-8 + VWB-16 individually pulled IN from `[later]`; everything else `[deferred]`). **Decisions recorded:** armv7/WB7-only release target · `/action` stays the documented internal door (full demotion = CORE-4, deferred) · DRV-3/DRV-8/children's-room-round-3/global-master-aliases/VWB-12 all release-2 · bedroom «шторы» stays curtain-only · wardrobe gains «свет» (VWB-25, executed same session) · docs-accuracy is a gate (REL-4 filed, DOC-11 folded in). **Structures reconciled:** the survey-era "Open questions" section closed (7/7 answered — 6 by events with evidence, 1 by decision); the acceptance-gate section annotated as absorbed (items 4½+5 ride REL-3); REL-2 (cutover, previously an ID-less user debt) + REL-3 (converged rack pass, the voice ARCH-25 pattern) + REL-4 (release docs pass) filed. Journal rotated (first archive under `docs/archive/journal/`, the 1595-line high-water breach).

- [x] **REL-2** `[P0]` — **DONE 2026-07-08** (the cutover sitting: user at the controller shell + Claude over SSH/HTTP from the dev box, live-debugging as a pair). **WB7 compose cutover + deployment realism — the house is now served from the controller.** All four exit items + both riders verified live: **(1) deployed** per `ops/INSTALL.md` — with the runbook rebuilt in-flight around controller reality: the user kept the historical runtime tree `/mnt/data/mqtt-bridge-config/{config,data,logs}` (no `.state/` subtree), the repo clone went to the exFAT SD card (`/mnt/sdcard/wb-mqtt-bridge`), Docker's data-root stays at `/mnt/data/.docker` (exFAT can't host overlay2 — checked live, decision recorded in INSTALL.md); `update.sh` gained the config-sync (rsync clone→runtime) so `git pull` remains the config channel. **(2) `wb-rules/all_lights.js` deployed** — the `all_lights` voice contract has a listening side at last. **(3) Realism dump: MATCH** — live `/system/catalog` = golden `8159b4b0068d1c63`, 79 devices, zero deployment drift. **(4) Reboot survival: PASSED on the second attempt** — the first reboot FAILED (`Dependency failed`: `RequiresMountsFor=/mnt/sdcard` forced the lazily-automounted card's mount+fsck into early boot before `/dev/mmcblk1p1` enumerated; oneshot never retries). Root fix per the user's call — **boot must not depend on the SD card at all**: `update.sh` deploys `docker-compose.yml` into the runtime tree and compose runs FROM there; the unit's `WorkingDirectory` moved there with `RequiresMountsFor=/mnt/data` only; the clone is an update-time-only artifact (`e88aa84`; one-time compose project-name migration handled — old-project containers downed before the first new-project up). Second reboot: bridge answering in ~3.5 min, unit clean, zero manual steps. **Riders:** fine-grained `WB_REPORTS_TOKEN` minted → `.env` next to the deployed compose file (survives both start paths; moved from host-env interpolation which `sudo update.sh` would strip); `reports.enabled` flipped **through the repo** (user's own commit `78673b3` — the config-source-of-truth flow used exactly as designed) and verified live (`enabled: true`, token present in the container env). **Found + fixed en route:** container healthchecks lied (`cc6a94d`) — UI probe resolved `localhost`→`::1` against IPv4-only nginx (unhealthy FOREVER while serving fine); backend `start-period` 20s < the device-fleet lifespan (transient unhealthy every boot) → both probes now 127.0.0.1, backend start-period 180s; images rebuilt. Pre-flight same day: INSTALL.md reconciled (UI port 3000 not 80, `state_store.sqlite` not `state.db`, the §4a cutover section: stop-the-other-bridge-first + carry the live state DB). Exit-criteria item 1 satisfied: **armv7 GHCR images on the WB7 via compose, serving the house.** VWB-13 is unblocked.
- [x] **REL-3** `[P0]` `HW-GATED` — **DONE 2026-07-10** (two rack sittings, same day; the converged release verification pass, voice ARCH-25 pattern). **Every HW verification owed by a closed task, run on the deployed WB7 — and the sitting turned up (then closed) a critical scenario-startup bug the mock tests structurally couldn't.** Deliverable evidence: `docs/review/rel3_rack_findings_2026-07-10.md` + two interactive checklist artifacts. **Sitting #1** (13 verified · 4 flagged · 14 not run) flagged a **critical**: START `movie_zappiti` with the TV already on **wedged the eMotiva XMC-2 hard** (wall-unplug required). Same-day live forensics (owner-copied container logs + five controlled power-ups + a remote OSD probe over the Emotiva protocol's menu system, `scripts/emotiva_menu_probe.py`) produced the full causal chain: `set_input` fired 3.3 s into a CEC/ARC handshake the driver's own state already showed → firmware wedge; the wedge+unplug wiped device-side subscriptions (bridge deaf until restart) AND reset the XMC-2's CEC config to disabled (the afternoon's "ARC is dead" mystery, panel-confirmed). Findings F1–F6 filed: **DRV-30** (readiness gate + keepAlive watchdog + re-subscribe on recovery) + **SCN-14** (gate canonicalization + timeout→failed-step) as **P0 release, shipped same day**; DRV-31/DRV-32 deferred (Zappiti IR toggle re-learn; CEC restoration + ARC bench, owner decision: post-release). **Sitting #2** (after DRV-30/SCN-14 redeploy): **the wedge gesture PASSED** — START-with-TV-on ran clean, both switches executed against a live eMotiva for the first time, END + restart-survival green; the sole flag (SCN-11 dialog showed the TV "out of sync") root-caused to two more wire-vs-canonical comparison bugs, both fixed same session: **DRV-33** (LG optimistic input write used the user-assigned label, not the id) + **SCN-15** (unify ALL state-vs-target comparison sites — gate, diff, preview — on one `_satisfies()`). Owner confirmed the dialog reads in sync after the SCN-15 redeploy → **close**. **Verified across both sittings:** bridge healthy + non-root (OPS-17) + golden `5622ba7a1a78102a` byte-match (pre-flight AND closing); the full scenario lifecycle (start/switch×2/end/restart-survival) on live hardware; the HVAC MitsubishiHvac live pass (all 3 ACs from the panel, WB cards correct, UI-16 icons, restore-at-boot — the DRV-26/28/29 arc); DRV-5 force re-tap + SCN-11 reconcile dialog; mf_amplifier mute (ROM20, last DRV-1 residual); end-to-end re-verification after cleanup; check_scope + CI green. **Deliberately not-run / deferred (not release gates):** the DRV-30 watchdog unplug drill, LG reconnect-cycle, VWB-32 broker-restart self-heal, the DRV-26 voice mode-change smoke, UI-13 SSE recovery, VWB-30 reports redaction, CORE-9 MQTT resilience, UI-15 pulse observation — optional-by-design or already unit-proven; `tv_on_speakers` is expected-fail until DRV-32 (SCN-14 now reports it honestly). **The two-room concurrency drill left REL-3 to SCN-13** (second-room scenarios are post-release; nothing to run against). Exit-criteria items 2 + 5 satisfied. Board effect: only **REL-4** (+ VWB-33) stands before the tag.

- [x] **REL-4** `[P1]` — **DONE 2026-07-10** (the release docs pass — exit-criteria item 6; the last `[release]` gate on the critical path). **User-facing documentation made true at the release version, in the voice project's spirit, and scrubbed of all internal tracking language.** Scope reconciled with the owner mid-task: the §0 "master-doc handover / plan-driven→architecture-driven" half was **descoped to the board** (project-governance convention, cross-repo — defers with VWB-33/34); **`project.md` archived** (not user-facing — voice keeps no vision doc; `docs/archive/project.md` with a banner); **`docs/planned/*` ruled NOT user-facing** (internal roadmap/design notes — excluded, and user-facing docs no longer route readers into them). What shipped: **(audit)** a four-way parallel review of every user-facing doc against release reality (architecture ×6, guides ×4, READMEs ×6, ADRs ×6) — 8 already accurate, 11 fixed. **(architecture)** overview (8→9 drivers, four→five ports incl. the missing `ReportSinkPort`, import-linter CI note), key-concepts (dead `/scenario/{id}/activate|deactivate` → `/scenario/start|switch|shutdown`), interfaces (canonical-first: `/canonical` the public write path, `/action` the internal door), **ui.md — DOC-11** (the pre-SCN-6 "dispatch at the role device / `sourceDeviceId`" scenario-manifest narrative rewritten to the render-projection-over-per-room-Scenario-Manager model; appliance section: kitchen-hood-only → +3 HVAC panels, real `/devices/:id` routing). **(guides)** howto-new-device (9 classes + MitsubishiHvac), howto-new-scenario (flat `name` → localized `names`, dead path-param endpoints, two lingering `sourceDeviceId` claims). **(READMEs)** root (pre-release → deployed; roadmap prose, no links into `planned/`), backend (7→9 drivers), contracts (realism-check pending → verified byte-match). **(new)** `docs/QUICKSTART.md` — a tester/first-run guide safe by construction (broker-free `wb-catalog`/`wb-openapi` first, then the test suites, then a local-broker-only full run with the never-run-a-2nd-backend-against-the-house caution). **(ADRs)** 0001–0005 in-force; 0006 amended (stale dep versions/SHA + the unmirrored `pyatv` git source → **OPS-19** filed for the Rule 2 decision). **(diagrams)** `rest-surface` + `ui-architecture` `.dot` sources updated + PNGs re-rendered (canonical write path, shipped HVAC appliances); the other 5 flagged diagrams left as-is per the owner's scope call (not deeply re-verified — a candidate follow-up if wanted). **The full user-facing set is verified clean of task IDs, `§P3.7` refs, plan/journal pointers, and links into internal docs.** No code touched; no contract change. Filed: OPS-19 (deferred). Board effect: **REL-4 was the last critical-path gate — only VWB-16 (voice fixtures, off-path) remains before the tag.**

- [x] **REL-5** `[P0]` — **DONE 2026-07-09** (review task per `review-then-remediate`; split out of REL-3 the same day). **Pre-tag code review of the release-critical backend + UI, adversarially verified.** Deliverable = the frozen review doc `docs/review/rel5_pretag_review.md`. Method: a multi-agent review — 7 reviewers over canonical dispatch, scenarios/reconciler, device drivers, reports/secrets, ops/bootstrap/MQTT/state, UI runtime rendering, UI api/hooks — each weighing correctness/actuation, security, hexagonal conformance, and simplification/test-coverage; **every finding re-read and adversarially refuted by an independent agent** (27 agents, 0 errors). **Result: 20 raw → 19 confirmed (1 refuted): 2 P0, 5 P1 (two are the same broker-password-in-logs defect), 12 P2.** Notable P0s: broker credentials committed in `system.json` + served unauthenticated on the LAN (#1), and the MQTT reconnect counter never resetting → the bridge permanently drops MQTT after 5 lifetime disconnects (#2). **Findings filed as fresh tasks (2026-07-09):** tag-blocking **CORE-9** (#2), **DRV-21** (#3 select-form force), **SCN-12** (#6 teardown reconcile:false), **UI-13** (#7 SSE stale-state); pulled-forward `[release]` **VWB-30** (#13/14/15/16 reports hardening), **UI-14** (#17/19 UI nits); deferred **CORE-8** (#1/4/5/9/12 broker/device secret handling — scoped to productization by the owner, house on a trusted LAN), **VWB-31** (#8), **DRV-22** (#10), **OPS-18** (#11), **UI-15** (#18). P0/P1 remediation lands before the tag (CORE-9/DRV-21/SCN-12/UI-13). No code touched by the review itself. Feeds REL-4.
